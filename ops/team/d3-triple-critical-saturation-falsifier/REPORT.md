# D3 triple critical-saturation falsifier report

Frozen constructor handoff: `97787bed0afae6a2f7cb7d2edf9df4f54bbe97f9`

Verdict: **`Q0_NULL_MINIMUM_DECREASE_NOT_REACHABLE`**.

The untrusted constructor package has a sound exact core.  An independent
reconstruction recovers all 59 formal source equations, 55 nonzero
generators, 14,741 sparse terms, the 62 ordered nonconstant parent walls, both
coordinate-four-space canaries, and all three false-infinity rejection
canaries.  All 27 hostile mutations are rejected.

That structural success is not enough to pass the governed Q0 gate.  The
first load-bearing failure is the absence of an end-to-end executable,
ceiling-bound path from the component-decorated saturation to either the
canonical row removal or a complete exact route obstruction:

1. Every stage contains a future instruction to emit component witnesses;
   zero stages contain a materialized removed-component list or exact
   reduction certificates.
2. The 210-minute numeric envelope covers only the 62 saturation calls.  The
   constructor explicitly leaves dimension, real-root isolation,
   parent-chamber classification, true-infinity continuation, and complete
   `S8` transfer outside that forecast.
3. The runner retains every standard-basis dump but does not measure scratch
   growth or enforce the 10-GiB scratch ceiling.
4. The forecast is marked `LOW`, has no exact-CAS saturation calibration, and
   uses construction/replay timings that explicitly do not measure a
   saturation solve.
5. Even a successful 62-stage run terminates with
   `SATURATION_COMPLETE_Q1_STILL_REQUIRES_DIMENSION_REAL_ROOT_AND_ATTACHMENT_CHECKS`.

The cycle's mandatory midpoint rule therefore fires before a raw research
saturation: the preregistered decrease is no longer reachable under the fixed
ceiling from the presented package.  Q0 is `NULL`, Q1 remains denied, the
trajectory is `STALLED`, and the only permitted action is `STOP`.

This is independent machine falsification, not human review.  It does not
disprove the mathematical saturation route, establish a compact component,
remove the orbit-5563 row, close either D3 branch, or change the `2/9` theorem
ledger.

Replay from the repository root:

```console
python -B ops/team/d3-triple-critical-saturation-falsifier/falsify_q0.py
```


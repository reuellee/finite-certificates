# Independent Stage D review

**ACCEPT the complete fixed-support-plane counterexample and its open-set stability. RETIRE that universal one-column criterion and STOP this cycle at its negative endpoint.** Research credit is INFORMATIONAL. The ledger remains 2/9 and all seven canonical obligations remain open.

## Frozen acceptance and independent arithmetic

The principal is `fdd143c8be96636b7b11af0ac7e2ea8d60b65eca`; the separate falsifier certificate is `52ad4e14c63831243f0a57b193e99ebbf32891e5`. Acceptance reads immutable Git blobs. No producer search, verifier, or acceptance code was imported or executed. `SOURCE_PINS.json` records the old constructor and separator-pool bytes, the actual NPZ and catalog, the second certificate, and the principal comparison output.

The new standard-library checker decodes exact integer NPY payloads, binds the matrix literally to upper178 chart zero of parent 2599, reconstructs colex indices by combinadic rank, checks all 70 labeled catalog signs, and evaluates each normal as `a_I[j]=det[y_i,y_j,y_k,e_j]`. Recursive Laplace determinants and **all four coordinate 3-by-3 minors** determine pair closures independently of the coordinator's Cramer membership implementation. Primitive normals are derived by positive gcd division and agree with the second certificate.

| Independently verified quantity | Result |
|---|---:|
| New rank-predicate points | 1 |
| Indexed pairs / distinct labeled closures | 1,680 / 896 |
| Distinct closures of size 6 / size 2 | 56 / 840 |
| Indexed closures of size 6 / size 2 | 840 / 840 |
| Strict retained-row inequalities per certificate | 65,520 |
| Minimum integer margin | 162,514,748,513,691 |
| Nonzero pair-rank / exclusion-rank witnesses | 1,680 / 28,560 |
| Coordinate 3-by-3 minors evaluated | 141,120 |
| Full strictly positive rank-four circuits | 3 in each certificate |

The existing 23-vector pool suffices; deterministic replay uses 11 vectors. Every expanded system includes all 35 nonstar rows. Both coverage tables pass independent set equality and strict dot products. `EXPANDED_COVERAGE.json` records the selected vectors; `RANK_WITNESSES.json` records a nonzero pair minor and every excluded-row rank-three minor. The output hashes bind the reconstructed table, all selected strict evaluations, and all rank witnesses.

Twelve hostile mutations fail, covering missing or duplicate choices, omitted closure additions, an inserted nonmember, zero separator, false margin, proportional star pair, corrupt or zero circuit data, and signature drift. The decisive expansion canary uses label 1, pair `[0,2]`, and old pool vector 0: all 37 old retained rows are strictly positive, but added row 21 has evaluation `-1462394266683677639253814792112316043753468487619159`. Thus added rows are actually tested.

The accepted Stage C original is `964c427f6b71b6223add7dc7fd3a695d4c7db270`. Both frozen independent checkers were replayed from Git without editing their lane. Every non-time output equals its original acceptance output. This re-establishes the same-parent admissibility, anchors 3/14/2, all six ordered noninclusions, signature validity, 3,780 GP checks, and the normalized nine-dimensional gauge. All eleven files in the normalization manifest equal the originals after CRLF-to-LF normalization. This inheritance supplies these already accepted bindings; Stage D's stronger rank predicate is independently checked here.

## Exact predicate and conditional escape

At a uniform parent, all star normals annihilate the nonzero column `y_e`, so their total rank is at most three. Distinct star normals cannot be proportional: their equal hyperplane would contain at least four distinct parent columns, contradicting uniformity. Every pair therefore has rank two.

Let positive support mean precisely the rows with positive witness weight. If the union of incident positive supports of three nonzero nonnegative witnesses has rank two, choose two spanning rows from that union. Every supported incident row belongs to their closure. If its rank is zero or one, pad with distinct star rows; uniformity guarantees an independent pair. Conversely, three witnesses on the nonstar rows plus any pair closure have incident union of rank at most two. Added rows may have zero weights. No circuit minimality, positivity on every retained row, or constant choice of witnesses is assumed. Strict separation of even one restricted block contradicts any nonzero nonnegative dependence on that block. Exhaustive separation consequently forces incident-union rank **exactly three** for every label and every triple of full-system witnesses at this point.

The conditional escape argument remains valid for the complete criterion. The annihilator of a spanning/padded pair is a two-dimensional vector plane through `y_e`; it preserves every positively supported incident hyperplane. Five of the seven fixed labels form a projective frame, with only scalar stabilizer on oriented rays. In its signed-simplex section `sum tau_j x_j=1`, this plane cuts an affine line. Parent signs cut out a bounded open interval containing the initial column. A finite endpoint is a genuine parent bracket wall; coordinate-zero walls are also bracket walls. The fixed frame remains valid there, so the limit cannot be removed by changing quotient chart.

Before that endpoint, uniformity keeps every selected triple spanning its original hyperplane. Its normal is a positive scalar multiple `c_I(t)` of the initial normal. Dividing witness weights by these scalars and renormalizing transports all three dependencies. Zero weights remain zero. Supported row ranks are preserved by positive row scaling even when initially deficient; unused rank strata impose no extra condition, and no witness limit at the excluded endpoint is required. Reparameterization gives a nonconstant proper path in the triple-bad intersection. If this condition held at every point, a nonzero compactly supported locally constant degree-zero class would contradict constancy along that proper path. No global continuous selection of paths is required. There is no pair-Hc1 conclusion.

## Open-neighborhood scope

For each indexed pair, its recorded nonzero two-coordinate minor persists nearby. For every star row outside its original closure, the recorded nonzero three-coordinate minor persists. A nearby pair closure can therefore only shrink. No persistence of its original zero minors is assumed. Every fixed separator stays strictly positive on its original retained rows, hence also on any smaller nearby retained set.

For each full five-row signed support, all deletion determinants are nonzero, and the alternating cofactors have one strict sign and are proportional to the supplied positive weights. The cofactor identity supplies a positive dependence throughout a neighborhood. These circuit signs, the 70 parent signs, the rank witnesses, and all strict separator evaluations form a finite family of open conditions. Their intersection contains the exact point and remains triple-bad while failing every complete fixed-plane choice.

This is also a nonempty open subset of the actual nine-dimensional parent space. Rank, nonnegative dependencies, and strict separation are invariant under invertible orientation-preserving coordinate changes and positive column scalings, using normal covariance and positive row rescaling. Transport the raw neighborhood to the accepted normalized representative and intersect it with the nine-dimensional section. The Stage C replay verifies that section and all 56 covariance identities. Coordinate choices of the recorded raw minors need not be preserved by the gauge change; their certified rank conclusions are invariant. No quantitative radius, maximal obstruction region, or component count is asserted.

## Decision and freeze

The retired statement is the **universal complete fixed-support-plane one-column escape criterion**, equivalently incident witness-support rank at most two. Rank three fixes that column's projective ray if the other columns and the selected support hyperplanes are held fixed. This does not exclude paths that rotate support planes, change witnesses, move multiple columns, or first move elsewhere. It neither constructs a compact triple-bad component nor refutes D3/9DVL or constructs a compactly supported class.

All seven obligations remain open: `global_gluing`, `extension_labels`, `strict_closure`, `relative_infinity`, `middle_rank_replay`, `diag3_pair_hc1`, and `diag3_triple_hc0`. Theorem delta is zero. No successor route is opened by this review. Prior lanes, ledger, and canonical files were not edited; no cohort, search, external/paid compute, human contact, push, or merge was used.

Replay `python -B ops/team/ai-d3-rank-pencil-referee/independent_check.py` with any standard Python 3 environment. The inherited binding replay is `python -B ops/team/ai-d3-rank-pencil-referee/replay_frozen_stage_c.py` and needs NumPy solely for the original Stage C NPZ decoder. `freeze_replays.py` writes both outputs with LF. Exact Git objects must remain available; no producer worktree path is a dependency. Resource records report the bounded role and measured checker memory. The owned local commit is the frozen handoff; its SHA is returned separately.

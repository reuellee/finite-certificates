# D4-S53 opening strategy review

Date: 2026-08-30 UTC

Track: `diag4-s53-referee`

Verdict: **GO**

This is an opening-only audit. No successor prover or falsifier conclusion was
supplied or inspected. The referee must resume only after exact candidate
revisions and artifact digests freeze.

## Canonical identity and source accounting

The assigned control-plane revision is
`f274ea35a6c96cbd0f094847eac5b9c6610516c7`, tree
`4a6aa3750fe7652e39ccf23e7b9a45cadbddfdce`. Its first and only parent is the
declared canonical mathematical/publication base
`aa784af939b55d3503e4782a9d65a9b06cf81ce0`, whose tree is exactly
`6aa36a92c5e5d2e420ec660a1ad2c2be2b06a561`. Relative to that base, the
control-plane commit adds only `CYCLE.md` and `WORK_ORDERS.yaml` for this
cycle.

The canonical ledger file has SHA-256
`f4360254e5c7e624b9c9194bb7cb0b3844d5fe3201ec9bc688c2f18d37276782`
and continues to state the honest score `2/9`. All nine canonical input
digests listed in the cycle and work orders match the checked-out bytes:

| Input | SHA-256 audit |
| --- | --- |
| `ai/omreal/NINE_DIAGONAL_STATUS.md` | PASS |
| `ops/research-team/PROTOCOL.md` | PASS |
| predecessor prover `PROOF.md` | PASS |
| predecessor prover `RESULT.yaml` | PASS |
| predecessor prover verifier | PASS |
| predecessor falsifier `FINDINGS.md` | PASS |
| predecessor falsifier `RESULT.yaml` | PASS |
| predecessor falsifier verifier | PASS |
| row-2599 shatter certificate | PASS |

The two control-plane file digests at the reviewed revision are:

- `CYCLE.md`:
  `eeffafa339fe195c1044a3c7b8e250b975f35911ee923e31f6f978e5321fc926`;
- `WORK_ORDERS.yaml`:
  `e842af2b52c85a45745a8b789e4f905344d174d4d3ccba7ae9b262e2370be7c4`.

The movable local `main` ref in this checkout is stale at the canonical
base's ancestor `d047359e7892106021022b0401554f56eb4e4d8a`; it was not used
as mathematical evidence. Before integration, the coordinator must perform
the already-required authenticated remote-main and exact-head repository
check. The pinned base object, tree, ancestry, and current control-plane
contents are internally consistent, so this local ref condition is not an
opening target defect.

## Eight-factor strategy gate

The opening comparison explicitly scores all eight mandatory factors:
ledger leverage, quantifier readiness, coverage burden, terminality,
structural compression, independent-verification cost, resource/information
return, and stagnation risk. The one-cycle `CONTINUE` decision is coherent:
the accepted predecessor strictly reduced the complete D4-SP domain from
`130` to `53` orbits, while the new target requires signed topology rather
than another unsigned statistic. Diagonals three and nine remain named pivot
reserves, and diagonal eight is retired as the primary target. The comparison
therefore neither hides the alternatives nor treats local infrastructure as
claim progress.

## Exact D4-S53 target and domain lock

The target quantifies over every realizable uniform rank-four oriented
matroid on labeled `[8]`, every proper pairwise-incomparable four-signature
family in the 9DVL domain, every selected `rho`, and every cover-all circuit
support whose unsigned `S_8` orbit is in the complete B31-resistant class.
It covers the entire closed `C_(rho,Q)`, including zero-weight faces and all
structural and residual-wall specializations in the normalized realization
cell. The work orders additionally keep sign/chart transport, orientation
local systems, and all split, merge, birth, and death behavior used by a
candidate inside the acceptance boundary. No sampled chart, abstract model,
or support-preserving subset can silently replace these quantifiers.

The predecessor semantic-kernel replay passed and returned:

| Domain | Labeled supports | `S_8` orbits |
| --- | ---: | ---: |
| complete cover-all sizes 3/4/5 | 1,715,980 | 130 |
| B31-certified | 915,740 | 77 |
| B31-resistant survivors | 800,240 | 53 |

The survivor orbit split is exactly four size-four and forty-nine size-five
orbits. A separate in-memory reconstruction, which imported neither discovery
checker, independently enumerated the complete cover-all domain, recomputed
the B31 predicate and full `S_8` orbits, and reproduced the semantic SHA-256

`16b11cba052b49af777354f256a783b419ec6e246d178de70c238807e50ecc11`.

Thus the `53`-orbit class is both complete relative to the accepted
`130 = 77 + 53` partition and semantically identical to the class named by
D4-S53. The actual signed survivor canary
`123/134/267/258/468` also replays, so the residual domain is not merely an
unattached unsigned census.

## Terminal outcomes, stop rules, and mandatory pivot

The bounded target is terminal in both relevant directions:

1. A positive D4-S53 result must be a universal signed proof on the complete
   class, or a universal signed theorem that strictly removes a completely
   enumerated nonempty subset and lists every survivor.
2. A refutation must give an exact admissible `(M,S,rho,Q)`, a checked nonzero
   compact-support class, and a checked map showing that the class survives
   inclusion into the entire closed circuit piece.

The row-2599 four-parameter subset remains only a discriminator until both
its compact-support topology and its full-piece inclusion are checked. The
canonical falsifier replay passes while preserving both caveats. An exact
exclusion of that candidate is a valid bounded track result but is not
misstated as a global D4-S53 reduction.

Each discovery track is limited to one agent turn and at most 90 minutes of
ordinary local compute. Null and timeout handoffs must pin the searched signed
domain, unchanged survivors, failed hypotheses, and next discriminator. If
D4-S53 is neither proved nor refuted and no universal signed theorem strictly
reduces the complete `53`-orbit class, the closing verdict is unconditionally
`PIVOT`, and no third D4-S53 cycle may begin.

## Independence, canaries, and publication gates

The coordinator, prover, falsifier, and referee have disjoint roles,
branches, and edit surfaces. Only the coordinator may integrate or change the
ledger. Candidate generation and acceptance must be separated, and the
referee is forbidden to repair a candidate or reuse discovery-side acceptance
logic. Those controls satisfy the opening independence gate.

The declared canaries cover an actual signed survivor, an actual signed B31
case, an out-of-domain null, split--remerge and separate-convexity false
positives, orientation/sign mutation, boundary behavior, and the crucial
subset-inclusion false positive. The predecessor replays pass their applicable
positive, negative, null, and hostile canaries. Candidate-specific canaries
remain future fail-closed gates.

The control plane correctly states the nonconsequences: even D4-S53 would
prove only D4-SP together with B31, while multi-piece terms, adjacent degrees,
compactification, sign transport, restriction maps, fivefold exactness, and
diagonal four remain open. No opening ledger change is authorized.

The publication authorization and prohibitions are present in every work
order. Publication remains gated on pinned artifacts, exact replay,
independent coverage/transport/adversarial review, protected checks at the
exact reviewed head, an honest ledger, an authenticated remote-main check,
and the coordinator's merge decision. Workers may commit or push only their
assigned branches and may not merge.

## Opening disposition

No actionable opening defect was found. The strategy, target lock,
quantifiers, complete survivor identity, terminal outcomes, pivot rule,
independence controls, resources, canaries, nonconsequences, and publication
gates are sufficient to start the bounded tracks. **GO**, then stop until
exact frozen candidate handoffs are supplied.

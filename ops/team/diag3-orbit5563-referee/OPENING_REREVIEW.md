# D3 residue-orbit global-exit opening rereview

Date: 2026-08-30 UTC

Track: `diag3-orbit5563-referee-rereview`

Opening verdict: **GO**.

The corrected control plane closes all four defects in the frozen `MODIFY`
review. Discovery may begin, but only under the 30-minute two-layer gate. No
new roadmap, collar, clipped-wall, or macrobox computation may start before
the quotient manifest and full-realization-space transport contract both
pass.

This verdict is bound to the exact corrected content below. It does not accept
any future discovery conclusion, change a residue count, or change the theorem
ledger.

## Frozen corrected identities

| Object | Identity |
| --- | --- |
| canonical base | commit `aa784af939b55d3503e4782a9d65a9b06cf81ce0`, tree `6aa36a92c5e5d2e420ec660a1ad2c2be2b06a561` |
| corrected local control plane | commit `5aae5f2680e436cf96499ae54be7ae3a8fae2957`, tree `d93a552a15f2165473a73b9f01db8568a54d5da0` |
| corrected published control plane | commit `a3d0e04cdb2b8a61b62f44da1aceddcf40e96cc1`, tree `d93a552a15f2165473a73b9f01db8568a54d5da0`, branch `research/diag3-orbit5563-global-exit-20260830` |
| published merge parents | initial D3 control `4d59201e84fd588b91a2a858ac4468c18b863544`; frozen D4 PR #43 head `06de9659d5090846afe09c95f1f8549d1d7b2ba5` |
| D4 predecessor publication | PR #43, head `06de9659d5090846afe09c95f1f8549d1d7b2ba5`, tree `692d173462497cb645127ea1ced6cbe40aba4d5a`, open and unmerged |
| canonical main observed during rereview | `aa784af939b55d3503e4782a9d65a9b06cf81ce0` |

The authenticated GitHub connector resolves the corrected branch to the
published commit above and PR #43 to its pinned head. PR #43 remains a
separate CI/merge publication gate; its open state does not block this
opening verdict because the D4 cycle is frozen, incorporated as the published
merge parent, and explicitly nonconcurrent.

For content authentication, the remote blob identities of corrected
`CYCLE.md`, corrected `WORK_ORDERS.yaml`, and
`verify_cycle_protocol.py` are respectively
`f8588047caf2fa961e363f41c8f746f6d4ada7e3`,
`ec0147dd82a4c56cea2ee3aa2d4c476b6b5e08ee`, and
`936850935eecb53b72f10659de15aaa1b11e6379`; each equals the local Git blob.
The three pinned D4 closing files likewise match their remote PR-head blobs.

## Repair gate table

| Required repair | Result | Independent finding |
| --- | --- | --- |
| frozen D4-S53 identity and nonconcurrency | PASS | The control plane pins integrated local/published commit and tree, PR #43 status, all prover/falsifier/closing-referee commits and trees, both closing-artifact digests, `2/9`, `800,240 / 53`, and mandatory `PIVOT`; GitHub reports PR #43 open at the exact head and `main` unchanged. |
| null versus timeout | PASS | Both discovery work orders and `CYCLE.md` classify an incomplete quotient manifest at 30 minutes as `timeout`; `null` requires the complete exact manifest and a missing transport/attachment proof. |
| verbatim authorization | PASS | The YAML literal equals the protocol block character-for-character after removal only of Markdown blockquote prefixes; worker restrictions are separate. Four hostile mutations—repository backticks, sentence spacing, Drive-path backticks, and `gh` backticks—change the extracted text and fail equality. |
| type/full-space domain guard | PASS | The control plane distinguishes `2,604` unlabelled realizable types and one representative matrix per type from each complete normalized realization space; it pins `2,604 * 40,320 = 104,993,280`, selected-triple stabilizer order one, and requires quotient multiplicities to sum to the raw census before topology. |

The prior `MODIFY` artifacts remain unchanged under
`ops/team/diag3-orbit5563-referee/`; this rereview adds new artifacts only.

## Independent replay

| Gate | Result |
| --- | --- |
| `python3 ops/research-team/verify_cycle_protocol.py` | PASS: three governed cycles, nine work orders, exact standing authorization in every governed work-order anchor |
| factor-action/Burnside kernel | PASS: factors `6`, pairs `9,476`, triples `79,102,449`; semantic SHA-256 `9dc473537e87e509031d4843d960f5ea4bfefb8508262cd1ebb5d44e1a49913d` |
| global-exit criterion | PASS: accepting complete component-faithful exit graph is sufficient; graph rejection remains inconclusive; artificial-boundary and omitted-edge canaries reject |
| full-space feasibility gate | PASS replay of expected `FAIL_CLOSED`; semantic SHA-256 `874c4895ae17843c6827c1c3a8d528eac0b45fc35dedc9159e4f447786ed2ace` |
| D3 completion object and decision ledger | PASS: `77,940,147 / 79,102,449`, residue `1,162,302`, pair closure open, ledger `2/9`, old clipped-wall route refuted |
| parent/frame atlas | PASS: `2,604` types, `40,320` frames, `104,993,280` raw presentations, hard-triple stabilizer trivial; exact sign subatlas `17,105,952 / 104,993,280` remains explicitly non-global |
| boundary stratification | PASS at declared scope; no primary decomposition or global closure claim |
| all 19 raw path-valued D3 canonical inputs | PASS SHA-256 equality |
| D4-S53 prover replay and manifest | PASS complete null: `1,715,980 / 130 = 915,740 / 77 + 800,240 / 53`, survivors unchanged |
| D4-S53 falsifier replay and manifest | PASS at declared local scope; whole-domain topology and full-piece inclusion remain unreached |

The exact target remains the canonical unresolved row
`(5563,4373,23221)`, authenticated by presentation
`(5563,16134,19284)`. A positive result must cover every point, component,
required stratum, and compatible transport over all complete normalized
realization spaces. A representative-matrix census alone is never a component
certificate.

## Discovery authorization gate

The prover and falsifier may now start independently on their assigned
branches and surfaces. Their first 30 minutes are restricted to:

1. an exact type/frame/`S_8` quotient manifest whose multiplicities sum to
   `104,993,280`; and
2. a replayable sign/chart transport contract over every point and required
   stratum of every complete normalized realization space.

The selected triple's trivial stabilizer is a required canary, not permission
to skip quotient accounting. If the manifest is incomplete at 30 minutes, the
track stops with `timeout` and a fail-closed partial frontier. If the manifest
is complete but transport or attachment remains missing, the track stops with
`null`. Only if both layers pass may discovery proceed within the balance of
the 90-minute total and the 12-GiB track ceiling.

Positive promotion still requires complete stable component IDs, sound
continuation, exhaustive component/stratum coverage, and sound true-parent-
infinity tags with every sink SCC meeting infinity. Only that exact outcome
may recommend `1,162,302 -> 1,162,301`. An exact compact component requires a
complete closure certificate disjoint from true parent infinity. Graph
rejection, samples, stored matrices, artificial boundaries, or local charts
cannot change any count.

The ledger remains `2/9`. If this cycle produces neither the exact row removal
nor an exact compact component, closing must issue the mandatory pivot and no
further local box/collar/macrobox/clipped-wall cycle for this residue may
start.

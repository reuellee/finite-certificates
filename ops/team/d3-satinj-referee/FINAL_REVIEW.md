# Independent final review: exact support event, global attachment still open

**Verdict: ACCEPT_AUXILIARY_AND_SCOPED_NEGATIVE_ONLY. Original target: NULL.
F4SAT gate: NO_GO. Theorem trajectory: STALLED. Final action: STOP.**

The reviewed constructive revision is
`3c3ccbcac95fe05543a870c86da1b2d29342f526`; the reviewed falsifier revision is
`1f8be67c47679d4283edec4b67ba3ae70c1c4818`. `INDEPENDENT_REPLAY.json` pins all
eleven candidate files and sixteen distinct inherited sources. These inputs
were read from frozen Git objects. No producer or historical acceptance code
was imported. All event arithmetic was reconstructed independently using
standard-library exact fractions, Leibniz determinants and ranks witnessed by
minors. The historical NPZ was decoded directly with the standard library.

## Constructive proof audit

The conditional local sign collar is correct on its declared domain.
Including all nonzero normal minors of orders one through four determines
the ranks and signed circuits of the actual derived-normal configuration.
Where exactly the chosen factor q can vanish, all other factors and chart
denominators are nonzero. The implicit-function theorem at dq nonzero makes
q a transverse coordinate. Every minor has fixed sign/zero status on each of
the two sides and the zero cell. On any minimal dependence, a nonzero
coordinate minor supplies its signed circuit coefficients, so the existence
of a positive circuit is constant on each cell. This argument applies to
each fixed original extension signing. Gordan's alternative then gives the
simultaneous product description for the actual bad-set membership diagram.

The zero cell includes smaller supports; it is not obtained by deleting zero
weights. This proves only membership constancy, not a witness-polytope
trivialization or a continuous choice of witness. Relative closedness of the
bad loci justifies the five listed transverse possibilities.

The smoothness ideal `<q,dq>:u^infinity` describes the localized singularity
test, with its zero set interpreted on u nonzero. A rational identity
`u^N = a*q + sum b_j*partial_j(q)` would exclude that singularity there.
No particular complete minor-factor list, ideal instance, computational size
forecast, or such identity has been produced. Its claimed conclusion remains
conditional. The explicit real equations for h>0, normalized nonnegative bad
witnesses, and strict separating good witnesses are correct: the square
slacks enforce the claimed inequalities over the reals. They do not encode
compact support, a nonzero class, or a class killed by D.

The first frozen prose had one reversed localization sentence. At referee
request it was corrected: saturation by a Jacobian minor keeps the locus
where that minor is nonzero, while its zero branch remains an unresolved
separate case. The corrected bytes and refreshed RESULT hash were checked.
This repair changes no candidate equations or conclusion.

The original-scope design gate still fails. The collar provides a transverse
germ; its missing complement, global component topology, signed incidences,
overlap maps and true-infinity comparison remain unconstructed. The mixed
prism candidate lacks the actual full-domain prism map. The critical-point
candidate lacks a specified stratified proper-function argument that turns
critical exclusion into detector nondegeneracy. None is an original global
attachment reduction or a certified smaller exhaustive residual. Thus the
conditional lemma does not escape the previous diagram-replacement null.

## Independent event reconstruction

The exact source chart is bound to the inherited NPZ and the original flow
record. The three signatures are unchanged. Independent checks bind the
properness/incomparability anchors to charts 3, 14 and 2 and their original
source points, verify 168 strict primal inequalities and six positive dual
dependencies, and check all 3,780 uniform extension GP sign relations.

At the reported rational coordinate motion

`t = 37864449186859942661765893 / 7029591949415530407677535`,

all seventy parent determinants are nonzero with their original signs. The
independent arithmetic matches every stored event determinant and all fifteen
normalized selected-support weights exactly. The first weight of block 0 is
zero and its other four weights are positive; all five weights of blocks 1
and 2 are positive. Every weight vector sums to one and satisfies its four
Gordan equations. The selected five rows, surviving four rows, and augmented
five rows have ranks 4, 3 and 5, respectively.

The selected cofactor has constant term
`-1590306865848117591794167506` and slope
`295242861875452277122456470`. Its affine degree is structurally justified:
only one of its four normal rows involves the single moving parent column.
The reported t is its exact root. At t minus and plus 1/1000000, the parent
signs remain unchanged and the selected block-0 weight signs are respectively
`(+,+,+,+,+)` and `(-,+,+,+,+)`. Blocks 1 and 2 remain bad at both neighbors.
No claim of block-0 goodness or badness on the right is justified by this
selected support. Since each parent bracket is affine in the one-coordinate
motion, equal nonzero signs at the endpoints also keep the whole intervening
parent interval strict.

Two separate arithmetic corruptions are rejected: altering a normalized
weight and shifting the claimed event parameter. The exact reconstruction is
the acceptance evidence; the falsifier's own four controls are additional
producer-side checks and are not counted as independent referee tests.

## Exact algebraic negative and its scope

The point annihilates all sixteen generators of the stated event ideal:
twelve coordinate dependence equations, three normalizations, and the one
zero-weight equation. Every factor of the parent-unit product is nonzero.
Evaluation at this rational point therefore extends to the localized
coordinate ring. If any power of that product belonged to the event ideal,
evaluation would make a nonzero rational equal zero. Consequently its
parent-unit saturation is a proper ideal over Q.

This is an exact negative for the proposed deletion of **all selected-support
zero-weight events** using parent units. It requires no modular solver or
Gröbner calculation. Adding the event cofactor or zero weight to the inverted
factors would remove an admitted interior stratum and require a separate
attachment proof.

The event is a witness-representation change. It does not establish a change
of bad-locus topology, a singularity of the augmented witness system, a
cohomological kernel, or failure of a collar. It therefore does not refute
the conditional local lemma, detector injectivity, or saturation-assisted
attachment methods in general. Its rank-five augmented system is consistent
with a regular local description. The original global frontier blocks remain
unknown.

## Cohomology and convergence gates

The direct degree-one cochain test of D uses degrees zero through two only
after a genuine finite global compact-support comparison is available. It
must retain all three pair contributions and their signs jointly. A small
transverse normal slice is not that comparison. The older balanced-end N/M
test additionally assumes `Hc0(T)=0`; the present candidate correctly refrains
from using it unconditionally. The independent triple obligation stays open.

The construction gate failed at the first structural null; discovery stopped.
The frozen worker records consistently report zero F4SAT runs, zero modular
screening runs and zero computational saturation certificates. No new engine
run was needed to establish the exact event ideal's properness. No source
census, full denominator or new global attachment is claimed. The main
saturation budget was not activated or expanded.

The opening vector was
`(2/9,1,{diag3_pair_hc1,diag3_triple_hc0},7,UNKNOWN,UNKNOWN,12,15)`.
After counting this independently accepted bounded cycle once, the closing
vector is
`(2/9,1,{diag3_pair_hc1,diag3_triple_hc0},7,UNKNOWN,UNKNOWN,13,16)`.

Original obligations closed: 0. Original residual decrease: 0. Pair coverage
and residual: UNKNOWN. Triple source counts remain unchanged and are not
component counts. The preregistered minimum decrease was not reached. All
seven original obligations remain open and the theorem ledger remains 2/9.

The theorem trajectory is STALLED; the conditional local result and precise
support-deletion exclusion are auxiliary information. Retire only the exact
universal support-deletion candidate. Stop this saturation/injectivity attempt
with no construction-ready successor. The same global attachment blocker
precludes same-route continuation on the current inputs.

## Reproduction and authority

Run the independent frozen-input replay without modifying the checkout:

```text
python -B ops/team/d3-satinj-referee/verify_frozen_candidates.py --prover-revision 3c3ccbcac95fe05543a870c86da1b2d29342f526 --falsifier-revision 1f8be67c47679d4283edec4b67ba3ae70c1c4818
```

The command writes JSON only to stdout by default and uses no timing field.
The separate `--output` option is reserved for explicit report creation.
`OPENING_SOURCE_AUDIT.json` and `INDEPENDENT_REPLAY.json` are the source and
frozen-candidate manifests. Written topology arguments are independently
reviewed proofs, not Lean formalizations or human review.

This worker changed only its assigned new surface, with local commits. It
made no remote publication, human communication, paid service request,
historical-ledger edit or durable-storage assertion. The coordinator owns
integration and recovery; this review does not certify later uploads.

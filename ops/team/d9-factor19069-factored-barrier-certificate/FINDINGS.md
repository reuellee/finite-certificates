# Producer-independent factor-19069 factored-barrier certificate

## Verdict

**ACCEPT**, only at the preregistered exact fail-closed null endpoint
`HASH_PINNED_FACTORED_BARRIER_CRITICAL_COMPONENT_FRONTIER_WITH_FIRST_UNSAMPLED_COMPONENT`.
The reviewed constructor-plus-falsifier handoff is frozen at
`27f934f5030c2912fbe760656dd38a201ee2e31c`, tree
`b147b9658ed51b6c4c2730e628b38aec97178dd7`.

This certificate does not import the constructor, its checker, the falsifier,
or the falsifier checker.  It treats their JSON as frozen data and reconstructs
the accepted envelope from twenty immutable mathematical sources.

## Exact replay

The verifier independently reconstructs the seventy ordered sign-normalized
row-2599 parent factors, 209 sparse terms, and degree-90 product circuit.  The
barrier stays factored.  All nine derivative nodes retain all seventy
provenance-bearing summands, giving 630 exact `dH_I product_(J != I) H_J`
records.  The degree-six, 108-term factor-19069 polynomial, its nine
derivatives, and all 36 ordered coefficients of `dB wedge df` replay exactly.

The full-support critical system retains every parent inequality, the exact
path selector to the pinned parent component, singular wall pieces, and all
possible component dimensions zero through eight.  It honestly contains no
connected-component decomposition and certifies zero zero-dimensional and
zero positive-dimensional component samples.

All 3,375 compactification supports are accounted for: 3,364 are excluded by
the parent Bernstein gate and ten proper candidates remain.  The exact linear
path to support `(15,7,15)` is certified through all seventy factors.  The
tested linear path to `(1,1,1)` first fails at factor `2578`; no claim about
the nonexistence of another path is made.  All ten boundary wall residences
remain unclassified.

All 2,800 parent tags on the forty-edge skeleton replay.  Factor 19069 has one
open root on edge 39 and none on the other 39 edges.  The root is a local wall
anchor, not a barrier-critical sample and not a global component certificate.

The verifier rejects 22/22 semantic-hash-resealed hostile mutations covering
factor loss, expansion, derivative and wedge provenance, generic-only and
positive-dimensional omissions, singular and connected-parent omissions,
boundary/path corruption, collar globalization, false attachment completeness,
endpoint promotion, and ledger drift.

## Scope

The accepted exact consequence is that the null frontier is hash-pinned,
source-derived, complete as an obstruction envelope, and honest about its
first unsampled interior stratum and first unclassified boundary support.
There is no component count, no complete attachment classification, no
diagonal-nine proof or counterexample, and no basis for changing the theorem
ledger from `2/9`.

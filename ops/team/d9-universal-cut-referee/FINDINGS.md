# Independent closing review: universal D9 cut opening gate

Reviewed commit: `39035a9c35b27a9893db393dbb8a9ec1b621754d`

Reviewed tree: `e05535180e3522521f5f151f956a9f407f5a2956`

Verdict: `ACCEPT`

Accepted endpoint: `NULL / UNIVERSAL_CUT_SCHEMA_COVERAGE_GAP`

`ACCEPT` means that the frozen candidate correctly fails the opening gate and
correctly denies the main census.  It does not mean that the opening gate,
universal coverage, diagonal nine, or any theorem-ledger promotion is
accepted.

## Frozen-head and evidence review

The supplied commit and tree match exactly.  Its parent is the integrated
evidence commit `9cb3652b00c0a07901b6fa32dd3f7d03987cfb66` with tree
`37f605949a2fbfdf8be8983b6ea24121eea4a177`.  The closing commit adds only the
candidate, report, and candidate verifier.  I checked all 87 source-pin
declarations across the five worker manifests, representing 51 unique paths,
against blobs at the frozen commit.  There are no conflicting duplicate pins.

## Opening requirements

| Requirement | Verdict | Independent reason |
| --- | --- | --- |
| Every feasible-component cut maps to the finite schema | `FAIL` | The required complete compactified component-faithful incidence object is absent.  Only a conditional finite-graph cut lemma is established. |
| Signs, duplicate occurrences, charts, properness, multiwalls, recursive facets, and infinity survive | `FAIL` | Projection is not closed, strict-coface attachment is missing, and no complete tagged compactification or ceiling certificate exists. |
| A proved D9 hypothesis rejects the disconnected countermodel mechanism | `FAIL` | Source reconstructibility excludes the abstract models as literal D9 instances, but no proved global sign-geodesy or component-faithful attachment theorem excludes their mechanism. |
| `S12,37` and factor-8552 replay only as hostile canaries | `PASS` | The opposite-parent-form no-go replays; factor 8552 has exact negative/zero/positive lifts, all confined to `[1237]=0`, and is not promoted to a strict-parent separator. |
| Portable source replay avoids `ca730426...` | `PASS` | The object is absent.  The allow-listed source-derived predecessor replay reconstructs the local endpoint without registering or dereferencing it. |

The opening gate is therefore `FAILED_CLOSED`.  Its stop rule fires before any
72-hour / 320-CPU-hour obstruction census; the frozen candidate correctly
records that the census was neither authorized nor started.

## Exact projection and local-memory findings

I independently reconstructed all 70 normalized parent brackets and the
26,740 primitive residual factors.  For residual type 36,

`q36 = a*f - c*d + c - f`,

restriction to `a=[1346]=0` gives `c*d-c+f` up to sign.  That polynomial is
absent from both catalogs.  The full pinned source audit independently replays
142 new first-layer irreducibles with degree census `2:23, 3:71, 4:43, 5:5`
and explicitly disclaims chamber coverage, sign-geodesy, and diagonal nine.

The finite local frontier remains exact: 13 wall types, 671 opposite-partner
pairs, and 2,420 persistent-support candidates.  The 16-chamber counterpair
has the same radius-one typed stars, circuit/transport records, and multiwall
inventory in both gluings, while its positive sectors have two components
versus one.  This refutes memoryless local completeness only.  The artifact is
an abstract signed chamber graph, not a `UOM(4,8)` realization and not a D9
counterexample.

## Source discrepancy

The predecessor elimination note prints an auxiliary total of 131.  Its 13
displayed row counts sum exactly to 123.  Recomputing `binom(m,2)` from those
rows gives 671, and applying the ordinary/localization support multipliers
gives 2,420.  Thus `131` is a preserved, non-load-bearing arithmetic typo; the
two load-bearing totals do not change.

## Boundary and countermodel scope

For factor 8552, `q=d*i-e<0`, all three exact lifts have `[1237]=0`, 69 other
parent brackets positive, and factor signs `-/0/+`.  The 64-chart atlas
identifies this coordinate face as a genuine parent divisor.  Opposite initial
forms `+n4` and `-n4` make the ordinary common-radial strict link empty.  No
strict coface with `[1237]>0` is certified.  The factor is a relative boundary
end, not a strict-open-parent or global separator.

The exact two-wall polynomial model has two all-positive components with the
same factor-sign word and 72 ordered noninclusion witnesses after its
nine-region lift.  The nested/disjoint pair also shows that identical local
link inventories and a common leading form at infinity do not determine
global component count.  Both are logical countermodels to memoryless
local-to-global inference, not source-realized D9 counterexamples.

## Certificate and portability

The endpoint-neutral certificate kernel accepts four synthetic endpoint
fixtures and rejects their promotion to live adapters.  Its hostile harness
rejects 32/32 mutations.  No live universal-coverage adapter is registered,
so it supplies no mathematical endpoint on its own.

The predecessor replay returns 3,539 active classes, 6,167 occurrences, 5,026
aligned occurrences, 70 parent inequalities, and 16 hostile rejections using
the pinned independent predecessor-referee kernel.  The unavailable historical
identifier `ca730426cdd5847ae262ddc29c6f4ae98369eba3` is recorded only as
history and is not a required Git object.

## Ledger and disposition

The theorem ledger stays exactly `2/9` with delta zero.  The memoryless
13-type-to-global-cut route is retired until both global attachment and
projection closure are supplied.  No successor is selected; the next state is
`PIVOT_REQUIRED`.

Nonconsequences: no source-realized D9 counterexample, no universal cut
coverage theorem, no exhaustive obstruction `UNSAT` result, no
strict-open-parent separator, no weighted recursive-link classification, no
diagonal-nine result, and no 9DVL score change.

## Replay

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONHASHSEED=0 python ops/team/d9-universal-cut-referee/verify_closing_referee.py
```

For the two slower source-derived checks:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONHASHSEED=0 python ops/team/d9-universal-cut-referee/verify_closing_referee.py --full-source-replay
```

The independent verifier contains 20 hostile closing mutations covering gate
promotion, endpoint and ledger promotion, census authorization, erased scope,
projection closure, counterpair realization, boundary promotion, a forged D9
discriminator, a required historical object, and a forged live adapter.


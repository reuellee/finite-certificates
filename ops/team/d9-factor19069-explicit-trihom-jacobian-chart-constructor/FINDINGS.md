# Factor-19069 explicit trihomogeneous Jacobian-chart constructor findings

## Endpoint

The constructor reaches the preregistered timeout endpoint
`HASH_PIN_ALL_COMPLETED_CHART_BRANCHES_AND_FIRST_PENDING_BRANCH`.  It exactly
constructs the source and the complete 64-chart atlas, but the isolated exact
characteristic-zero Gröbner prerequisite on the first stable chart reaches its
20-second wall ceiling.  The first pending branch is
`DEC-JCH-00-a-d-g`, semantic SHA-256
`abfc0e6ecf60da4924c0991e8f78ea6df06a3270353eeedba411096ee3727725`.
No primary/equidimensional decomposition, component, contraction, or
parent-factor test is accepted.  The theorem ledger remains `2/9`.

## Exact source and atlas

The lane reconstructs the 108-term affine factor solely from the pinned
predecessor JSON.  Its unique termwise trihomogenization is stored in

```text
Q[a,b,c,u,d,e,f,v,g,h,i,w]
```

and every term has exact block degree `(2,2,2)` in
`(a,b,c,u)|(d,e,f,v)|(g,h,i,w)`.  Exact sparse substitution proves

```text
F(a,b,c,1,d,e,f,1,g,h,i,1) = f_19069.
```

This projective reformulation is not treated as a decomposition certificate,
and it does not alter the predecessor's proved fact that the affine polynomial
is neither homogeneous nor multiaffine.

The atlas enumerates pivot offsets lexicographically in `{0,1,2,3}^3`.  Thus
`JCH-00-a-d-g` is first and `JCH-63-u-v-w` is the affine source chart.  Every
chart records one pivot per four-coordinate block, the other nine coordinates,
the full sparse `F_chart`, all nine exact free-coordinate derivatives, and 63
directed overlap records.  The total directed overlap census is `64*63=4032`.
No symmetry quotient or ambient-orbit shortcut is used.

## Euler transfer and affine ideal equality

For each of 64 charts and each of three blocks, the lane verifies by exact
sparse equality

```text
(dF/dp)|chart = 2 F_chart - sum(x dF_chart/dx),
```

where `p=1` is the block pivot and the sum runs over the other three block
coordinates.  All `192` stored residual polynomials are exactly zero.  Hence
the three pivot derivatives lie in `<F_chart,nine free partials>`.  Conversely,
the block Euler identity puts `2F_chart`, and therefore `F_chart` over `Q`, in
the ideal generated by all twelve specialized homogeneous derivatives.  The
two chart Jacobian ideals are therefore equal.

On `JCH-63-u-v-w`, the free-coordinate order is exactly
`a,b,c,d,e,f,g,h,i`.  The stored chart polynomial equals the original affine
factor coefficient-for-coefficient, and its nine free derivatives equal the
original nine affine derivatives.  Therefore its ten-generator ideal is
exactly the original affine singular ideal.

## Boundary and component preservation

A standard product chart is never mislabeled as wholly at infinity.  If a
homogenizer is free, its zero equation is recorded as an available boundary
divisor; if that homogenizer is the pivot, its zero divisor is excluded from
that chart.  Nonempty local boundary intersections are ordered by bitmask.
There are `279` local records and the seven global nonempty subsets of
`{u=0,v=0,w=0}`.  Every record is retained, and zero boundary or embedded
strata are discarded.  The predecessor's boundary, fixed-skeleton, null, and
source tags are also preserved exactly.

The first decomposition attempt uses SymPy over `QQ` with grevlex order in an
isolated process.  It times out before a Gröbner prerequisite is completed.
The result therefore stops at the first stable chart and does not start later
chart decompositions.  A Gröbner basis, even if it had completed, would not
have been accepted as a primary-decomposition certificate.

There are no accepted projective or affine components.  Consequently there
are no contractions and no componentwise parent-factor tests.  The frontier
still retains all 70 ordered parent-factor records and makes the required
future ordering explicit: exact chart decomposition, overlap deduplication,
affine restriction and contraction to the original singular ideal, then all
70 parent-factor tests.

## Verification and nonconsequences

`build_projective_chart_frontier.py --check` reproduces the manifest, frontier,
and result byte-for-byte.  `verify_projective_chart_frontier.py` independently
reconstructs the 108-term lift, all 64 chart polynomials, 576 free derivatives,
192 Euler identities and zero residuals, 4,032 overlaps, 279 local boundary
records, the affine ideal equality, all 70 parent records, and endpoint/ledger
accounting.  It rejects 41/41 hostile mutations.

The frontier file SHA-256 is
`815edf97a68a049f8fb6749adf948cc406ec7a258de480cf3ada3e301ca6de67`;
its semantic SHA-256 is
`aec3c7e29744d6d9a7df004c611c7edbdfbfedfad2bbea6f206df09d5fb14f33`.

This result is not a complete characteristic-zero decomposition, an embedded
component census, a projective overlap-deduplicated component list, an affine
component contraction, a 70-parent-factor incidence classification, a strict
real-residence or connected-parent classification, a singular-emptiness
certificate, a theorem-level counterexample, or evidence for changing the
`2/9` ledger.

# Independent backward-detector opening audit

Base: `730fd6c7b6c2c3e8cbbf94ab4da3bc97ba53f095`.
Tree: `dbe7645743d09758eb3403272908ac3dcf88bf65`.
Owner: independent referee; only `ops/team/d3-detector-referee/` is writable.
No worker acceptance logic is imported. No external write, publication,
theorem-ledger modification, or further delegation is authorized here.

## Recovery and immutable inputs

The local isolated checkout has exactly the declared base and tree. All eight
SHA-256 source pins in the September 9 opening manifest match independently
read bytes. The actual latest `DIAG3_PAIR_DIFFERENTIAL_ENDS.md` and September 5
`CLOSING_STATE.json` were read, rather than inferred from historical V11.

The recovered full-history bundle has 137,374,370 bytes and SHA-256
`f3c1859c01b2848f70ed7c275150d3edf9890cb269ec141b04843f6989c5977a`, matching
its recovery manifest. Its clean-restore record has SHA-256
`bd462b3ed86501e4739230e19034216692f09a91ca6dc858b1a4b774971ba388`, also
matching that manifest. This verifies recovery identity, not a new replay of
every historical theorem. The inherited seven successful historical replay
records remain evidence at their recorded source revisions.

The accepted state is ledger 2/9, seven original obligations open, triple
source counts 77,940,147 settled / 79,102,449 total / 1,162,302 residual, and
pair residual and global coverage UNKNOWN. Source-orbit counts are not a
component or global pair denominator. The inherited consecutive counters are
11 and 14, from the accepted September 5 close. The opening proof distance is
`(2/9,1,{diag3_pair_hc1,diag3_triple_hc0},7,UNKNOWN,UNKNOWN,11,14)`.

## Original claim and strategy eligibility

Write `Aij=Bi∩Bj`, `T=B0∩B1∩B2`, and
`D=(r01,-r02,r12):⊕Hc1(Aij;Q)→Hc1(T;Q)`. The target is quantified over every
realizable uniform rank-four eight-element parent and every admissible
three-element antichain of proper extension signatures. All components,
original extension labels, support specializations, and true parent infinity
remain in scope. The auxiliary balanced-frontier description assumes
`Hc0(T)=0`; that assumption remains open. The original map itself does not
depend on proving it first.

The opening compares three distinct routes against all eight protocol
criteria and correctly records a PIVOT. Its low quantifier readiness and high
coverage burden are accurate. Its strict-decrease condition is one original
quantified obligation closed, or a strictly smaller globally attached finite
residual with a bounded theorem path. New formulas, finite arithmetic, and
local no-go results are explicitly insufficient.

**Opening verdict: eligible only for the bounded candidate-design gate.**
There is no independently supplied new global geometric input at opening.
Thus the opening is not permission to begin another atlas or signing census,
nor acceptance that a theorem-capable construction already exists. Before
global discovery could proceed, the candidate must supply more than an
equivalent injectivity condition and identify a finite way to prove its first
missing all-parent geometric implication. The first decisive null, eight-minute
gate, or ten-minute midpoint stops new discovery when that is absent.

## Independent detector requirements

Over Q, an injective linear map `D:V→W` has a left inverse after choosing a
complement to its image. Conversely a left inverse proves injectivity. Even
finite-dimensionality does not make this equivalence a geometric
construction. Three selective maps `pij:W→Hc1(Aij)` must satisfy
`pij rij=id` and `pij rkl=0` for the two other colors; the signs `(1,-1,1)`
then produce a left inverse of D. Individual restriction injectivity, or
pairwise disjoint images, does not suffice for the three-color direct sum.

A splitting of the underlying constant-sheaf restriction morphism is much
stronger. At any actual triple-bad point its degree-zero stalk is
`Q^3→Q`, `(1,-1,1)`, which has rank-two kernel. A sheaf or derived-sheaf left
inverse would induce an impossible stalk left inverse. This excludes that
literal local rule wherever T is nonempty. It does not exclude a splitting
only after taking global compact-support cohomology in degree one. The
stalks are not those global groups.

For a compatible compactification of a closed stratum Z, a possible
infinity-based description must start from the exact sequence

`H0(Zbar)→H0(IZ)→Hc1(Z)→H1(Zbar)→H1(IZ)`.

Boundary components alone carry every degree-one class only if the indicated
map to `H1(Zbar)` vanishes; `H1(Zbar)=0` is one sufficient additional
hypothesis. Every restriction, orientation, chart seam, and specialization
still has to be compared with the actual compactification. Internal witness
rank drops or residual walls cannot be declared parent infinity. Component
noncompactness does not prove these hypotheses.

A contraction on a replacement complex is usable only with actual compared
chain maps. For example, if `i:C→Chat`, `p:Chat→C` are chain maps,
`pi-id=ds+sd`, and `dH+Hd=id` on the required middle degree of Chat, then
`h=pHi-s` satisfies `dh+hd=id` on C. Without the maps, their homotopies, and
relative-boundary compatibility, a formal contraction of Chat supplies no
contraction of the original N,M complex. Pointwise convex witness fibers or
generic occurrence H1 vanishing do not furnish those comparison maps.

These requirements are an audit boundary, not new global mathematical input.

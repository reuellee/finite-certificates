# Independent closing review: auxiliary acceptance only

Base `730fd6c7b6c2c3e8cbbf94ab4da3bc97ba53f095`.
The precise frozen worker revisions and SHA-256 bytes reviewed are in
`REVIEWED_INPUTS.json`. No producer acceptance code or repository checker is
imported by `verify_independently.py`. NumPy only decodes the original archive;
all arithmetic uses exact integers and rational numbers.

**Verdict: ACCEPT_AUXILIARY_ONLY. Original pair target: NULL. Global
construction gate: NO_GO.** The canonical boundary reformulation is correct,
the narrow local exclusions are supported by actual admissible parent data,
and neither closes an original obligation. Discovery stopped at the first
decisive structural null. Certificate completion and independent review do
not reopen discovery or increase its budget.

## Mathematical findings

The constructive prover's two-step connecting map is valid on the original
spaces. For `Ci=Bi\(Bj∪Bk)`, it gives
`τi:Hc1(T)→Hc3(Ci)` with
`ker τi=im rij+im rik`, using only the inherited `Hc2(Bi)=0`. The ordering of
the closed cover changes a conventional overall sign, with the original
pair-map signs retained. Its explicit relative cochain lift formula is the
usual pair of connecting constructions: the first coboundary glues on the
union, and the second vanishes on that union, so lies in the open complement.
Different lift choices change only the represented coboundary class.

All three maps `κi=τi rjk` being injective is equivalent to the original D
being injective; no injectivity of an individual restriction is silently
assumed and no triple `Hc0` vanishing is used. The full independent proof is
in `CANDIDATE_REVIEW.md`. Nondegeneracy remains unproved. The old one-boundary
construction's reverse arrow indeed has the wrong variance for extension by
zero, with lift obstruction and ambiguity correctly given by localization.

The moment-curve shear fixture is exact: all 70 parent brackets remain
positive, six selected plane normals remain fixed, and an omitted normal
changes its ray. The worker's isolated fixture uses the opposite uniform
normal convention to `det(Y_a,Y_b,Y_c,x)`; the independent replay records
that factor of -1 explicitly. Since this fixture asserts no signature,
positive Gordan support, or feasibility change, that convention difference
does not change its limited plane-preservation conclusion.

The falsifier's actual row-2599 data independently yield all 70 nonzero
parent brackets, all 3,780 uniform extension GP tests, six strict positive
rank-four five-circuits, and three genuine one-dimensional coordinate faces.
The properness and incomparability anchors are separately bound to the
original archive. Their 56 strict inequalities per designated signature and
six ordered opposite positive dependencies are checked, so those properties
are not inferred from differing bit strings.

The strict cofactor signs persist on an open parent neighborhood. A local
projective slice matching the anchor's own frame values makes this a
nine-dimensional open ball. Nonzero frame brackets alone would not justify
an arbitrary all-positive global frame normalization; the worker was required
to correct that sentence. Locally, choose the first four columns as a basis
and express the fifth in that basis. Its four coefficients are nonzero.
Near the anchor their ratios to the anchor coefficients stay positive, so
the gauge fixing can be chosen continuously near the identity, with positive
column scales and positive determinant. This establishes the required local
slice without reorientation.

On the resulting ball all three pair and triple loci coincide, while the
exclusive pair and singleton targets are absent. The literal degree-zero
constant-sheaf restriction therefore has stalk matrix `(1,-1,1)` and a
rank-two kernel. A sheaf or derived-sheaf left inverse is impossible. But
`Hc1(R9;Q)=0`; this stalk kernel supplies no class in the original global
degree-one kernel. The boundary candidate may still detect classes through
attachment outside the ball.

The three actual coordinate edges exclude a contraction to a single witness
that commutes strictly with **every** coordinate-face inclusion: both
endpoint vertices would have to stay fixed. This is a stronger requirement
than the current candidate needs. Homotopy-coherent comparisons,
acyclic-carrier constructions, and global cohomological detectors survive.
The exclusion must not be promoted to a detector impossibility theorem.

## Verification and scope gates

`INDEPENDENT_REPLAY.json` records the exact replay. In addition to the actual
parent arithmetic, 343 finite quotient-space controls test the formal
detector equivalence, including 96 injective examples, zero-column cases,
and dependent image cases. A separate three-line example verifies that
pairwise image independence is insufficient. These controls do not replace
the universal exact-sequence proof. Supported-sign and positive-weight
corruptions are rejected. The all-normal-scaling extrapolation is rejected
by an exact rank-two span computation.

All opening source pins, reviewed producer bytes, inherited closing state,
and immutable base tree are checked. Recovery bundle size/hash and its
complete-history verification were independently checked at opening.
Historical theorem suites were not rerun for unchanged historical bytes.
Source accounting and scope are complete for this auxiliary handoff. No
actual global compact-support complex, factor-end matrix, or compared chain
contraction has been certified.

Replay after coordinator integration:

`python -B ops/team/d3-detector-referee/verify_independently.py`

For isolated worktrees, the command additionally accepts `--opening-root`,
`--prover-root`, and `--falsifier-root`. They only select the locations of
hash-pinned files; they do not import worker logic.

## Closing accounting and decision

All seven original obligations remain open, original obligations closed
equals zero, ledger remains 2/9, pair residual and global coverage remain
UNKNOWN, and triple source counts remain 77,940,147 / 79,102,449 with
1,162,302 residual. No source count is a component denominator.

Count this independently reviewed bounded candidate attempt once after the
accepted September 5 close: the opening vector is
`(2/9,1,{pair Hc1,triple Hc0},7,UNKNOWN,UNKNOWN,11,14)` and the recommended
closing vector is
`(2/9,1,{pair Hc1,triple Hc0},7,UNKNOWN,UNKNOWN,12,15)`. The earlier
conversational backward audit is not counted. Historical V11 remains frozen.

The minimum acceptable decrease is not met. The theorem trajectory is
**STALLED**, while the auxiliary knowledge is **INFORMATIONAL**. The same
global nondegeneracy/attachment blocker survives; protocol reset conditions
continue to forbid same-route continuation by local refinement. The closing
action is **STOP**, selected successor **NONE**. Only a genuinely new
globally quantified geometric input could justify a future strategy pivot.

No GitHub write, external communication, paid computation, ledger edit, or
durable checkpoint is performed by this worker. The coordinator owns
integration and final storage of the reviewed result.

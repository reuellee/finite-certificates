# D3 mixed-carrier topology theorem-feasibility memo

## Handoff

**`NULL` / `DECISIVE_NULL`.**  The pinned sources do not contain a complete
finite proof program for conditional Target A together with global Target B,
and that program is not reachable within one referee-directed revision under
the frozen resource ceiling.  This is not a universal obstruction to A or B:
the correct classification is therefore `NULL`, not `NEGATIVE`.

No new mathematical theorem is proved here.  In particular, this handoff
does not construct a carrier, certify the actual bad union, prove a rank
identity, prove either D3 branch, or change the `2/9` ledger.

## Frozen base and authority

- Lane branch:
  `research/lane-d3-mixed-carrier-topology-20260901`.
- Frozen lane-opening commit/tree:
  `dd86907bebbfaaac9caee4e1d93dc77bc9f3ad8b` /
  `a59d740c008ae04accf200a8373d16b4d9c70ae4`.
- Immutable canonical close commit/tree:
  `9116771ba80ed3d033516d0dd666b34348aad348` /
  `a64438426dc792af67d5ccc0dd2f4d1231dbaa14`.
- Canonical state: ledger `2/9`, status `PIVOT_REQUIRED`, promotion `NONE`.
- Authority permits one theorem-feasibility audit only.  Construction,
  atlas/census growth, theorem credit, ledger edits, network use, and external
  solvers are all unauthorized.

The authority surfaces were read at the hashes recorded in `RESULT.json`.
That file also carries all thirteen exact opening source pins.  In particular,
the stricter topology work order caps this memo at six named obligations,
despite the charter's looser ceiling of nine.

## Quantified scope

Any acceptable program must be uniform over every realizable uniform
rank-four parent on eight labels, every normalized realization component and
required chart, every source-derived proper antichain of pairwise-incomparable
proper extension signatures, and every admissible witness-support stratum.
For `1 <= r <= 3`, the ten joined shapes are classified modulo permutation of
the active blocks, but **every labeled instance** of every shape is quantified.
The dimension formula is

\[
 k=(r-1)+\sum_i k_i.
\]

One common semialgebraic subdivision must simultaneously handle all blocks,
weights, supports, zero-weight faces, chart seams, residual walls, parent
boundary, and face specializations.  Compatibility is required for every
composable face chain, not just codimension-one squares.  Parent-matrix rank
drop is distinct from witness-kernel, occurrence, concurrence, residual, and
support rank drop.  Only a certified nonuniform parent-rank face or a genuine
end of the parent space may enter the true relative-infinity subcomplex.

The exact shape accounting is:

| layer | source status | shapes |
|---|---|---|
| lower skeleton, universally credited | proved in the pinned single-bad source only | `(0)`, `(1)`, `(2)` |
| lower skeleton, missing | open | `(0,0)`, `(1,0)`, `(0,0,0)` |
| dimension three, missing | open | `(3)`, `(2,0)`, `(1,1)`, `(1,0,0)` |

Thus the source taxonomy is `3/10`; it is not an end-to-end denominator.

## The noncircular `L_source` interface

Target A is stated conditionally over **any** candidate `L_source` satisfying
the following declared interface:

1. finite source-derived labeled `C_0,C_1,C_2` generators on the full parent,
   proper-antichain, active-block, chart, and support scope;
2. integral orientations and boundary maps with `d_1 d_2=0`, plus the finite
   list of every relevant primitive integral kernel class;
3. stable provenance and face tags distinguishing chart, support,
   zero-weight, interior witness-rank, parent-rank, and certified true-infinity
   strata; and
4. one simultaneous subdivision with specialization maps forming a category
   on all composable face flags, including active-block permutations.

A may assume those interface axioms while proving a conditional mixed-chain
theorem.  A may not assert that such an interface exists, is exhaustive, or
compares to the actual bad union.  B must construct and verify an actual
source-derived instance before invoking A.  This separates the two logical
directions: A's conditional statement does not assume B, while B's eventual
use of A is permitted only after the interface has been verified.

## Six-obligation acyclic program

The dependency graph is

```text
pinned sources --> O1 --> O2 --\
                                 \
declared L_source contract --> O3 --> O4 --> O5 --> O6
                         O1,O2 -----------/
```

The arrow from the contract to O3 is conditional: it does not assert that B
has realized the contract.  O5 is the first point at which B may combine its
verified source instance with A's conditional output.

| id | named obligation | owner | prerequisites | finite acceptance endpoint | audit status |
|---|---|---|---|---|---|
| `O1` | Source domain and simultaneous subdivision | B | pinned sources | A finite, source-provenance-complete index covering the full quantified scope, with one subdivision and separately tagged parent/witness rank strata | **open; first B edge** |
| `O2` | `L_source` lower-skeleton realization | B | `O1` | An independently replayable labeled integral `C_0,C_1,C_2` complex containing every labeled lower shape, strict incidences, all face-flag specializations, true infinity, and `d_1d_2=0` | **open**; all three mixed lower types are missing |
| `O3` | Universal genuinely mixed chain theorem | A, conditional | declared `L_source` interface | For every relevant primitive integral kernel class, a finite genuinely mixed chain family of type `(1,0,0)` and its active-block permutations, with integral boundary and a finite falsifier | **open; first A edge** |
| `O4` | Source-natural specialization and iterated-face coherence | A, conditional | `O3` and the declared interface | The O3 assignments commute with support, zero-weight, chart/monodromy, parent-boundary, rank-drop, permutation, and every iterated-face specialization | **open** |
| `O5` | Complete joined bad-union comparison | B | `O1`--`O4` | A coverage-certified regular relative complex containing every labeled instance of all ten shapes and a proper filtration-preserving comparison to the compactified actual bad union relative to genuine parent infinity, inducing the needed compact-support comparison through total degree two | **open; separately decisive** |
| `O6` | Rational pair-kernel exactness | B | `O5` | On the actual compared pair piece, `C_pair^0 --N--> C_pair^1 --M--> C_pair^2` has `MN=0` and `im_Q(N)=ker_Q(M)`, equivalently `rank_Q(N)+rank_Q(M)=dim_Q(C_pair^1)` | **open instance**; only the conditional algebraic implication is proved |

The listed order is acyclic.  O3 and O4 are conditional theorem obligations;
they do not depend on O1 or O2 being true.  Their *invocation inside B* occurs
at O5, after O1 and O2 have realized the interface.  This is the required
A/B noncircularity.

## First missing edges

There are two independent root gaps.

1. **B root:** `pinned sources -> O1`.  The decision ledger records zero
   current global pair adjacencies, strict closure pairs, strict closure
   triples, and parent-infinity cells.  Later row-2599 artifacts are expressly
   local and state `global_parent_cell_coverage: NOT_CLAIMED`; the two-edge
   labeled tree covers only two of forty retained source paths.  No pinned
   source supplies a finite exhaustive source index or one simultaneous
   subdivision for the full parent/antichain/support domain.
2. **A root:** `declared L_source contract -> O3`.  The row-2599 chart-zero
   complex exhibits one primitive class
   `(-1,1,1,1,1,1,1)` and disproves only singleton-root/fixed-one-block
   fillers.  It neither constructs nor obstructs genuinely mixed chains for
   every primitive class on every admissible interface.

Even if both roots were supplied, O5 remains a separate missing edge.  The
pinned sources contain no proper filtration-preserving comparison between a
complete joined complex and the actual compactified bad union.  Giving that
edge a name does not give it a finite proof program or verified hypotheses.

## Pair comparison and rational endpoint

The smallest acceptable B endpoint is source-first:

1. realize `L_source` from the full source index;
2. adjoin every labeled dimension-three shape, using A only for the genuinely
   mixed `(1,0,0)` part and proving `(3)`, `(2,0)`, and `(1,1)` separately;
3. verify strict closure, incidence, `d^2=0`, true-infinity provenance, and
   the comparison to the actual bad union; and
4. extract the compared pair cochain complex and prove rational middle
   exactness.

The architecture audit proves one useful **conditional** algebraic endpoint:
if the complete geometric model has signed integral lifts with `MN=0` and
its reduction satisfies

\[
 \operatorname{rank}_{\mathbb F_2}N+
 \operatorname{rank}_{\mathbb F_2}M=
 \dim_{\mathbb F_2}C_{\rm pair}^1,
\]

then rational middle exactness follows.  This implication does not provide
the matrices, their geometric lift, coverage, or comparison.  Local rank
computations therefore cannot be promoted to O6.

## What the pinned sources actually prove

- The single-bad source proves `H_c^q(B_rho;R)=0` for `0 <= q <= 2` and
  supplies the universally credited `(0)`, `(1)`, and `(2)` terms.  It
  explicitly does not prove pair or triple terms.
- The row-2599 flow triangle has `rank(d_1)=3`, `rank(d_2)=6`,
  `rank(C_2)=7`, `H_0=H_1=0`, and `H_2=Z`, with the displayed primitive
  class.  This is a diagnostic local two-skeleton, not a global denominator.
- The architecture audit proves the conditional `F_2`-to-`Q` rank argument
  above, provided a genuine integral cellular/Cech lift exists.
- The global-exit source proves a fail-closed graph criterion after complete,
  component-faithful coverage and true-parent-infinity tags are supplied.  It
  does not supply them; it also proves that a graph alone cannot settle the
  pair branch.
- Narrow fixed-order, singleton-root, and small-family no-gos do not quantify
  over every admissible mixed carrier.  They do not justify `NEGATIVE`.

This lane adds no theorem beyond those pinned results.  Its contribution is
the fail-closed dependency audit and the precise location of the first open
edges.

## A/B/C status and midpoint action

- **A:** `NULL_CONDITIONAL_PROGRAM_INCOMPLETE`.  The interface statement is
  noncircular, but O3 and O4 have no finite universal proof program.
- **B:** `NULL_GLOBAL_PROGRAM_INCOMPLETE`.  O1, O2, O5, and the actual O6
  instance are missing.  B neither covers nor compares the actual bad union.
- **C:** `NULL_UNCHANGED_INDEPENDENT_OPEN`.  C is outside this lane's A/B
  design remit and remains independently open; no boundary term from A or B
  is component escape.

A complete A+B program is not reachable in one referee-directed revision.
O1 would require a new global source denominator and simultaneous
subdivision—effectively the atlas/census work forbidden in this feasibility
lane—and O5 needs a new global comparison theorem after that object exists.
O3/O4 are additionally unresolved.  These are method-level discoveries, not
bounded corrections to a nearly complete proof package.

**Recommended midpoint action:**
`STOP_DISCOVERY_FREEZE_AND_SEND_TO_REFEREE`.  Do not spend the optional
revision, start construction, or infer theorem progress.  The trajectory is
informational dependency sharpening with proof-distance delta zero.

# D3 mixed `(1,0,0)` universal-carrier falsifier findings

## Handoff

**`NO_UNIVERSAL_OBSTRUCTION_FOUND_WITHIN_FROZEN_SCOPE` — `NULL / STALLED / STOP`.**

No exact admissible rank-four eight-label source instance was proved to
obstruct every genuinely mixed coherent `(1,0,0)` filler.  The selected-route
residual therefore remains `O3/O4 = 2/2` open and the canonical ledger remains
`2/9`.

The exact reusable result is sharper than another failed ansatz: the declared
`L_source` interface is too weak to decide the geometric existence question.
It admits a canonical algebraic cone that defeats every obstruction based only
on integral boundaries, monodromy, permutations, or arbitrary face flags, but
it contains no predicate from which the cone can be certified as a genuinely
mixed semialgebraic carrier.  Conversely, interpreting the admissible mixed
carrier set as empty is compatible with the same declared lower data, but has
not been shown to describe any actual 9DVL source instance.  That is exact
logical non-entailment, not the cycle's required geometric `NEGATIVE`.

## Frozen scope

The lane opened cleanly at commit
`1c6519d89335dde215e93887de074ea4e6d6464a`, tree
`ff8a33e13952e86b27f184e3d8c40e768fbeb110`, on branch
`research/lane-d3-mixed-100-carrier-falsifier-20260903`.  The immutable
canonical base is `fb667bfe33ef9e945a82e9a23b615e67f5f39c0f`, tree
`117850b25cd94f865cb85e681c465b8260dd9c6a`.  Every source pin in
`SOURCE_MANIFEST.json` replays byte-for-byte.

This lane inspected no constructor worktree or output.  It used no network,
external solver, atlas, census, sampling campaign, residual growth, canonical
edit, ledger edit, GitHub write, or merge.

## What the interface actually says

The four declared axioms give:

1. finite labeled `C0,C1,C2` generators;
2. integral `d1,d2`, `d1 d2=0`, and a finite declared list of relevant
   primitive integral kernel classes;
3. provenance and tags distinguishing chart, support, zero weight, internal
   witness-rank, parent-rank, and certified true-infinity strata; and
4. one finite specialization category, including all composable face flags
   and active-block permutations.

They do **not** give a set or lattice of admissible mixed `C3` generators, a
semialgebraic parameter space for those generators, a source-realization map,
a properness condition, an oriented geometric-boundary map, or a predicate
certifying which ends map to actual `I = Xbar - X`.  Those missing data are
exactly what distinguish a formal algebraic filler from the required genuine
mixed carrier.

## Exact kernel-cone control

Let `F` be any finite diagram satisfying the declared integral lower-chain
and specialization axioms.  For every object `x`, set

```text
C3_cone(x) = ker(d2_x),        d3_x : ker(d2_x) -> C2(x)
```

with `d3_x` the inclusion.  For a specialization `f:x->y`, set
`C3_cone(f)` to the restriction of `C2(f)` to kernels.

This is well-defined because a chain map sends cycles to cycles.  It is finite
free over `Z` because every subgroup of a finite-rank free abelian group is
finite-rank free.  Identities, compositions, every arbitrary face flag,
monodromy loop, and active-block permutation are inherited by restriction.
Every primitive integral kernel class is filled with coefficient one.  Hence:

> There is no obstruction to O3/O4 that can be derived solely from the
> declared integral lower complex and specialization category.

This lemma does **not** prove O3/O4.  A kernel element renamed as a degree-three
generator has no automatic `(1,0,0)` semialgebraic realization, proper map, or
true-parent-infinity boundary.  The geometric words in the target carry the
entire remaining burden, and the interface supplies no data with which to
verify them.

## Finite independence fixture

`interface_independence_fixture.json` uses the exact row-2599 direct-edge flow
triangle:

```text
rank(d1)=3, rank(d2)=6, rank(C2)=7,
alpha=(-1,1,1,1,1,1,1), ker(d2)=Z alpha.
```

It places this lower complex on the Boolean face poset generated by support
loss, zero weight, chart transition, parent boundary, parent rank, witness
rank, and residual wall.  The poset has `2^7=128` objects and `3^7=2187`
morphisms.  All lower maps are identities; an `S3` active-block action and a
`C2` monodromy action are also represented trivially.  Thinness gives strict
coherence for every composable flag, not just codimension-one squares.

The same lower interface has two exact expansions:

- the formal cone has one `C3` generator with boundary `alpha` and is
  integrally and categorically coherent;
- the empty geometric-carrier interpretation has no admissible mixed
  generator, leaving `H2=Z`.

The second expansion is deliberately marked
`actual_9dvl_admissibility_established=false`.  It proves the conclusion is
not a logical consequence of the declared interface.  It does not prove that
the actual row-2599 geometry, or any other quantified source geometry, has no
mixed carrier.

## Obstruction attacks

The bounded attacks close as follows.

| Attack | Exact outcome | Full-negative status |
| --- | --- | --- |
| row-2599 singleton-root cocycle | excludes every filler supported on one fixed bad block at a time | insufficient; genuinely mixed witness exchange remains allowed |
| compact triple component | no example; row 2599 instead has a certified proper triple-bad ray to parent wall `[2467]` | no obstruction |
| integral parity/torsion | impossible from the lower interface alone because the kernel-cone inclusion fills with coefficient one | would require the actual geometric mixed-boundary lattice |
| monodromy or permutation stabilizer | kernel restriction is exactly equivariant | would require a separately specified action on actual mixed cells |
| arbitrary face flags | restriction to kernels preserves every composite strictly | formal coherence does not certify geometric specialization |
| empty mixed-carrier universe | exact non-entailment model | actual source-derived admissibility not proved |

The first missing negative edge is therefore not another formula.  It is an
exact source-derived definition of the exhaustive admissible mixed-cell
universe and its geometric boundary map, followed by one actual instance in
which the primitive class is outside that entire boundary image while all
support, zero-weight, chart, monodromy, rank, permutation, face-flag, and true-
infinity requirements remain enforced.

## Why this is `NULL`, not `NEGATIVE`

The cycle's negative handoff requires one admissible finite `L_source` tied to
the quantified rank-four source geometry and an exact proof that **no** genuine
mixed coherent filler exists.  The finite fixture settles only the formal
logic of the declared interface.  Promoting its empty expansion would silently
equate “not provided by the interface” with “does not exist in the actual
semialgebraic geometry.”  That is precisely the prohibited ansatz-to-universal
jump.

Accordingly, O3 and O4 remain open.  The promised minimum decrease is not met;
the correct midpoint and final action is `FREEZE_NULL_STALLED_STOP`.

## Replay

The following commands are the frozen replay set:

```text
python -B ops/research-team/cycles/2026-09-03-d3-mixed-block-100-universal-carrier-gate1/verify_opening_state.py
python -B ai/omreal/verify_canonical_research_state_v10.py
python -B ai/omreal/verify_diag3_joined_flow_triangle.py
python -B ai/omreal/verify_diag3_single_bad_two_skeleton.py
python -B ai/omreal/verify_diag3_row2599_common_proper_escape.py
python -B ops/team/d3-mixed-100-carrier-falsifier/verify_falsifier.py
```

The lane verifier uses only the Python standard library.  It checks all source
digests, reconstructs the exact ranks and primitive kernel, proves the formal
cone boundary, exhausts all `4^7=16384` composable triples in the Boolean face
category, verifies the finite symmetry orders, enforces the null scope, and
rejects 40 hostile in-memory mutations.

## Nonconsequences

There is no admissible counterexample to O3 or O4, no proof of O3 or O4, no
actual mixed cell constructed or excluded, no global coverage or true-infinity
claim, no D3 pair/triple result, and no theorem-ledger change.

# Independent frozen-candidate referee review

**ACCEPT_SCOPED_AUXILIARY_RESULTS_ONLY. No actionable defect remains in the
reviewed claims.** These are finite exact results and their stated deductive
consequences. They close zero original global obligations and yield zero
theorem-ledger change. Numerical discovery remains heuristic.

Opening: `c27a36d6e3f47a2b4c7bb017774cbe5df30abdc2`, canonical base
`21a97db6aa66912fd37556d81711c0588c454a7d`, base tree
`4b7b850cd3e0e1ebb2aa0bf4e7c5c3f25a360bf7`. The independent opening audit is
in `OPENING_REVIEW.md` and `OPENING_SOURCE_AUDIT.json`.

| Track | Frozen revision | Accepted scope |
| --- | --- | --- |
| Event | `dff5dc46744ca6680770b20392139f61bec69ff6` | Exact interval transition for all 56 original block-0 inequalities; open parent-matrix local germ and singleton full normalized dual fiber on its boundary |
| Topology | `b468869778beb404cd2c4f4b5b426115a4c9356b` | Complete horizontal triple-bad interval and transverse proper escape of its particular ambient component in normalized parent space |
| Coherent | `845791277cb0c41eb7b8d6dd7aea892cf135d79c` | Three-point restricted-fiber computation and actual fixed-parent triangle; inherited mass-map obstruction in the pair-face layer |
| Alternative diagonals | `efbb652c543dbf05a6ebb947fde5cb4218b07e26` | Failure of the fixed-frame endpoint-coordinate and endpoint-column hybrid graphs for the two specified endpoints |

## Independent reconstruction and hostile controls

`verify_candidates.py` reads immutable candidate blobs using `git show`, binds
each source manifest both to the opening blobs and current inherited files,
and records every candidate file's SHA256. It imports no discovery or producer
acceptance module. NumPy is used only to decode the original NPZ. All acceptance
arithmetic is rational/integer arithmetic in this referee's assigned surface.
No candidate file was changed or candidate verifier executed in this worktree.

The event was reconstructed from the original chart-zero integer matrix.
Permutation determinants recover the affine section `adj(D)1`, the event
factor, all 56 margins, all 70 parent brackets, and all 12 selected-dual
polynomial equalities. Centered Taylor absolute-remainder bounds certify the
52 strict nonactive margins and the strict dual weights over the whole stated
interval. This is a different positivity method from producer Bernstein
coefficients. The active matrix has exact rank three; the nonactive margins
are strict at the event. Positivity forces every full-block dual onto the four
active rows; their one-dimensional positive kernel gives a singleton after
normalization. The event/non-event endpoint distinctions are retained.

The topology computation reconstructs every chord normal and cofactor and
uses rational Sturm sequences, removing explicit endpoint factors, to exclude
interior roots. It certifies the exact horizontal parent interval, isolates
the unique quadratic alpha using its positive derivative, verifies beta,
and checks both full 56-row primal systems. The other dual coefficients give
badness on the complementary stated intervals. Thus the complete slice is
exactly `[alpha,beta]`, including both support-loss endpoints.

On the transverse chord all 15 dual weights stay strictly positive and all
12 kernel identities hold. The exact affine parent inequalities remain strict
until `v=2037283091/10548542`; exactly `[3578]` vanishes there. The ratio
`[3578][1234]/([1238][3457])` has balanced column multiplicities and two brackets
in both numerator and denominator, so it is invariant under GL(4) and column
rescalings. Its denominator has the specified nonzero limit. A continuous
nonzero ratio on a compact subset has a positive absolute minimum. Its limit
zero therefore proves escape from every compact subset of normalized parent
space. Concatenating the horizontal path to zero connects every point of the
certified interval to that escaping ray. This proves noncompactness of that
one ambient component, without counting or classifying the other components.

Original label admission was independently reconstructed: 3,780 extension
Grassmann–Pluecker sign relations, three bank-bound strict properness anchors,
and six ordered noninclusion duals. The inherited anchor duals use primitive
normal rows; their weights were explicitly divided by the positive row gcds
when replayed against raw determinant normals. The new chord certificates
already use raw normals. This convention difference is accounted for.

For the coherent track, exact ranks and normalized endpoint signs recover the
six-coordinate edge/singleton/empty sequence. The three actual event witnesses
satisfy their labeled equations. The triangle boundary is primitive integral,
its composition with the edge boundary is zero, and the edge kernel has rank
one. The mass-map section carries the circle generator nontrivially into the
whole pair-face fiber. Filling it in the full joined fiber uses three positive
block masses. This is inherited convex-fiber geometry, instantiated at the
event, rather than a new global attachment mechanism.

For D9, independent Cramer's-rule normalization matches both recorded rational
matrices and verifies positive transformation determinants, positive column
rescalings, and all original endpoint parent signs. Rational Gaussian
determinants recover exactly two valid vertices among 512 coordinate hybrids
and two among eight column hybrids. Every reported first wrong bracket has
the claimed opposite or zero sign. The two valid vertices are nonadjacent and
isolated in the stated graphs, so backtracking cannot repair that finite
mechanism.

Hostile checks reject an event labeled strict primal, negated primal,
right-hand selected dual, shifted root, flipped active signature bit, corrupted
dual identity, missing/empty row denominator, and negative column rescaling.
The independent positivity checker rejects both an interior double zero and
a polynomial positive at the endpoints but negative inside. Shifted beta,
interior-origin infinity, and wrong triangle orientation are also rejected.
These controls exercise sign, equality, strictness, interval coverage,
normalization, and boundary distinctions.

## Proof scope and nonconsequences

The event's open neighborhood and implicit-function argument are valid in
its **stated parent-matrix coordinates**: strict coefficient and margin
conditions remain open, the active determinant has nonzero partial derivative,
and the Gordan/adjugate identities give the claimed half-space germ. This
review does not assert descent of that collar to the normalized nine-dimensional
gauge, a global frontier block, or any orientation/compact-support attachment.
The normalized dual fiber here means sum-of-weights normalization and must
not be confused with normalized parent coordinates.

The topology ray proves normalized-parent escape using its invariant ratio;
the compact coordinate slice does not give a compact ambient component. One
escaping component is not an exhaustive component census, an orbit removal,
or a global triple `H_c^0` theorem. The sampled fan's component/hole structure
is not inferred from its graph or LP classifications.

The coherent obstruction concerns this loop in the fixed-parent pair-mass
layer. It does not exclude mixed carriers, parent motion, coherent filtered
complexes, or global pair-cohomology vanishing. Its original target remains
NULL and its claimed mechanism is correctly classified as inherited.

The D9 obstruction concerns endpoint-valued hybrid vertices in one fixed
frame. It excludes no path with arbitrary intermediate coordinates, adaptive
frames, or general semialgebraic motion. The inherited exact positive path
between the same endpoints remains a required noncontradiction check. That
historical long path was source-pinned, not re-executed in this bounded review.
The diagonal strategy rankings are judgments, not measured proof distances.

## State, replay, and disposition

The ledger remains **2/9**. All seven global obligations remain open. Triple
source accounting remains `77,940,147 / 79,102,449`, with `1,162,302` unresolved
source orbits. These numbers are not component counts. Pair/global residual
and component coverage remain UNKNOWN. The opening mathematical distance
coordinates do not decrease; the coordinator owns any closing streak update.
Useful discovery information is accepted, with no theorem-convergent
same-route continuation implied. The four bounded tracks should STOP at their
completed tests. A future parameterized escape corridor with original labels
and boundary compatibility would require a separately bounded work order;
the present evidence does not establish it or authorize an unbounded program.

Portable replay from a checkout containing the frozen raw commits:

```console
python -B ops/team/d3-explore-referee/audit_opening.py
python -B ops/team/d3-explore-referee/verify_candidates.py
```

All paths resolve from the referee script to the repository root; no absolute
worker path is an input. The full independent candidate replay used about
1.2 CPU seconds. The review completed within its 15 active-minute ceiling,
with no new search, dependency, external write, paid service, human contact,
ledger edit, or unassigned-surface modification. Integration, protected checks
at the final head, and durable clean-restore replay remain coordinator gates.

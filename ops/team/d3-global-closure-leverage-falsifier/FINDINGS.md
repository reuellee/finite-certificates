# D3 global-closure leverage: independent falsifier findings

## Verdict

**Reject every construction-selection claim at this gate.** The falsifier
classification is `STALLED_INFORMATIONAL`; the mandatory reset permits only
`STOP` or a fresh theorem-level `PIVOT`. It does not permit same-route
`CONTINUE`.

The reason is precise rather than heuristic: the pinned artifacts prove a
large local fiber inventory, but the global denominator is still `UNKNOWN`,
all seven load-bearing obligations remain open, and no compared route supplies
the three mandatory successor properties simultaneously: theorem-level
quantifier attachment, a certified finite/exhaustive progress measure, and a
preregistered strict decrease that reaches diagonal 3 through a bounded chain.
This lane selects no successor and starts no construction.

## Independently reconstructed source facts

The exact base is
`98ed8d36f08f13305d226d3d3770e71542a0a408`, tree
`52a03a10b62e54a9ff5aa4e59dbbc2af9a69eeab`. SHA-256 pins in
`RESULT.json` cover the protocol, canonical D3 state and ledger, open global
object, compactification and parent gates, all cumulative local-lift heads,
the cosheaf pilot, and the graph countermodel fixture.

The local inventory arithmetic does pass:

| fiber tranche | base cells | locally lifted cells |
| --- | ---: | ---: |
| open `t`, open `u` | 133,828 | 4,496,636 |
| open `t`, algebraic `u` | 132,134 | 4,047,846 |
| algebraic `t` | 261,571 | 8,390,619 |
| **total** | **527,533** | **16,935,101** |

The last row also decomposes independently as
`131,632 + 129,939 = 261,571` base cells and
`4,419,172 + 3,664,301 + 307,146 = 8,390,619` local lifts. The final residual
of **local** `v` fibers is zero. This is a genuine and useful local result.

The compactification and gates also replay exactly: 64 charts, 3,375 support
faces, 3,364 excluded and 11 nonexcluded faces, 196,064 remaining candidate
factor/face pairs, and 70,218 remaining mixed restrictions. But the source
artifacts expressly deny a global master-cell coverage claim. None of these
counts is a denominator for a globally glued regular complex.

## Direct-route attacks

The global open object records zero certified adjacency edges, zero strict
closure pairs, zero strict closure triples, and zero parent-infinity cells.
Its first missing block is the coverage-certified global cell universe. Thus
the direct route still has all of the following end-to-end obligations:

1. global gluing with stable cell identities;
2. complete 97,224-extension-signature labels;
3. complete strict closure;
4. genuine relative parent infinity;
5. integral incidence and middle-rank replay;
6. `diag3_pair_hc1`; and
7. the independent `diag3_triple_hc0` obligation.

The triple obligation remains
`79,102,449 - 77,940,147 = 1,162,302`. The pair obligation remains open. A
complete local fiber census cannot be promoted across these quantifiers.

## Structural-compression attacks

The component-cosheaf pilot audits seven full-atlas inputs. Every one lacks
all five required compiler fields: `cells`, `strict_closure_pairs`,
`strict_three_cell_chains`, `parent_infinity_subcomplex`, and
`signature_profile_source`. Its exact result is
`FAIL_CLOSED_BEFORE_COMPONENT_SPECIALIZATION`.

Therefore the present pilot cannot replace the global census. The negative
scope is deliberately narrow: a component-cosheaf compiler remains credible
*after* a theorem-level source supplies certified global incidence,
specialization, true-infinity, and label data. This falsifier does not retire
that post-closure use.

## Graph-only countermodel and its exact scope

The fixture has the same accepted component graph and the same one-skeleton in
both relative CW complexes. For the unfilled complex,
`dim H_1(F_2) = 1`; adding one two-cell with boundary the loop gives
`dim H_1(F_2) = 0`. Both have the same first boundary and satisfy
`d_1 d_2 = 0`.

This exactly retires **graph-only inference of pair relative `H_1` or middle
rank**. It is not a D3 counterexample. It does not retire the sufficient
component-faithful exit theorem for the triple `H_c^0` branch, a
coverage-certified global master closure, or post-closure cosheaf
compression.

## Counterexample route and D9 baseline

The graph-only shortcut has a terminal exact retirement, but no pinned finite
exhaustive candidate family can retire the remaining direct or structural
routes. It therefore supplies information, not a construction successor.

Deferred D9 `B-UV-01` remains only the required low-leverage baseline. Its
bounded local status does not overcome its downstream types and global
transport obligations, so it is not the default route.

## Convergence result

The falsifier vector is unchanged from opening:

`(2/9, 1, {diag3_pair_hc1, diag3_triple_hc0}, 7, UNKNOWN, UNKNOWN, 3, 6)`.

Neither acceptable decrease occurred: the obligation count did not fall from
7, and `UNKNOWN` did not become an exact globally attached residual with
coverage `k/N` and a preregistered strict decrease. The automatic reset is
therefore `SAME_ROUTE_CONTINUE_FORBIDDEN__STOP_OR_PIVOT_REQUIRED`.

The standard-library verifier recomputes all source facts and the two relative
`H_1(F_2)` ranks, then re-seals and rejects 34 hostile mutations covering
pins, arithmetic, global promotion, infinity semantics, invariant promotion,
compression scope, graph-countermodel scope, proof-distance claims, and
successor eligibility.

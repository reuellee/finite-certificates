# Producer-independent factor-19069 certificate

The certificate accepts only the exact fail-closed null.  Its verifier does
not import the constructor.  It pins the frozen constructor and falsifier
bytes, reconstructs the target polynomial and all seventy signed parent
polynomials from upstream sources, repeats all 2,800 parent-safe skeleton
checks and forty exact root counts, and independently rederives the active-set
and compactification-support frontiers.

Seventeen re-sealed hostile semantic mutations are rejected.  These include
removing a parent tag, globalizing the local collar, fabricating a tie filter
or critical sample, omitting a boundary record, inventing attachment counts,
selecting another sampled separator, and promoting the ledger.

Accepted endpoint:
`HASH_PINNED_PARENT_RESIDENT_CRITICAL_COMPONENT_FRONTIER_WITH_FIRST_UNCLASSIFIED_STRATUM`.
The complete component sample and attachment classification were not built;
diagonal nine remains open and the theorem ledger remains `2/9`.

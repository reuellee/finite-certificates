# Bounded actual-parent slice test

Opening revision c27a36d6e3f47a2b4c7bb017774cbe5df30abdc2; mathematical base
21a97db6aa66912fd37556d81711c0588c454a7d. Initial test: retain chart-zero
row-2599 parent and its three admitted proper incomparable extension signatures;
move label 8 in the first two vector coordinates by (u,v). All other entries
are fixed. Derive all 70 affine parent inequalities and, if bounded, the
complete closure polygon; sample its interior on a declared rational fan.
Classify actual goodness/badness using all 56 signed normals, keeping original
labels. Search sampled triple-bad connectivity and holes; record no inference
from samples to topology. If a discriminating boundary/chord appears, allow
one exact rational one-parameter follow-through, covering its full maximal
parent interval where feasible. This is separate from label-2 support events,
fixed-five-coordinate factor-source ovals, and full-height critical searches.

Budget: 20 active discovery minutes, 10 CPU minutes; one initial test and at
most one exact follow-through; all work stays on this surface. No global
coverage or cohomology inference is licensed by a slice.

# Falsifier findings

The smallest quotient attack already succeeds.  The transposition of the last
two parent labels maps active primitive factor 26 to factor 285, which is not
in the fixed `S12,37` active set.  Therefore the ambient `S_8` pair quotient
cannot be imported into this fixed domain.

The selected projection also has an explicit source-derived fatal witness.
For active factor 57, the eight polar equations have exact rank four and a
five-dimensional affine solution space.  The wall polynomial restricts to
zero identically on that space, producing a positive-dimensional polar locus.
This rejects the projection specialization without solving a critical system.

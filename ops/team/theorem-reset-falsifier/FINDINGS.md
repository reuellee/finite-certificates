# Theorem-reset falsifier findings

Verdict: **overall `NULL`; exact narrow `NEGATIVE` handoffs; no successor**.
The honest ledger remains `2/9`, the seven D3 load-bearing obligations remain
open, and the opening and closing proof-distance vectors are identical.  The
mandatory midpoint found the promised theorem-distance decrease unreachable,
so discovery stopped without increasing the fixed resource ceiling.

## Sharp joined-Gordan result

The exact rank-four row-2599 chart-zero canary replays successfully.  Its
three GP-valid bad signatures have elementary escape-set sizes `56/56/60`,
empty triple intersection, three pair roots, and valid ordered singleton
sectors.  The integral relative complex has

```text
rank(partial_1)=3, rank(partial_2)=6, dim(C2)=7,
H0=H1=0, H2=Z,
primitive class = (-1,1,1,1,1,1,1).
```

The Cech cocycle evaluates `1` on the root-choice loop.  Therefore no chain
built from arbitrarily long ordered-root cells that carry one fixed block at
a time can fill this class.  This exactly retires the singleton-block/root-only
filler architecture.

It does **not** retire a genuinely mixed-block carrier.  The same canary has
an exact common proper triple-bad ray to the genuine parent wall `[2467]`, and
the current local open object has four of six comparison incidences.  It has
zero certified mixed `d3` cells, but absence from the artifact is not a proof
of nonexistence.  The first local missing datum is a face-natural H1
compatibility homotopy from the nonradial `p01` disk to `p12`, followed by H0
and a primitive mixed-J assembly with no untagged frontier.  The radial H1
replay proves only that its second `p01` stage is not relative; that radial
shortcut is retired, not the nonradial mixed-block theorem.

## Ten clauses are not a global denominator

The joined dimension formula enumerates exactly ten formal low-skeleton face
types:

```text
(0);
(1), (0,0);
(2), (1,0), (0,0,0);
(3), (2,0), (1,1), (1,0,0).
```

The universal single-block replay proves only `(0)`, `(1)`, and `(2)`, so the
honest clause baseline is `3/10`.  The number ten classifies formal face
types; it does not count parents, signature triples, support choices,
specializations, residual-wall cells, or true-parent boundary attachments.
It is neither a global parent denominator nor an end-to-end D3 denominator.
The critical `(1,0,0)` clause remains universally open over all of those
quantifiers.

## Other D3 routes

The direct triple route retains its exact global source accounting:

```text
77,940,147 / 79,102,449 settled,
1,162,302 residual,
semantic SHA-256 a76a7c2cd6631c2d9724b450540bec7f3be6c106a41ae41f1736bbd2755a5ca4.
```

This lane decreases the residual by zero and proves no uniform
`Q3_COMPLETE_PARENT_BOUNDARY_ATLAS`.  Even complete triple closure would not
promote D3 while the pair invariant remains open.

The pair compression/descent route also remains `NULL`.  Its pilot has no
global stable IDs, no nontrivial split/merge specialization theorem, no
complete labels or strict closure, no genuine infinity subfunctor, and no
chain equivalence recovering global middle rank.  Current exact global counts
remain zero for adjacencies, strict closure pairs/triples, parent-infinity
cells, and middle-rank replays.  A four-fixture post-closure compiler cannot
manufacture those missing inputs.

## D9 alternatives

The exact alternating `A(4,8)` canary proves that reducibility is not
hereditary after even one proper extension: two witnesses for the same
restricted signature differ in whether the deleted element can be reinserted,
with a five-row positive Gordan dependence certifying the failure.  This
retires the inference that the `2,546` reducible or dual-reducible parents are
automatic induction cases.  It does not refute a witness-retaining theorem for
the simultaneous-insertion loci `G_(e,S)`, nor D9.  The exact parent split is
`2,546 + 58 = 2,604` realizable classes.

For sign-geodesy, the bounded replay passed only the local `P26`, `K2`, and
`Q2` factor-isometry phase before the mandatory midpoint stop terminated the
remaining run.  That partial run is classified `TIMEOUT` and excluded from
acceptance.  The pinned exact audit already states the decisive scope: 178
row-2599 points, 97,224 signatures, and 39,366 support traces have neither
chamber coverage nor adjacency coverage.  The first global quantifier is that
every complete residual-factor sign condition is connected and determines a
single chamber.  Separately, 142 new first-layer resultants retire only a
projection recursion restricted to parent brackets plus the 26,740 residual
factors; finite CAD/roadmap methods are not ruled out.

## Exact route handoffs

| Route | Handoff | First unclosed gate |
| --- | --- | --- |
| D3 joined-Gordan ten-clause theorem | `NULL` | universal mixed `(1,0,0)` cell with all parent/signature/support/specialization/true-boundary attachments |
| D3 uniform parent-boundary atlas | `NULL` | true-parent escape for all `1,162,302` residual source orbits |
| D3 pair compression/descent/equivalence | `NULL` | globally stable descent diagram and chain equivalence |
| exact counterexample/retirement | `NEGATIVE` | narrow no-gos proved; no theorem-attached counterexample to the surviving mixed/global routes |
| D9 simultaneous insertion over 2,604 parents | `NULL` | topology and coverage of every witness-retaining `G_(e,S)` |
| D9 actual-arrangement sign-geodesy | `TIMEOUT` | connected, unique complete sign cells with full roadmap coverage |

Overall handoff is `NULL`, because the exact negatives retire only auxiliary
lemmas and no surviving theorem-level route is proved or disproved.  No
load-bearing obligation closed, no globally attached residual decreased, no
bounded strict-decrease chain was established, and no construction successor
is eligible.

## Verification and replay

Completed exact replays:

```text
python -u ai/omreal/verify_diag3_joined_flow_triangle.py
python -u ai/omreal/verify_diag3_single_bad_two_skeleton.py
python -u ai/omreal/verify_diag3_row2599_common_proper_escape.py
python -u ai/omreal/verify_diag3_row2599_h1_radial_gap.py
python -u ai/omreal/DIAG9_GRAPH_REDUCIBILITY_NO_GO.py
```

Interrupted by the mandatory midpoint after the local metric phase, and not
used for acceptance:

```text
python -u ai/omreal/verify_diag9_sign_geodesy_audit.py --workers 4
```

The lane verifier is standard-library only, checks every source digest,
reconstructs the face-type partition and exact relative-chain ranks, checks
route scopes and proof distance, and rejects 18 hostile in-memory mutations:

```text
python ops/team/theorem-reset-falsifier/verify_falsifier.py
```

# D9 component-roadmap representation canaries

The certificate-language opening gate passes.  This is a representation result,
not a roadmap construction or a connectivity result.

The independent verifier replays four exact discriminators from pinned sources:

- the 16-chamber counterpair has identical memoryless local observations but
  positive-component counts `2` and `1`; full global adjacency digests keep the
  two configurations distinct;
- residual type 36 contributes the projection factor `c*d-c+f`, so the factor
  registry must grow on demand rather than assume the 13-type grammar is
  projection-closed;
- factor 8552 lies on recursive facet `1237` across 64 charts and remains
  boundary-only, with neither a strict-parent crossing nor a global separator;
- the exact canary `x^2=0` has gradient zero on the positive-dimensional locus
  `x=0, y` free, so point-only critical registries are rejected.

Five hostile mutations are rejected: erased global adjacency, erased novel
factor, false interior promotion, collapsed singular-locus dimension, and a
fixed-domain-to-diagonal-nine claim promotion.

Critical-system enumeration remains denied.  The next constructive preflight
must define the deterministic projection, compactifying boundary barrier,
Jacobian-minor stratification, exact algebraic-point encoding, and certified
path adapter.  Only after that preflight and a producer-independent replay may
the fixed-domain pilot solve any critical system.


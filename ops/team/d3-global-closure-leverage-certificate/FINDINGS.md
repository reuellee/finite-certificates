# Independent certificate: D3 global-closure leverage audit

## Frozen verdict

`STALLED_INFORMATIONAL / STOP_PIVOT`. No bounded construction target is selected.
The honest theorem ledger remains `2/9`, with both diagonal-three invariant
obligations open. This lane found no route that simultaneously has
theorem-level quantifier attachment, a certified finite/exhaustive progress
measure, and a preregistered bounded decrease capable of reaching diagonal
three in a bounded successor chain.

The reconstruction is independent of the audit and falsifier lanes. It reads
only repository objects pinned to commit
`98ed8d36f08f13305d226d3d3770e71542a0a408`, tree
`52a03a10b62e54a9ff5aa4e59dbbc2af9a69eeab`. No construction or global census
was run.

## What is actually complete

The direct D3 pair route has a substantial certified local foundation:

- the compactification is the 64-chart product of three closed positive
  projective 3-simplices;
- the support-face atlas contains 3,375 faces, of which the parent gate
  excludes 3,364 and retains 11;
- on the first two four-support closures, 8,017 ambient mixed restrictions
  reduce to 94 parent-reduced zero sets and 22 active wall equations, with zero
  unresolved local classifications;
- all 527,533 base cells are locally lifted: `133828 + 132134 + 261571`;
- all 16,935,101 local `v`-lift cells are present:
  `4496636 + 4047846 + 8390619`;
- the unresolved local `v`-fiber count is zero.

Those numbers certify a complete local fiber inventory on supports `(3,1,15)`
and `(3,3,7)`. They do not certify a global regular complex.

## Exact remaining proof distance

Seven end-to-end obligations remain:

1. Global gluing: zero certified global adjacencies.
2. Extension/bad labels: zero complete global label contracts.
3. Strict closure: zero certified global pairs and zero certified global
   triples.
4. Relative infinity: zero certified genuine parent-infinity cells.
5. Middle-rank replay: not globally computable from the current data.
6. The global exclusive-pair invariant: open.
7. The triple compact-component invariant: open, with 1,162,302 unresolved
   source orbits.

This is why the local denominator `527533` cannot be reused as a denominator
for global progress: the missing objects are incidence, label, infinity, and
rank objects, and the repository supplies no certified total count for them.

## Route decisions

The direct global master-closure route is attached to the pair theorem
quantifiers, but it lacks a certified exhaustive remaining-work measure and a
preregistered decrease across a bounded successor chain. It therefore fails
the selection bar despite the finite 64-chart ambient atlas.

The credible structural-compression idea is a component-specialization
cosheaf/roadmap representation that retains exact overlap and two-cell
incidence. The existing component-cosheaf pilot is `BOUNDED_NO_GO` only for
reusing the seven completed lift manifests as-is. All seven lack the required
global cell, strict-closure, infinity, and signature-label contract. The method
remains safe only as post-closure compression; no present bounded construction
shows it can replace the census.

The exact retirement route is narrower. Two relative CW complexes have the
same accepted component graph and the same one-skeleton, but the unfilled
complex has `dim H1(F2)=1` and the filled complex has `dim H1(F2)=0`. The
discriminator is two-cell incidence. This retires graph-only pair
certification, not diagonal three, and it is explicitly not a 9DVL
counterexample.

The deferred D9 B-UV-01 route is retained only as a low-leverage comparison
baseline. It has no theorem attachment to either D3 invariant and is not a
default successor.

## Replay

Run:

```text
python ops/team/d3-global-closure-leverage-certificate/verify_certificate.py
```

The standard-library verifier reads the pinned Git objects rather than lane
outputs, reconstructs every count above, validates all route/nonconsequence
claims, and rejects 42 re-sealed hostile mutations.

# Control-plane cycle: clipped-wall decision and global exit gate

## Pinned state and target

- Canonical base: `ae8a3afc24abfea94acf4b22ea35c2ca18f3c577` (merged PR #39).
- Honest theorem ledger at start and finish: `2/9`; diagonals 1 and 2 proved, both diagonal-three invariant obligations open.
- Bounded target: decide whether the registered triple-zero component can attach to the genuine `[3468]=g-a` wall through clipped macrobox 20, and replace projection-only growth with a finite global acceptance criterion.

## Independent tracks

| Track | Role | Outcome | Integration |
| --- | --- | --- | --- |
| clipped-wall prover | constructive prover | route refuted; `q16134>0` on macrobox 20 and boxes 6–20 zero-free | accepted |
| clipped-wall falsifier | red team | independent exact Taylor exclusion on the complete wall face | accepted |
| clipped-polytope certificate | certificate engineer | exact 640-vertex clipped cell and the same wall exclusion | audited but omitted as redundant repository weight |
| preliminary and final referees | independent verification | clean-archive replay, independent Taylor/Bernstein and topology checks | accepted bounded claims |
| global exit | theorem/formalization | one-way component-faithful sink-SCC certificate; graph-only pair-H1 route refuted | accepted after quantifier repair |

## Exact decision

The macrobox-20 terminal bridge is false. The stored `[3468]=0` witness has nonzero residual values, and `q16134` is strictly positive on the entire proposed wall face. A stronger independent certificate proves the whole deterministic suffix of macroboxes 6–20 is zero-free. No finer subdivision of that suffix can recover the missing triple-zero attachment.

The cycle therefore retires projection-sign-only corridor extension. Future triple work must carry occupancy and globally stable component identities, then terminate with the fail-closed sink-SCC test. Future pair work must add two-cell/strict-three-chain incidence; another exit graph or source edge alone is nondiscriminating.

## Ledger effect

- Added one exact route refutation and one accepted methodological lemma/no-go.
- Closed triple rows/orbits: `0`.
- Triple unresolved count: `1,162,302` (unchanged).
- `diag3_triple_hc0`: `OPEN`.
- `diag3_pair_hc1`: `OPEN`.
- Honest score: `2/9` (unchanged).

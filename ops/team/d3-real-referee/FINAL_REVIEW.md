# Independent final review: real fibers after cefc495

**ACCEPT the three scoped auxiliary claims and the NULL/STALLED original-target
closure. No original obligation is closed and the theorem ledger remains
2/9.** No actionable mathematical defect was found in the frozen candidate.
This review does not authorize publication or modify any canonical ledger.

Reviewed immutable candidate:
`65f1524bda77ebc5b74a0857e23c4323c18ad263`.
Original requested base:
`cefc495b27247c4a76f88bce04978422161f7ce9`.
Independent referee branch: `agent/d3-independent-referee-20260905`.
This is the separate final audit under the 20 active minute ceiling; it is
not an extension of the completed opening audit or a new discovery lane.

## Accepted scope and exact provenance

| Candidate claim | Verdict | Original target effect |
| --- | --- | --- |
| A complete compact fixed-base source oval avoids all 70 parent brackets | ACCEPT, exact counterexample to universal fixed-base escape | No compact full-source or actual triple-bad component established |
| Generic same-factor occurrence H1 vanishing for arbitrarily many independently signed blocks | ACCEPT, with all stated genericity and pencil hypotheses | Original alternating direct-sum pair map remains uncomputed |
| No nonzero constant symmetric second-order operator annihilates the original graph numerator | ACCEPT, exact rank certificate | No full-source noncompactness or source-count reduction |
| Unchanged theorem/source accounting and stopping decision | ACCEPT | 2/9; zero original obligations closed; STALLED |

The replay records SHA-256 values for all 51 reviewed or inherited input
files in `FINAL_ARITHMETIC.json`. Principal claim identifiers include:

| Frozen artifact | SHA-256 |
| --- | --- |
| Oval `certificate.json` | `790daed2e50a77b424873c1e18dca918b2497970286aa95d055a84871c3b0bf4` |
| Oval `REPORT.md` | `4ef44739091bb9bacd59837332b6147defa51b6d83531b6159fec71124abfeea` |
| Pair `FINDINGS.md` | `6425d367675fc709cc65f4560e654b645cb4f92f07571bb81f74ca0e9ab99778` |
| Global `HARMONIC_SCREEN.json` | `701e2f95064834409218ea5518f56b77d5ebddf4d1d55a9262f7eddcd344add3` |
| Cycle `CYCLE_REPORT.md` | `6d830146fc74646cbd27ffbfa68ecddbed6491db4476a5820354ab24ee05c7e9` |

The referee reads candidate bytes with `git show` at the exact reviewed
revision. Neither producer code nor a producer verifier's acceptance logic
is executed or imported. The only shared arithmetic is this referee's own
previously independent `audit_cefc495.py`. New resultant construction,
Sturm division and sign variation, signed chain calculations, actual normal
circuits, and matrix-rank checks are independently implemented using the
Python standard library.

The opening audit already reconstructed the original factor equations and
their normalized determinant provenance. The final audit repins those exact
source bytes and again uses only the first three equations. It does not
impose the 56 formal height-critical minors. The candidate has not changed
V11, the research decision ledger, or the original equation object.

## Complete parent-interior oval

The referee independently substitutes the displayed rational base in the
original equations, derives `d=-253/51`, verifies the rational graph for a,
and checks both remaining equations by clearing only their actual graph
denominator. The denominator is exactly
`-9180[1267] = 9180ce-92565`. The full graph numerator specializes to a
nonzero constant multiple of the candidate's F. Thus this is a component of
the original fixed-base factor source on the parent domain.

Independent exact checks give positive C2 everywhere, the displayed quartic
W, a squarefree Sturm sequence, and exactly one real root in each of the
four claimed disjoint rational intervals. The two middle roots lie strictly
inside `J=[-53/25,-403/200]`, while the other roots lie outside J.
The positive quartic leading coefficient gives W positive between its middle
roots. Its two square-root graphs join at both endpoints, giving a complete
compact oval of the affine F-curve. C2 never vanishes. At branch endpoints,
squarefreeness and `W'=-4C2 F_c` ensure smoothness; away from them F_e is
nonzero. The two empty c-gaps separate this oval from the outer components.

For every one of the 70 increasing-label parent brackets the referee
rebuilds its original 4-by-4 determinant, substitutes the fixed base,
clears a by the exact rational graph, and reconstructs its Sylvester
resultant with F. All 70 resultants agree with the certificate up to a
nonzero scalar. The denominator resultant is checked separately. The
largest resultant degree is eight. All 71 are nonzero and have zero real
roots on the **closed** interval J: endpoint values are nonzero, and exact
Sturm variation differences are zero. A common finite e-root would force
a resultant zero, so no bracket or graph denominator vanishes anywhere on
the oval, including its branch endpoints. Possible degree drops in a
specialized resultant cannot defeat this implication.

At c=-21/10 the claimed e-isolator contains one root by an independent
Sturm check. Rational interval Horner bounds establish strict signs of the
denominator and all cleared bracket numerators. The resulting 70-character
sign string agrees exactly with the candidate. By connectedness and
nonvanishing, these signs hold on the entire oval. The graph and coordinate
projection are continuous inverses, so its image is a compact smooth circle
inside a single uniform parent sign component.

This disproves the claim that **every compact oval for every base** must
hit a parent wall. It does not dispute the accepted cefc495 historical-base
result. Most critically, a component after fixing five coordinates need
not be a component after allowing those coordinates to move. No original
proper incomparable extension triple has been attached to this oval. The
candidate explicitly preserves both limitations. It is not a counterexample
to 9DVL and does not remove a residual source record.

## Arbitrarily many signed blocks

The inherited six-kind generic occurrence classification is pinned at
`DIAG3_PAIR_FACTOR_ROOT_SWITCH.md`, SHA-256
`e57ccb4c5dfdf209f7534b45080eea7a88cee7457527c4d720574f639a9f0338`.
The reviewer checked its actual hypotheses and support description. This
review does not rerun that full global classification. It independently
checks the new extension conditional on that existing theorem.

For kind 38, the six n12k normals lie in the annihilator of a parent line,
hence in a two-dimensional vector space. Under the retained generic rank
and nonzero circuit-coefficient assumptions, the two core normals have a
nonzero combination in that space, giving a seventh projective ray w.
Choose an affine coordinate avoiding all seven rays, then order the six
outer rays. Rescaling may reorient them, which is harmless because all
signings are allowed. Only the order of t_i and w affects the signs of
`w-t_j` and `t_i-w`. Equally spaced model points are therefore valid for
sign patterns, without claiming a projective equal-spacing theorem.

Each active edge requires one specified bit at each of its two endpoints.
Starting from s0, it survives a finite collection of signings exactly when
neither endpoint is changed by their bitwise disagreement union d.
Consequently the intersection equals `P(s0) intersect P(s0 xor d)` for
**every finite number** of blocks. This argument supplies the unbounded
quantifier; a finite ternary table alone would not do so. Blocks with
incompatible core signs give the empty pattern and no H1 obstruction.

The referee independently verifies the 58 single patterns and 180 pair
patterns in every gap, closure under intersection, all 1,835,008 ternary
identities, and the union of 487 masks. All 105 matching-triangle instances
have an additional vertex h. The three oriented triangles through h have
exactly the missing triangle's boundary over the integers, and each uses
at most seven normals. Since the graph is complete, these unit fillers
kill every one-cycle over any coefficient ring. The support structures
for the other five kinds already work for every active subset, as the
inherited theorem states. Thus the new generic arbitrary-block theorem
follows with the claimed coefficient scope.

As a genuine-parent arithmetic anchor, the referee reconstructs all parent
brackets and derived normal determinants at
`(-1211/27,-26,-32,28,-15,6,-19,-2,-10)`. The six stated outer normals
have rank two, all eight have rank three, and all 15 core-plus-pair supports
have full rank-three circuits with no zero coefficient. All 256 direct
normal reorientations produce precisely the model's 58 gap-one patterns.
This verifies the attachment of the model to real parent normal geometry;
the one anchor does not replace the general pencil argument.

The synthetic signing limitation is essential and correct. The common
intersection for signings (8,25,26) at gap zero is 512, but no pair among
those three original patterns yields 512. The synthetic pair (8,27) does.
The result is not a two-signature Helly theorem. Extra rank drops, vanishing
weights, changing factors, strict closure, geometric incidence, and genuine
parent infinity remain outside its generic pointwise scope. Accordingly,
none of it computes or makes injective the original alternating map.

## Constant-metric obstruction and numerical scope

The referee reconstructs the 389-term N from the original equations,
differentiates it in all seven graph coordinates, and forms all 28
symmetric coefficient columns with the off-diagonal factor two. Their
union contains 1,191 monomials. Every row of the selected 28-by-28 matrix
matches the independently differentiated coefficients; a separate rational
elimination gives determinant `-7421703487488`. Its kernel is zero over Q
and R. A constant positive-definite matrix is therefore impossible. An
invertible affine coordinate change of N would pull a Laplacian back to
such a matrix, so that corollary is valid.

This is an identity on full affine coordinate space for this particular N.
It does not exclude nonlinear coordinates, nonconstant multipliers,
variable coefficients, or an operator identity only on the zero set.
It proves no compactness or noncompactness of that zero set.

The exact original (a,d) Jacobian pivot of the first two equations is also
nonzero on the uniform parent domain, validating the stated multiplier
normalization for the exploratory critical system. The numerical log
contains 120 trials, 32 success flags, 70 small-residual outputs, and zero
outputs passing its joint residual/bracket threshold. The referee verifies
these as descriptive counts from the recorded floating-point log only.
There is no independent exact critical-point certificate, exhaustive search,
or emptiness inference. The report gives that diagnostic no theorem credit,
which is the correct treatment.

## Closure accounting and hostile controls

V11 and the original decision ledger independently agree on
77,940,147 settled source records plus 1,162,302 unresolved records,
total 79,102,449. Their bytes are unchanged from cefc495. All seven original
obligations remain open, and pair/global component coverage remains UNKNOWN.
The source records are not a component denominator. The original alternating
sum is retained; separate injectivity of its three restrictions would not
settle it.

The accepted structural-reset closing report records inherited streaks
10/13, following V11's historical 9/12 baseline. Its independent negative
certificate acceptance and unchanged ledger are also pinned. The two later
coordinator investigations supplied no accepted closing counter update.
This audit's retrospective acceptance of the historical slice mathematics
does not silently create another historical cycle closure. Counting this
completed cycle exactly once therefore gives **same-blocker streak 11 and
zero-ledger streak 14**. No historical file is rewritten.

The unchanged first six proof-distance coordinates justify STALLED for the
requested theorem, with INFORMATIONAL credit for the auxiliary results.
Retire universal fixed-five-base escape and the tested constant-metric
harmonic mechanism. The pair compression is useful but does not justify
more same-route signing enumeration. The coordinator's NULL close and stop
without a claimed ready successor are justified.

Twelve hostile controls reject corrupted oval algebra, omitted parent
brackets, a false resultant, an enlarged interval containing a genuine
denominator root, repeated branch roots, duplicate Hessian rows, altered
Hessian coefficients, a matching-only unfilled cycle, a reversed integral
filler, treating the synthetic pair as an original pair, deleting genericity,
and promotion to the original pair-map theorem.

## Portable replay and review boundary

Run in any clone containing the reviewed commit and these referee files:

```text
python -B ops/team/d3-real-referee/audit_real_fiber.py
```

An optional `--repo PATH` selects another clone. No absolute workspace path,
network access, external package, producer execution, or publication is
required. Outputs are deterministic. `FINAL_ARITHMETIC.json` contains the
reconstruction facts, hostile outcomes, exact accepted revision, and all
input hashes; `FINAL_MANIFEST.json` binds this report and replay.

Later coordinator closing records may cite this acceptance while preserving
the frozen producer bytes. Acceptance of any changed mathematical candidate
would require another explicit review; no such change is needed here.

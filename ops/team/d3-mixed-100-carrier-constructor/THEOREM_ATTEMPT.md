# Universal mixed `(1,0,0)` carrier theorem attempt

## Frozen handoff

```text
NULL / STALLED / STOP
```

The full O3+O4 statement is not proved.  The first missing edge is

```text
DECLARED_L_SOURCE_INTERFACE
  -> O3_MIXED_GEOMETRIC_RELATIVE_BOUNDARY_SURJECTIVITY.
```

The interface supplies a finite, functorial, oriented lower two-skeleton and
a finite declared list of relevant primitive classes.  It does not supply a
geometric mixed degree-three carrier functor, its relative boundary map, or a
theorem that this boundary hits those primitive classes.  The pinned Gordan
results do not derive such a functor: they prove the single-block terms, one
local flow-disk class, a no-go for singleton-root fillers, and one local proper
triple-bad ray.  The missing mixed comparison homotopies and all universal
specialization data remain absent.

This is a constructor null, not a negative result.  In particular, it is not
an admissible `L_source` counterexample and does not show that a genuinely
mixed carrier cannot exist.

## Statement attempted

Let `F` be the full face category of any finite `L_source` satisfying the four
interface clauses pinned in the theorem-feasibility memo.  The objects range
over every source-derived parent/component/chart/antichain/active-block/
support stratum in the declared scope, and the morphisms include support loss,
zero weights, chart changes and monodromy, parent-boundary specialization,
parent-rank and witness-rank strata, residual walls, and active-block
permutations.  All composable face flags are morphisms in `F`.

For each object `x`, write

```text
L_2(x) --d2--> L_1(x) --d1--> L_0(x)
```

for the relative lower complex (with the certified true-infinity part
quotiented), and let `P(x)` be the declared relevant primitive integral
classes in `ker d2`.

The attempted theorem requires a finite geometric chain group
`M_3^mix(x)`, generated by genuinely mixed labeled `(1,0,0)` cells and their
active-block permutations, together with a relative boundary

```text
b_x : M_3^mix(x) -> ker d2(x)
```

and integral assignments `s_x(alpha)` for every `alpha in P(x)` such that

```text
b_x(s_x(alpha)) = alpha.
```

For each face morphism `f:x->y`, it further requires mixed-chain maps
`M_3^mix(f)` for which

```text
b_y M_3^mix(f) = L_2(f) b_x,
M_3^mix(f)(s_x(alpha)) = s_y(L_2(f)(alpha)),
```

with the evident integral extension when the image is a signed sum of
declared primitive classes.  Equality must hold for every composite flag,
not just up to an unrecorded homotopy.  All geometric parameter ends must be
proper over certified true parent infinity; chart cutoffs, support loss,
zero weight, and interior witness-rank loss may not be substituted for it.

This functor-plus-natural-section formulation is exactly the combined O3+O4
burden.  It is not an extra restriction: `b_x s_x = id` is the required
boundary equation, and naturality of `s` is the required equality on all face
flags.

## What the declared interface actually gives

The four clauses give:

1. finite labeled `C_0,C_1,C_2` generators over the full declared source
   scope;
2. integral `d_1,d_2`, orientations, `d_1 d_2=0`, and a finite declared list
   of relevant primitive kernel classes;
3. provenance and separate chart/support/zero-weight/witness-rank/
   parent-rank/true-infinity tags; and
4. a simultaneous lower-source subdivision whose specialization maps form a
   category on every composable face flag, equivariant for active-block
   permutations.

These data define `L_{<=2}:F->Ch_{<=2}(Z)` and the primitive-class records.
They do not define any of:

- the admissible semialgebraic mixed-cell space `M_3^mix(x)`;
- a finite source-derived list of its cells and all of their frontiers;
- the boundary `b_x` and a proof that `P(x)` lies in its image;
- relative acyclicity of the carrier assigned to every lower face;
- a properness theorem sending every unbounded mixed parameter end to actual
  `I = Xbar minus X`;
- specialization maps on mixed cells, loop descent, or a natural section on
  arbitrary face flags.

Thus `d_1d_2=0` proves only that the listed classes are cycles.  It does not
prove that they are geometric mixed boundaries.

## Exact logical frontier

There is a small algebraic witness to the insufficiency of the lower-chain
axioms.  Take one object and

```text
C0 = 0,  C1 = 0,  C2 = Z<a>,  d2 = 0,
P = {a},
```

with only the identity specialization.  Then `d1 d2=0`, `a` is primitive,
and `H2 = Z`.  No degree-three boundary exists in these supplied data.  This
is only an axiom-independence witness for the algebraic part of the interface:
it is **not** asserted to be source-derived or geometrically admissible and is
therefore not a counterexample to the quantified theorem.

The row-2599 chart-zero lower complex is a theorem-attached diagnostic of the
same missing operation.  Its exact ranks are

```text
rank(d1)=3, rank(d2)=6, rank(C2)=7, H2=Z,
alpha=(-1,1,1,1,1,1,1).
```

The singleton-root cocycle evaluates `1` on `alpha`, so all fixed-one-block
root-only fillers are excluded.  This does not obstruct a genuinely mixed
chain.  It shows that any successful O3 theorem must genuinely add a mixed
boundary column and cannot derive it from the existing lower-source columns.

## Candidate proof routes and their first failures

### 1. Formal functorial cone

Algebraically one can set `D_3(x)=ker d2(x)`, make its boundary the inclusion,
and restrict each lower specialization to kernels.  This gives a functorial
algebraic filler for every cycle and automatically respects composites.

It is not an O3 construction.  A basis vector of this formal cone is a cycle,
not a semialgebraic `(1,0,0)` cell.  The construction proves no three-block
witness feasibility, support bound `6+5+5`, source provenance, frontier
identity, properness, or true-infinity attachment.  Declaring the formal
generators to be mixed would merely rename the conclusion.

### 2. Acyclic-carrier extension

The standard induction would work after assigning every lower face a
relative-acyclic mixed carrier, functorial on `F`.  At the dimension-two step
one needs the relevant `H_2` of each assigned relative carrier to vanish.
Neither this relative acyclicity nor the carrier assignment is in the
interface.  The exact row-2599 lower carrier instead has `H_2=Z`; the missing
mixed cell is precisely what would have to kill it.  Invoking an
acyclic-carrier theorem before proving that hypothesis is circular.

### 3. Cone the row-2599 flow disk along the common proper ray

The pinned replay proves one common proper triple-bad ray ending on the true
parent wall `[2467]`.  It does not give a continuous face-natural cone from
the seven lower two-cells to that ray.  Only four of six required comparison
incidences are certified.  The radial `H1` shortcut fails because its second
`p01` stage has no parent-infinity frontier; the nonradial five-stage `p01`
compatibility homotopy remains open.  Even completing this canary would not
cover every parent, class, support, wall, chart, placement, or permutation.

### 4. Choose objectwise fillers and transport them

No objectwise universal filler theorem was obtained.  Even assuming arbitrary
objectwise choices, the lower specialization functor supplies no maps between
the chosen mixed-cell spaces.  Codimension-one repairs would still not prove
equality around chart loops or along all longer flags.  This route therefore
does not reach O4.

## O4 status

O4 is not independently claimable after O3 fails.  The source category is
present only through degree two.  To prove O4 one must first construct a mixed
degree-three functor and then a natural right inverse of its relative
boundary.  The prior naturality audit records the same missing seams:
support loss, zero-weight cospans, chart overlap, monodromy, arbitrary flags,
rank distinctions, and true-infinity attachment.  None is universally closed
by the local replays.

## Why the handoff is NULL rather than NEGATIVE

The algebraic witness above is deliberately not promoted to an admissible
geometric `L_source`.  “Source-derived” includes realizability, proper
antichains, witness faces, and compactification provenance that the witness
does not instantiate.  Likewise, failure of the formal cone, acyclic-carrier
template, local radial cone, or objectwise-choice route refutes only those
arguments.  No exact obstruction has been proved against every admissible
genuinely mixed geometric carrier.

Conversely, the local common ray and the inability to extend the singleton
cocycle are not a proof of existence.  The universal boundary, properness,
and specialization quantifiers remain open.

## Frozen conclusion

- `O3_universal_mixed_chain`: **OPEN**.
- `O4_arbitrary_flag_coherence`: **OPEN**, blocked behind O3 and additional
  mixed-functor descent data.
- Selected-route residual: `2/2 -> 2/2`.
- Canonical ledger: `2/9`, unchanged.
- Formal shape count: ten, with provisional `3/10`; not global coverage.
- Pair coverage and residual: `UNKNOWN / UNKNOWN`.
- No fixed-block filler, artificial infinity, atlas, census, residual growth,
  canonical edit, GitHub write, external solver, or external compute was used.

The constructor must therefore freeze `NULL / STALLED / STOP` at
`O3_MIXED_GEOMETRIC_RELATIVE_BOUNDARY_SURJECTIVITY`.  Same-route continuation
is not justified by this lane.

## Exact replays

All exited zero from the frozen lane opening:

```text
python -B ai/omreal/verify_canonical_research_state_v10.py
python -B ops/research-team/cycles/2026-09-03-d3-mixed-block-100-universal-carrier-gate1/verify_opening_state.py
python -B ai/omreal/verify_diag3_joined_flow_triangle.py
python -B ai/omreal/verify_diag3_single_bad_two_skeleton.py
python -B ai/omreal/verify_diag3_row2599_common_proper_escape.py
python -B ai/omreal/verify_diag3_row2599_h1_radial_gap.py
```

The first two replay governance and canonical state.  The remaining four
replay the exact lower-chain facts and local canary frontier only; none is
used as a universal mixed-carrier proof.

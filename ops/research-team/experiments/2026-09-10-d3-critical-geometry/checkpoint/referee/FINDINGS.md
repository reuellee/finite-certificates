# Independent referee findings

Status: a global triple subclass is now proved; original diagonal three is
not yet proved. The inherited complete factor-pair endpoint remains accepted
input, 9,476/9,476. No numerical new triple-orbit count or original diagonal
is credited by this review; original status is2/9.

TWO_PARENT_LINE_AUDIT.md accepts the frozen conventional proof that every
triple with at least two factors in global families36/38 has no compact
component, with an arbitrary third factor and every original parent
component. The two concurrences have global parent-line parameters, reducing
the entire triple to four affine equations in four heights over an open
six-dimensional base. All coefficient rank drops and concurrence collisions
are included. The reviewed proof hash is recorded in that audit and HANDOFF.

COINCIDENCE_EXCISION_AUDIT.md also accepts removal of the closed union of
all three coincidence loci p=q, p=r, q=r. Re-anchoring at the repeated
point gives positive-dimensional full convex height fibers. This leaves
distinct points, including distinct collinear points; no collinearity escape
has yet been proved.

PENCIL_AUDIT.md accepts the finite231-pair critical-point exclusion at the
single specified projected parent. All22 actual quadratic determinant
inputs were rebuilt independently; a clean replay regenerated identical
evidence, and altered-polynomial, missing-pair and unresolved-exception
controls were rejected. This carries no compactness or global-parent
conclusion. FINAL_REVIEW.md consolidates the final verdict and root-report
scope audit; REVIEW_IDENTITIES.json binds the reviewed files.

The source audit removes an avoidable coverage restriction. All six global
primitive factor kinds have ordinary four-row occurrences, so every factor
can be given a unique concurrence point throughout its wall. The explicit
four-line contraction construction gives four projected-parent coordinates
and four free parent heights for each kind. This includes global36 via
ordinary type37, global38 via ordinary38, and the familiar types48–51.
Required projected-parent coincidences are retained. All original parent
components, all positive-normalization sign sectors, concurrence collisions,
and lower coefficient-rank strata are included in the stated model.

See ANCHOR_COVERAGE_AUDIT.md for the source dependencies and conventional
proof, and ANCHOR_FRAME_REPLAY.json for an independent exact reconstruction
of the six displayed projected frames. The replay has no producer imports:

    python referee/verify_anchor_frames.py

The exact final 1,162,302-row residue distribution has not been independently
reconstructed. In particular, no assertion that all its rows have a50/51
anchor is accepted. The all-factor coordinate extension makes that assertion
unnecessary for the proposed global model.

Two remaining objects are now precise. In the retained Q/R-direction model,
the rank-six feasible augmented-system compatibility locus still needs
component noncompactness. In the parent-height projection model, the closed
locus where the two vertical wall gradients are dependent still needs Hc0
vanishing. REGULAR_HEIGHT_REDUCTION.md accepts the latter reduction but not
its missing critical-locus hypothesis.

Next discriminator: a frozen global proof for one of these remaining loci,
or an exact actual-parent compact component of a fixed projected-parent
height fiber that rules out that stronger fiber theorem. A failure of the
stronger fixed-fiber theorem would not by itself refute full triple escape
or original injectivity.

# Proof-track findings

The new constructive result is a global triple noncompactness theorem whenever
at least two of the three factors belong to the global families36/38. It uses
their ordinary parent-line concurrences to obtain four affine equations in
four parent heights over an open six-dimensional parameter base. The proof
retains every coefficient rank, concurrence collision, allowed projected
coincidence and original parent component. See `TWO_PARENT_LINE_FACTORS.md`.

This is a genuine all-quantifier subclass theorem, but no new numerical
triple-orbit count or original diagonal credit is asserted. Its overlap with
the authenticated already-covered source layers is the coordinator's audit.
The universal factor-pair endpoint remains the accepted input9476/9476;
the original ledger remains2/9.

I independently accepted the referee's extension of the four-projected/four-
height contraction chart to all six global factor kinds. The review is
`ANCHOR_AUDIT_REVIEW.md`. This removes the earlier anchor-kind limitation from
the general triple height model.

The full unresolved triple endpoint has also been reduced to the
collision-free vertical critical locus. All three pairwise-concurrence
collision pieces have vanishing H_c^0. Away from them, wherever the two
remaining wall gradients in the four parent heights are independent,
projection to the open projected-parent base is a submersion and excludes
compact components. The remaining closed-relative critical locus is detected
without poles by the circuit-gradient formula in
`VERTICAL_CRITICAL_REDUCTION.md`.

The inherited simultaneous (39,48,50) critical canary has p48=r50 and both
vertical gradients zero. In fact the global identity
q39=(f-i)q48+(1-c)q50 makes its entire triple zero set a previously proved pair.
`critical_canary.py` independently reconstructs all70 nonzero parent brackets,
three ordinary concurrence kernels, circuit gradients and this exact identity;
`CRITICAL_CANARY.json` is its output. This diagnostic earns no new count.

The following tempting steps remain unproved and must not be used:

* Fixed-direction rank6 is populated and cannot be dismissed by dimension.
* Vertical criticality has not been proved to force concurrence collinearity.
* Even distinct collinearity alone has not been proved to reduce the
  eight-by-six height coefficient rank below6.
* With only one parent-line factor, the analogous construction leaves five
  affine equations in four heights over a seven-dimensional base. Its one
  compatibility determinant is not automatically affine in a concurrence
  parameter, and a forbidden parent endpoint does not make its leading
  coefficient vanish identically.

The next precise discriminator is an actual collision-free vertical critical
point with noncollinear concurrence points, or a structural proof that none
exists. The stronger useful positive endpoint is H_c^0 of the full
collision-free critical locus equal to zero. The independent falsifier's
bounded fixed-projection quadratic probe is supportive but cannot substitute
for that global proof. No finite-certificate requirement prevented this work.

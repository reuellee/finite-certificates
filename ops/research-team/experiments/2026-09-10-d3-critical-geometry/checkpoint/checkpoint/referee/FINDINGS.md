# Final independent findings

**Universal stronger factor-pair endpoint P is proved: 9,476/9,476.** Original D3 injectivity and triple H_c^0 remain formally open, and the original ledger stays **2/9**. The all-factor triple noncompactness theorem is now the sole missing hypothesis in the inherited sufficient route to **3/9**.

## Accepted principal theorem

The final proof is `original/CONCURRENCY_HEIGHT_PAIR_VANISHING.md`, SHA256 `c4542171cc2cd0c1dd5007bc80d3dfa2e079892cf6417edce760410eed069f91`. Its independent deductive review is `CONCURRENCY_HEIGHT_AUDIT.md`. Retaining both unique concurrence points gives four affine equations in five height variables. The proof retains positive normalization sectors, allowed projected coincidences, full convex fibers, the orientation local system, all coefficient-rank drops, and the closed concurrence-collision locus. No orbit-by-orbit computation is needed for the all-type50/type51 statement.

The earlier one-column and two-column proofs are independently accepted intermediate results. The final accounting is:

| Stage | Covered factor-pair orbits | Remaining |
| --- | ---: | ---: |
| Inherited | 8,902 | 574 |
| One-column affine application | 9,431 | 45 |
| Two-column affine application | 9,458 | 18 |
| Universal concurrence-height theorem | 9,476 | 0 |

The last theorem covers all type50/type51 relative labelings, including the previous layers. These counts are not original parent components or percentages of the original proof. No triple-source count changes.

## Independent exact checks

`verify_affine_pair_coverage.py` reconstructs the529/45 partition and actual pivots from the authenticated574-row source, rejecting six hostile mutations. `verify_two_column_coverage.py` reconstructs27 orbits,39 presentations, the fixed-normal rank/noncoincidence witnesses, and the final18-row partition, rejecting four hostile mutations. Neither trusts stored degrees or imports producer acceptance code.

`verify_conic_specialization.py` reconstructs the actual two-to-one conic-fiber component change using rational permutation determinants and exact univariate root isolation. It independently derives the parametrization and all70 parent inequalities. A common t=5 path preserves all70 parent signs throughout the c interval, so both fibers belong to one parent component. Independent relabeling identifies source254. This is an obstruction to one conic projection's shortcut, not to the accepted height theorem or to original injectivity.

`verify_conic_markers.py` reconstructs285 parent-marker presentations on44 records and306 valid ruled-quadric presentations across all45 balanced records, including every rank/skew/nonzero witness. Their intersection has238 parent-marker presentations across44 records. Four hostile mutations are rejected. The normal-form proof is accepted at its stated geometric scope: smooth split quadric, honest plane, forbidden smooth section marker, and retained reducible sections. Source573 gets a parent-line intersection marker. These chart facts alone yield no H_c^1 theorem.

The four referee replays contain fourteen hostile controls. The conic specialization checker uses SymPy only for univariate rational-polynomial arithmetic and certified isolation; the other three use the standard library. No producer modules are imported. The universal final theorem is accepted deductively, not inferred from these examples.

The resumed independent challenge is recorded in `newfalsifier/`. It separately accepts the final proof and reconstructs an actual rank-four system with five heights and its entire strict-parent interval. Its `verify_triple_rank_independent.py` independently rebuilds the alternative fixture with Fraction arithmetic: the8-by10 matrix has rank6 and only the three shears and scale in its kernel; the normalized8-by6 matrix has rank6. It also checks the polynomial factor/unit identities and all70 parent brackets. Its scoped triangular-escape review is accepted. This is an already settled predecessor fixture, so no triple count or original obligation is credited.

## Source and auxiliary scope

`SOURCE_AUDIT.md` records original internal antichains, parent terms in supported duality and the historical D4/D8/D9 route limits. The exceptional58-parent contractibility source qualification is retained. Historical workflow stop rules do not override current conventional-proof authorization.

This referee authored `PARENT_H4_CANDIDATE.md`; a different agent accepted it in `falsifier/PARENT_H4_INDEPENDENT_AUDIT.json`, binding SHA256 `4b2c956617826560a0d5b95d248300d3ef94b6bf17a7409f85694725dd7c9af5`. It validates D5's supported bad-union equivalence without blanket parent contractibility, not D5 vanishing. In its residence argument, a full fixed quotient fiber is closed in the face stratum; that and its genuine parent end justify noncompactness of the containing component. No self-approval or original diagonal credit is assigned.

## Precise remaining obligation

The inherited comparison injects H_c^2 of each original bad union into H_c^2 of its aligned full wall union. Completed P kills the pair-factor term. Triple-factor H_c^0 can still contribute, including triples aligned to one signature. Therefore P alone does not establish original D injectivity or pair-bad H_c^1 vanishing.

Proving noncompactness of every component of every full triple-factor intersection would now complete this sufficient route to original diagonal three. This quantifier covers all factor triples, not only three-color matched triples. The1,162,302 inherited residual source records remain unchanged. The next retained-concurrence obstruction is the actual rank-six compatibility locus, with remaining factor kinds, collisions, lower ranks and parent infinity also retained. No new triple escape theorem or completion-time estimate is claimed.

Root REPORT.md and CLAIM_LEDGER.json accurately reflect this status and are accepted at their pinned final snapshots.

# Independent supplement: proportional height sections and the covered witness

The three deductions below are ACCEPTED in their stated scopes. This supplement does not change previously bound audit files.

## General closed proportional-section excision

Reviewed `noncollinear/escape_redundant_height.md`, SHA256 `13fe6f21ab42e3a36a0cfef2a9a529177b1b5425175de9fa2851feb8a99088c3`.

The parent-unit products U,V are fixed in the theorem and nowhere zero throughout the actual parent residence. Equality UR=VQ in the full polynomial height ring is therefore a finite closed coefficient condition on the projected base, and it identifies the complete triple fiber with the complete P/Q fiber. The ordinary Q concurrence gives the closed q=p projected-only piece and, off it, four affine equations in five height variables. The nonempty fibers remain full positive-dimensional open convex affine subsets at every rank. Compact-support degree-zero base change and localization establish Hc0 vanishing over any such closed coefficient base. No base smoothness or open-image assertion is needed.

The proof correctly distinguishes an identity on the entire nonempty open height residence from a common factor on a selected branch. A componentwise extension needs a closed selected branch with full concurrence-fiber saturation, including q=p, and that hypothesis is not supplied by a gcd. Finite sign/normalization sectors are retained. This is a valid general sufficient excision theorem, with no global claim that every proportional pair of gradients satisfies its hypothesis.

## Specific global rational graph

Reviewed `noncollinear/escape_global_graph.md`, SHA256 `18ad54664a496d827c9120cbc9f8c73b961135a9ac93f439f37d7dfa3c89d5f6`.

The first-five-parent frame exists on every uniform parent. The fixed sign reorientations are homeomorphisms and are retained, so the argument does not identify original components. Independently reconstructed raw determinants give the two claimed ideal identities. Their divisions use b,c,h,f-i, each an explicitly verified nonzero parent bracket. They establish exact equivalence to the rational graph, including every coefficient specialization permitted by uniformity. Its inverse forgets three coordinates; the full sign residence is therefore an open subset of R6, not merely homotopy equivalent to one. Every component is noncompact.

Independent replay: `referee/verify_global_graph_independent.py` and `GLOBAL_GRAPH_REPLAY.json`. It uses referee-owned permutation determinants and symbolic simplification, imports no producer helpers, and rejects two deliberately incorrect algebraic controls.

This support triple was already covered by the inherited light-label escape: parent7 occurs only in the distinct normal378. The explicit graph supplies a sharper description, with zero new coverage or method credit.

## Complete critical-locus classification for that support triple

Reviewed `noncollinear/escape_critical_classification.md`, SHA256 `38ae60867ac8760dc570e1bd8f7155e8294167033b3a60662e826e45f5bd4a22`.

The chart is the accepted full P48 contraction chart: the complete quadrilateral fixes the six high projected parents, the omitted two have affine projective coordinates selected by excluded-plane units, and the first four parents fix the four height gauges. Constant sign sectors and relabelings are transported explicitly. The selected denominator coordinates are genuine nonzero parent brackets, so no original uniform subcase is omitted.

Independent raw determinant expansion gives P=0 and R-(2u-1)F=3(k+2t)(2u-1)H, H=t(x-2)-w(z-5). The entire coefficient identity R=(2u-1)F holds exactly at x=2,z=5. Writing F=vF_v+C with C=3(2t-5w)(k+2t), the three parent units v,2t-5w,k+2t prove F_v=-C/v is nonzero on F=0. Off that projected base, dH has zero v coordinate and a nonzero w or t coordinate, hence is independent of dF. On the base it vanishes. The parent-unit row operations between (dF,dR) and (dF,dH) are invertible on the triple locus. This proves the stated entire critical-locus equality and its inclusion in the general removable proportional-section piece.

Independent replay: `referee/verify_critical_chart.py` and `CRITICAL_CHART_REPLAY.json`; all raw identities and parent-unit signs are reconstructed without producer imports. This classifies the full chart for the specific already-covered triple, not arbitrary source triples or the authenticated unresolved residue.

The original score remains2/9. No numerical triple count or original obligation is promoted by this supplement.

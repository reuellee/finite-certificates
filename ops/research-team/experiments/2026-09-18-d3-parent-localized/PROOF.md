# Parent-localized continuation: exact forbidden-boundary classification

18 September 2026. Original diagonal ledger: **2/9**. The full authenticated
triple and the semidefinite target remain open. No source orbit is closed.
This is a source-specific deduction with exact arithmetic checks, not independent
mathematical referee acceptance.

## 1. Scope and notation

Use the inherited complete P-contraction chart for canonical survivor
(5563,4373,23221), named (5563,16134,19284). The parent matrix is

```
Y = [0 0  0 1 1 1   1      1  ]
    [0 1  1 0 0 B   C      D  ]
    [1 0 -1 0 A 0  -1-C   -1-D]
    [0 0  1 0 u v   w      t  ].
```

P has supports 123/145/246/378 and vanishes identically. Q has supports
126/257/367/458; R has supports 157/168/245/348. Each is the determinant of
four plane-normal rows, with the convention reconstructed by verify_exact.py.
All 70 original parent brackets, every original sign residence, and inherited
normalization sectors remain required. Neither B=D nor C=D is removed.

Let L=R_w=-[1468][3458] and U=Q_B=[1267][2458][2357]. Put
F_x=j Q_x-R_x for x in {A,C,D,u,v,w,t}. The prior corrected enlarged system is

    Q=R=0, all seven F_x=0, y L U=1.                    (C)

The inherited maximum argument puts every hypothetical compact component's
B-maximum into (C). Its nonzero-multiplier justification is unchanged.
But (C) omits other parent conditions, so it is not the genuine parent set.

## 2. A second enlarged emptiness target is conclusively refuted

The rational point

    (A,B,C,D,u,v,w,t,j,y)=(3,0,-1,-3,3,1,0,-2,1,1/1296)

solves every equation of (C). Here L=18 and U=72. A proposed polynomial identity
1=sum multipliers*generators for (C) would give 1=0 on substitution. Thus a
unit certificate for this enlarged system cannot exist even over Q.

This point is forbidden by the original domain: for example [1247]=-w=0 and
[1456]=-Bu=0. Exactly 15 of the 70 brackets vanish there. It is not a genuine
stationary parent and not a counterexample to 9DVL or to triple noncompactness.

A pure Python Fraction implementation independently computes the normal
determinants and first derivatives at this point. For each differentiated
coordinate, the relevant parent occurs in at most two normal rows, so each
polynomial has degree at most two in that coordinate. Central differences
with step one therefore give its derivative exactly, without a numerical
approximation. This second arithmetic implementation is not peer review.

## 3. Complete classification on one explicit forbidden boundary slice

Intersect (C) with B=w=0, C=-1, D=t-1. All solutions on this slice are exactly

    A=a, B=0, C=-1, D=-2a/(a-1), u=a, v=1, w=0,
    t=-(a+1)/(a-1), j=1,
    y=(a-1)^2/(16 a^4(a+1)),        a not in {0,1,-1}.   (F)

This classification is over any characteristic-zero field in which the
parameters lie, including R and C. It is only a classification of the displayed
boundary slice, not of all of (C).

Proof. On that slice exact substitution gives

    Q=A t v(A+u)(v-1),       R=t u(A+u)(v-1),
    L=-v(A+u)(t-1),          U=-t v(A+u)^2.

The inverse equation makes v, t, t-1 and A+u nonzero. Combining Q/v and R
forces v=1, since their sum is t(A+u)^2(v-1). The F_v equation now gives
u=A j. Also F_w=j Q_w-L and L!=0 force j!=0. Therefore A!=0, u!=0 and
j!=-1, using A+u=A(1+j).

After these substitutions, two of the remaining equations are

    F_D=-A j(A j t+A-t+1),
    F_w=-A(A j^2+A j t-t+1).

Subtracting their normalized expressions yields A(j^2-1)=0. Thus j=1,
because j=-1 was excluded. The remaining expression is
(A-1)t+(A+1)=0. A=1 would imply 2=0; hence t=-(A+1)/(A-1).
The restrictions above give a!=0,1,-1. Substitution proves all remaining
stationary equations and yields y in (F). Conversely, direct symbolic
substitution verifies every member of (F), with

    L=4a^2/(a-1),      U=4a^2(a+1)/(a-1).

There are 15 identically zero brackets on this entire curve. All members
are forbidden parents. No valid parent is removed when this slice is excluded.

## 4. Two exact low-degree consequences

Define polynomial vector fields, both having zero B component,

    V=AD d/dD+(At+u) d/dt,
    W=C d/dC+(w-u) d/dw.

Direct coefficient identities give V(Q)=A Q and W(R)=R. Let

    E=(2A R-V(R))/u,       T=W(Q).

E is a polynomial; E and T have total degree five. The exact certificate
identities are

    u E = AD F_D+(At+u) F_t-j A Q+2A R,
    j T = C F_C+(w-u) F_w+R.

On the original parent domain u!=0, because [1245]=-u. On the reciprocal
stationary set j!=0, because F_w=j Q_w-L and L is a parent unit. Thus E=T=0
are valid additional consequences at every genuine stationary point.
These identities do not alone exclude the stationary set or supply an escape.

## 5. Stop guessing an incomplete fixed list of units

Let I be generated by Q,R and the seven reciprocal equations F_x in
Q[A,B,C,D,u,v,w,t,j]. Let Delta be the product of the 61 distinct monic
nonconstant irreducible factors of the 70 brackets, authenticated by exact
factorization. The relevant localized ideal is I:Delta^infinity.

A sufficient positive algebraic certificate is

    product_r p_r^(n_r) = a_Q Q+a_R R+sum_x a_x F_x,     (U)

where every p_r is an authenticated parent factor and n_r is a nonnegative
integer. At any genuine parent the left side is nonzero and the right side
would be zero. Such an identity would exclude every genuine B-stationary point
and, by the inherited maximum reduction, every compact component of this triple.
A checked unit identity in the localization is an equivalent certificate form.

No existence of (U) is asserted. Even the fully localized stationary set may
have genuine points that are not maxima; if so, a different objective or an
extremum/escape argument will be needed. A complex solution is not automatically
a real parent. The stronger semidefinite vanishing statement is still separate.

The implementation only cancels a listed parent factor after exact polynomial
division and a multiplication check. Finite-field evaluations only reject
unlikely divisions. No division by B-D or C-D is permitted. Its bounded partial
basis is not a completed saturation, and no modular unit output is accepted as
a characteristic-zero certificate.

## 6. Acceptance and limits

verify_exact.py reconstructs the source, authenticates all 70 brackets and 61
factors, checks the entire rational family and every identity used above, and
rejects three corrupted mathematical cases. verify_fraction_witness.py checks
the explicit point by separate Fraction arithmetic and rejects two alterations.
The classification's logical case argument is the proof in Section 3.

The parent-localized modular run stopped with a growing queue. No rational
parent-unit product identity, complete localized basis, real stationary census,
source-orbit closure, original D3 proof, or independent referee acceptance has
been obtained. Original 9DVL ledger: **2/9**.

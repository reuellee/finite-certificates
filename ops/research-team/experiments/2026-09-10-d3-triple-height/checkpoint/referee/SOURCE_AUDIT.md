# Route-reset source audit

The original theorem quantifies over all realizable uniform rank-four parents on eight labels, all normalized components, and every internal proper pairwise-incomparable family of the stated size. Global inclusion-minimality in E(M) is not required. Current user authorization allows conventional deductive proofs and supersedes historical workflow stop rules. The frozen common-weak excision theorem is an input, not new credit.

The prospectus's blanket bad-locus equivalence is conditional on parent homology. The exact supported-duality sequence is

    H_(10-s)(X) -> H_c^(s-1)(B_S) -> H_(9-s)(F_S) -> H_(9-s)(X).

The inherited contraction-height theorem gives parent H_i=0 for i>=6. ATLAS_HELLY section 4.2 independently proves H_5(X)=0 by deletion into the contractible rank-four/seven-label space and one-bad escape. Consequently the bad-locus isomorphism is sound through s=4 without blanket contractibility.

| Route | Exact original target | Parent-independent or checked formulation | Current missing global step |
| --- | --- | --- | --- |
| D3 | H_6(F_S)=0 for every three-family | H_c^0(T)=0 and injectivity of the alternating pair H_c^1 restriction map, after coherent common-weak deletion if desired | Whole complement, attachments and infinity; both original summands remain |
| D4 | H_5(F_S)=0 for every four-family | H_c^3(B_S)=0, using independently proved parent H_6,H_5 bounds | Complete total complex, including surviving single-piece strata, multiple pieces and outgoing terms |
| D8 | H_1(F_S)=0 for every eight-family | Direct primal homology, or reduced Cech H^7(S^9 minus F_S) | Global coverage and filling; the single mask-6 loop is already filled and proves no universal statement |
| D9 | Reduced H_0(F_S)=0 for every nine-family | Direct common-support master-chamber graph connectivity | Globally labeled chamber coverage or an actual geometry theorem establishing connectivity |

For D8 the supported-duality reduction additionally needs parent H_2 and H_1. For D9, on one connected parent component, H_1(X)=0 suffices to identify H_c^8(B_S) with reduced H_0(F_S). Neither bound follows from the cited contraction-height argument. ATLAS_HELLY explicitly records that the exceptional 58 realizable classes rely on a blanket published contractibility statement with incomplete historical proof trace; this audit does not elevate that statement into a verified proof. The safe formula for all s is Alexander duality in S^9 with A_S=(S^9 minus X) union B_S. Omitting S^9 minus X is not legitimate without the parent bounds.

The historical route reports give no shortcut to an original diagonal. D4's 53 surviving support-shape orbits are only one column of a larger complex. D8's exact disk fills one genuinely admissible loop, without global family or parent coverage. D9's ordinary radial normal-link model fails at singular parent-boundary strata; this retires that proposed local model, not connectivity. The master-chamber theorem itself is a valid direct-primal reduction that does not require parent contractibility.

`PARENT_H4_CANDIDATE.md` proposes one additional parent bound by a seven-label specialization of the inherited two-pencil argument. It is authored here and is pending independent review by another agent. Even if accepted it would only validate D5's bad-locus formulation; it would not prove D5 or displace the whole-obligation priority.

# Independent deductive acceptance: two parent-line factors

Reviewed proof: `proof/TWO_PARENT_LINE_FACTORS.md`.
SHA256: `85c449f0d814872c92a9b07f5962a9af6fdaa6c0d18b8a779c86610ba5d05b44`.

Verdict: ACCEPT, with its stated all-parameter scope. For arbitrary original
uniform rank4 parents on8, arbitrary normalized parent components, and
three distinct primitive factors, if at least two factors have global kind36
or38, every component of their full triple common zero set is noncompact.
No finite fixture or orbit census is required for this conventional proof.

The relevant points were reconstructed rather than accepted from producer
code:

1. In ordinary37/38, the first two parent planes intersect in exactly their
   shared parent line. Uniformity gives independence of their four defining
   parents. The concurrence is unique by the inherited ordinary circuit
   identity. It is neither parent endpoint, because a remaining selected
   plane excludes both endpoints and its corresponding parent brackets are
   nonzero.
2. Consequently q=Ya+tYb gives a unique continuous parameter t in R* for
   the actual fixed positive parent representatives; the analogous u exists
   for R. No projected-parent independence is needed for these line
   parameters. Concurrence collisions and cancellations of the projected
   vector are automatically retained.
3. Each remaining incidence equation is exactly
   det(Yi,Yj,Yk,Ya)+t*det(Yi,Yj,Yk,Yb)=0. At fixed projected parents each
   bracket is affine jointly in the four free heights. Dependence of the
   concurrence height on those same heights introduces no quadratic term,
   since every determinant term using the vertical vector twice vanishes.
4. The resulting four equations in four heights have an open six-dimensional
   parameter base. The full strict height residence is convex and selects
   a whole connected fiber of the fixed original parent component.
5. A feasible rank-drop fiber is connected, noncompact and closed in the
   incidence space. It contradicts compactness of any component containing
   it. Off rank drops the incidence is a graph over an open subset of R6,
   whose components cannot be compact. This includes all coefficient ranks.

The ordinary-occurrence identities and the all-six-factor anchor model are
explicit inherited/reviewed dependencies. The anchor model itself received
a separate proof-track review in `proof/ANCHOR_AUDIT_REVIEW.md`.

Coverage is the stated global subclass, with arbitrary third factor.
No numerical new triple-orbit count is inferred here. In particular this
does not establish the universal triple endpoint or original injectivity.
The original score remains2/9 unless another complete original proof is
independently accepted.

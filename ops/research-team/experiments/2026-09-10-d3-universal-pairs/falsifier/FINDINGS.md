# Actual conic-fiber specialization challenge

**No original diagonal-three obligation is closed or refuted.**

## Actual fiber component counts

The hard factor pair has occurrence quartets

`P=123/145/246/378`, `Q=567/158/268/347`.

Fix retained parent coordinates `(a,b,d,e,f)=(7,-8,-17/4,2,-3)` and vary c. The first wall fixes the forgotten parent column by `g=-5i/4-8`. The second wall is the conic

```
28c²h² + 35c²hi + 168c²h - 70c²i - 448c²
 - 37ch² + 335chi + 1053ch + 350ci² + 2430ci + 4192c
 - 663h² + 1565hi + 2472h + 250i² + 19120i + 62208 = 0.
```

All parent signs are fixed to the exact inherited anchor `(c,h,i)=(-4,-2116/67,-8)`. The component counts of the **full** conic residence fiber in that parent sign chamber are:

| c | Components |
|---|---:|
| -401/100 | 1 |
| -39999/10000 | 2 |
| -3999/1000 | 2 |
| -1999/500 | 2 |
| -799/200 | 2 |
| -399/100 | 1 |

Every component is an open interval. Therefore the degree-one compact-support fiber group has rank one or two as shown. The map's degree-one shriek direct-image sheaf cannot be locally constant throughout this actual family. This challenges the conic route's possible locally constant-direct-image shortcut, not the original theorem.

The calculation uses the rational conic point `(h,i)=(2,-3)`, corresponding to duplicate parent columns 8=7 and hence excluded from the original parent space. A line through this point has equation `h=2+t(i+3)`. Its second intersection rationally parameterizes the nonsingular projective conic by RP1. All 70 strict parent brackets become rational sign conditions in t with numerator and denominator degree at most two. Their real roots partition RP1; exact rational tests on every complementary interval determine the entire residence set. The parameter value at infinity is checked separately. The conic is nonsingular at every listed c. Root sorting is proposed numerically but verified by exact algebraic inequalities before acceptance.

At c=-401/100 the residence interval ends at the two different parent walls `[3468]` (i=-12) and `[3458]` (i=-36/5). At c=-399/100 both ends are on `[3468]`, with a turning conic arc in between. The original prover independently identified the intermediate `[3458]` tangency and is checking its specialization maps.

## Scope and the missing global step

The artifact establishes counts in the fixed parent sign chamber. A simultaneous path through the merge must be retained to assert that the secondary fiber component belongs to the same normalized parent component as the original anchor; we do not silently invoke blanket parent contractibility. The original prover was explicitly notified of this requirement. The retained seven-column parent signs remain fixed throughout the small c segment, since their brackets are affine in c and have the correct strict signs at both endpoints.

Two intervals specializing to one interval do **not** by themselves supply a nonzero compactly supported global section of `R1 f!`. The secondary interval disappears into the excluded parent-unit component at c=-4; that birth specialization can prevent the difference class from extending. Likewise this is a factor-pair fiber calculation, not an original pair-bad Hc1 class or an injectivity counterexample. A correct next argument must calculate the specialization maps and their global support, retaining both the parent-boundary tangency and the c=-4 degeneration.

Producer and exact data: `conic_components.py`, `CONIC_COMPONENTS.json`. Independent arithmetic review was requested from the referee for at least c=-3999/1000 and c=-399/100. The original prover derives the conic/tangencies independently.

## Independent review of the parent H4 corollary

A separate bounded deductive challenge accepts the scoped candidate in `referee/PARENT_H4_CANDIDATE.md`, binding SHA256 `4b2c956617826560a0d5b95d248300d3ef94b6bf17a7409f85694725dd7c9af5`. The audit is `PARENT_H4_INDEPENDENT_AUDIT.json`.

Exactly five frozen uniform columns provide the required projective frame for two moving degree-two labels. The inherited two-pencil argument explicitly handles shared support planes; its full open two-dimensional fiber components have interval sections. Positive normal rescaling carries the entire positive coordinate face and its orientation line to the retained normal-ray quotient. The vertex and edge incidence counts yield the required two-dimensional and positive-dimensional motions. The deletion-space contractibility input is explicitly the inherited rank-three-on-seven result, rather than blanket rank-four-on-eight contractibility. No load-bearing defect was found. This proves an auxiliary parent homology bound and closes no original diagonal.

# Cycle: D3 global semialgebraic diagram replacement gate 1

Canonical predecessor revision: `0b8141223193c1ea2a1b4fce8e862466749f8b6b`

Canonical predecessor tree: `faad8f9e78bd54435ee6212535198a08c0e3fe76`

Opening theorem ledger: `2/9`; only diagonals 1 and 2 are proved.  The
canonical V9 state remains immutable and `STOPPED`.

Opening action: **`PIVOT` to one bounded qualification-and-construction
gate** for
`D3_B0_GLOBAL_SEMIALGEBRAIC_DIAGRAM_SIMPLICIAL_REPLACEMENT_GATE1`.
The cycle is open, but theorem construction is conditional on the
qualification gate below.  Qualification artifacts carry zero theorem
credit.

## Authority and execution environment

The user's 2026-09-02 messages are the new explicit authority required by
V9.  They authorize acquiring useful resources and starting this cycle.
Human review and compute beyond this laptop are unavailable, except that the
user expressly authorizes a useful ephemeral cloud environment.  Therefore:

- the saved local project is the canonical working home;
- bundles and manifests are written under
  `E:\Projects\9DVL Research\outputs` and mirrored by native filesystem copy
  to `G:\My Drive\Projects\research-backups`;
- GitHub remains read-only; no push, pull request, CI trigger, or merge is
  authorized;
- network acquisition and connected exact-computation services are allowed
  for this research, but no secret or unrelated private file may be sent;
- an ephemeral Google Compute Engine worker is allowed only for one
  predeclared, hash-pinned job after the local qualification accepts its
  executable and verifier;
- that worker is limited to `n2-highcpu-16`, `us-central1-a`, 16 vCPU,
  16 GiB RAM, a 30 GiB boot disk, four elapsed hours, and **USD 5 total** for
  this cycle; the exact cycle prefix is
  `d3-srep-gate1-20260902-`;
- the worker and attached disk must be deleted after result collection or on
  failure.  Stopping is not cleanup.  Existing instances, especially
  `claude-control`, are out of scope and must not be changed;
- cloud use is `AVAILABLE_NOT_ACTIVATED` until a job manifest names the exact
  input tree, command, verifier, output hashes, timeout, and cleanup action.

The numeric cloud ceiling is conservative relative to the public
`us-central1` on-demand price of `n2-highcpu-16`; it is an authorization cap,
not a promise that a billing budget enforces shutdown.  The launcher and
post-run inventory check are acceptance dependencies.

## Mandatory opening strategy tournament

V9 closed with seven load-bearing obligations, pair residual and coverage
both `UNKNOWN`, and streaks of six same-blocker and nine zero-ledger cycles.
It prohibits repeating the mixed-carrier feasibility round, local chart or
carrier refinement, unattached triple-atlas growth, and factor/residual
sampling.  The new route changes the source object: it begins with one finite
closed-formula diagram over the entire compact quantified domain and asks for
a low-degree simplicial replacement of that diagram.

| Route | Ledger leverage | Quantifier readiness | Coverage burden | Terminality | Structural compression | Independent replay | Resource / information | Stagnation risk | Disposition |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| global closed-formula diagram plus homological-2 simplicial replacement | 5 | 3 | 2 | 5 | 5 | 4 | 4 | 2 | **selected** |
| frontier-condition CAD / direct master closure | 5 | 2 | 1 | 4 | 3 | 3 | 2 | 5 | reject: continuation of the stopped direct-master route |
| universal mixed-carrier theorem A | 4 | 1 | 1 | 3 | 5 | 3 | 3 | 5 | reject: no exhaustive `L_source` or arbitrary-chain rule |
| triple component atlas or residual sampling | 3 | 2 | 1 | 4 | 2 | 4 | 2 | 5 | reject: independent C branch and expressly prohibited continuation |
| exact counterexample / route retirement | 4 | 3 | 4 | 5 | 4 | 5 | 5 | 1 | retain as hostile-canary lane |

Scores are `1..5`, with `5` favorable except that stagnation risk is literal
and `1` is favorable.  Selection does not assert that an implementation of
the published replacement algorithm exists locally.

## Mathematical target and global quantifiers

For every one of the `2,604` realizable unlabelled rank-four parent types,
every required labelled frame and every realization component, and every
ordered proper pairwise-incomparable triple of proper extension signatures,
the producer must define one canonical compact closed semialgebraic diagram.
It must contain:

1. the projective compactification `Xbar_M` of the normalized parent
   realization space and its genuine parent-infinity subset `I_M`;
2. the three bad loci `B_0`, `B_1`, `B_2`, defined from the original
   determinant/Gordan source formulas rather than sampled cells;
3. every pair, triple, union, and subset inclusion used by the block-mass
   filtration; and
4. the exclusive-pair relative terms, for example
   `(B_0 intersect B_1, (B_0 intersect B_1) intersect (B_2 union I_M))`.

Discrete parent, frame, signature, active-block, support, orientation,
parent-rank, witness-rank, zero-weight, chart, and true-infinity tags remain
distinct.  Formula closure may not be obtained by blindly replacing strict
inequalities with weak ones.  The formula tuple must cover all realization
components without choosing a sample point from each.

The selected structural theorem is the Basu--Karisani low-degree simplicial
replacement for closed and bounded semialgebraic formula tuples, used at
`ell = 2`; the maps/zigzags companion is permitted only when an inclusion
diagram cannot express a required comparison.  The existing proper compact
Gordan-fiber quotient may be used before the pair differential.  No receiver,
root, row-2599, support-atlas, or master-CAD continuation is part of this
cycle.

## Two-stage gate

### Q0: executable qualification

Qualification starts immediately and has no theorem credit.  From polynomial
formulas, not hand-authored output complexes, an executable path must:

1. distinguish the M3 unfilled and filled compact pairs, which share their
   accepted exit graph and one-skeleton but have relative `H_1 = 1` and `0`;
2. detect the M2 tangential first-exit union as nonclosed and reject its
   artificial relative boundary;
3. reproduce the proved single-bad low-degree vanishing control; and
4. compile the complete global tagged formula **schema**, without solving its
   topology, and report exact `(N, s, d, k)` parameters and an output/resource
   forecast.

The trace must expose exact polynomial ASTs, ordered infinitesimals or their
absence, exact algebraic-number encodings, generated cover/poset data,
simplices and subcomplex labels.  A closed-source theorem paper, a CAD plot,
a manually supplied complex, or a solver answer without an independently
checkable trace fails qualification.

Q0 ceiling: eight laptop-hours, 32 GiB RAM, 25 GiB scratch, one initial
producer handoff and one verifier-directed repair.  The optional cloud worker
cannot be used to evade a missing executable or trace.  If no qualifying
backend exists, record `NULL_NO_EXECUTABLE_REPLACEMENT_BACKEND`, preserve the
schema/compiler work, and close `STALLED / STOP`.

### Q1: conditional full-scope construction

Q1 opens only after an independent verifier accepts every Q0 item and the
global forecast fits the remaining ceiling.  Its entire compute ceiling is
the unused part of Q0 plus the one cloud worker authorized above; no larger
cluster, human referee, third pass, or ceiling extension exists.

The producer must emit one full-scope finite simplicial pair diagram through
dimension three and a homological-2 comparison to the actual compactified
bad-union diagram.  A separately implemented verifier must regenerate the
scope denominator, replay formula/subset facts using an independent exact
real-closed-field backend, verify every face and subcomplex, recompute signed
relative boundaries and `d^2 = 0`, and recompute exact ranks.  Partial parent,
signature, chart, or component coverage is `STALLED`, never a residual
decrease.

## Certificate and falsification contract

The accepted package must contain exact source digests; a complete enumerable
scope manifest and denominator; canonical integer-polynomial ASTs; stable
simplex IDs; formula-indexed subcomplexes and maps; the replacement trace;
relative boundary matrices through `C_3`; exact rank calculations; a resource
log; and an independent replay with no producer acceptance-code import.

At minimum the verifier must reject: an omitted parent, signature triple, or
block permutation; an improper/comparable triple; separate per-block
complexes; a deleted triple locus in the exclusive-pair relative subset;
artificial boundary relabelled as infinity; swapped parent/witness ranks; a
deleted zero-weight face; a broken inclusion or cocycle; a missing face or
duplicate simplex ID; flipped incidence; corrupt algebraic-root or
infinitesimal data; M3 filled/unfilled conflation; and M2 nonclosed pointwise
assembly.

## Mandatory solution-convergence gate

Opening proof-distance vector:

```text
(2/9, 1, {diag3_pair_hc1, diag3_triple_hc0}, 7,
 UNKNOWN, UNKNOWN, 6, 9)
```

The last three comparable closing vectors are:

```text
global-master-closure audit: (2/9, 1, {...}, 7, UNKNOWN, UNKNOWN, 4, 7)
joined-Gordan tournament:    (2/9, 1, {...}, 7, UNKNOWN, UNKNOWN, 5, 8)
mixed-carrier feasibility:   (2/9, 1, {...}, 7, UNKNOWN, UNKNOWN, 6, 9)
```

Convergence hypothesis: a formula-first diagram plus a certified
homological-2 replacement can simultaneously attach the global source,
strict closure, true infinity, and the source comparison without enumerating
a direct master CAD.

Minimum acceptable decrease: an independently verified **full-scope** finite
simplicial pair diagram and homological-2 comparison must close at least the
`global_gluing`/B-root attachment edge and reduce the open load-bearing count
from `7` to at most `6`.  The ledger remains `2/9` unless both independent D3
invariants are actually proved.  Q0, a schema, a theorem citation, an output
forecast, a sample, or a partial diagram earns zero decrease.

Mid-cycle checkpoint: immediately after Q0, or earlier upon a failed canary,
missing trace, or forecast that cannot fit the remaining laptop/cloud ceiling.
Continue to Q1 iff every Q0 item passes independently and complete `N/N`
construction and replay remain plausible under the fixed ceiling.  Otherwise
freeze the handoff and stop; resources may not be enlarged.

Trajectory is `CONVERGING` only if the minimum decrease is independently
accepted.  Reusable Q0 knowledge without that decrease is `INFORMATIONAL` or
`STALLED` and cannot justify same-route continuation.  A sound counterexample
retires only the precisely falsified encoding or theorem route.

## Roles and boundaries

1. **Formula/compiler producer:** owns the closed global schema and Q0
   executable trace; cannot accept its own output.
2. **Topology falsifier:** owns polynomial M2/M3/control fixtures and hostile
   mutations; cannot define producer success.
3. **Independent verifier:** uses separate parsing, exact arithmetic and RCF
   queries; cannot import producer acceptance functions.
4. **Coordinator:** pins the opening, authorizes Q1 only after the independent
   Q0 verdict, integrates evidence, performs cloud cleanup checks, and closes
   the cycle.  With no human reviewer available, no result may be described as
   human-reviewed.

No role may edit canonical theorem, ledger, or V9 state surfaces.  No role may
push or merge.  Missing independence fails closed.

## Handoffs and stop rules

- `POSITIVE`: full `N/N` diagram and independently verified comparison meet
  the strict decrease.  It closes only the named B-root edge unless further
  invariants are separately proved.
- `NEGATIVE`: an exact full-scope counterexample falsifies one named necessary
  claim; freeze it and scope retirement narrowly.
- `NULL`: no executable trace, incomplete schema, impossible resource
  forecast, or another first missing prerequisite is pinned.
- `TIMEOUT`: preserve hashes, completed fractions and the first unfinished
  deterministic item; claim no decrease.

Stop immediately on pin drift; local-to-global promotion; missing exact
denominator; sampled coverage; chart/artificial boundary used as infinity;
parent/witness rank conflation; producer self-certification; a failed hostile
canary; cloud job without a preflight manifest; any instance name outside the
cycle prefix; discovery touching an existing cloud resource; failure to
delete the cycle worker and disk; USD 5 or four-hour ceiling exhaustion; or a
forecast that cannot complete and replay `N/N` inside the remaining ceiling.


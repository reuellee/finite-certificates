# Publication authorization update

Date: 2026-09-04.

After the governed opening was created, the user explicitly instructed:
"update github and google drive".

This supersedes the opening's read-only publication restriction only for the
following bounded actions:

1. commit the new cycle artifacts on the current local branch
   `research/local-9dvl-post-mixed-100-stop-v11-20260903`;
2. push that branch to the configured GitHub remote named `github`;
3. create a complete-history Git recovery bundle and SHA-256 manifest under
   `E:\Projects\9DVL Research\outputs`;
4. upload the bundle and manifest to the existing Google Drive folder
   `Projects/research-backups`, preserving its organization; and
5. verify GitHub by remote ref and Google Drive by returned metadata, byte
   length, and the uploaded manifest.

The instruction does not authorize a pull request, merge, force-push, branch
deletion, history rewrite, sharing change, author contact, paid compute,
canonical theorem-state edit, or theorem-ledger promotion.

The opening state remains unchanged as the historical record of conditions at
cycle start.  This document records the later explicit publication authority.

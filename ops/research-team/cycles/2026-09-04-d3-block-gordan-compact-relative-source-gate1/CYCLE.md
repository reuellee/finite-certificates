# D3 block-Gordan compact relative source gate 1

Canonical theorem state: `CANONICAL_RESEARCH_STATE_V11.json`.

Canonical base revision: `5587d2da0e6e7a03a716f7e25ddbd98a82f4af28`.

Opening theorem ledger: `2/9`.

## Authority and reconciliation

On 2026-09-04 the user explicitly instructed the project to continue the
recommended 9DVL plan and to work as a serious human research team would,
with the additional objective of pushing the limits of AI research.  This is
authority for one new bounded local theorem-input cycle.  It is not authority
for author contact, paid or external compute, GitHub writes, CI, merges,
canonical-state edits, or theorem-ledger promotion.

Canonical V11 is `STOPPED / NONE`.  Its two post-V10 cycles closed
`NULL / STALLED / STOP`; the current proof-distance vector is
`(2/9,1,{pair,triple},7,UNKNOWN,UNKNOWN,9,12)`.  This cycle is not an automatic
continuation of either stopped route.  Its genuinely new input is the full
source-derived block-Gordan total space `Gamma_S`, rather than the reduced
lower-chain interface used by the stopped mixed-`(1,0,0)` gate.

## Mandatory opening strategy evaluation

Higher scores are favorable except Stagnation risk, where lower is favorable.

| route | Ledger leverage | Quantifier readiness | Coverage burden | Terminality | Structural compression | Independent verification | Resource / information | Stagnation risk | decision |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| compact relative block-Gordan source theorem or exact obstruction | 5 | 5 | 4 | 5 | 5 | 5 | 5 | 2 | **PIVOT / SELECT** |
| guess a global mass-transfer potential immediately | 5 | 2 | 2 | 4 | 5 | 4 | 3 | 5 | defer: the finite relative domain and boundary must first be certified |
| repeat lower-interface mixed `(1,0,0)` carrier | 4 | 1 | 2 | 4 | 4 | 5 | 2 | 5 | reject: V11 forbids repetition without a geometric source |
| resume SREP implementation search | 4 | 2 | 2 | 4 | 4 | 4 | 2 | 5 | reject: predecessor lacked backend, denominator, and compact formulas |
| triple saturation, pair CAD, or atlas/residual growth | 3 | 2 | 1 | 3 | 2 | 3 | 2 | 5 | reject: prohibited repeats with no new component-complete input |

Opening verdict: `PIVOT_SELECT_D3_BLOCK_GORDAN_COMPACT_RELATIVE_SOURCE`.

The selected route is objectively preferred because it attacks four
load-bearing structural obligations with one construction, is stated over all
2,604 parent classes and every required three-signature subdiagram, and has a
short exact falsification surface: frame normalization, compact closure,
relative infinity, simultaneous triangulation, or filtration-preserving
comparison.  It does not assume the desired rank or escape conclusion.

## Bounded target

Target:

```text
D3_BLOCK_GORDAN_COMPACT_RELATIVE_SOURCE_THEOREM_OR_OBSTRUCTION_GATE1
```

For every realizable uniform rank-four parent oriented matroid `M` on eight
labels and every ordered proper pairwise-incomparable signature triple `S`,
prove the following source theorem, simultaneously for all nonempty
`R subseteq S`.

1. A fixed five-label projective-frame normalization realizes every point of
   the full normalized parent space `X_M` as a semialgebraic subset of
   `(relint Delta^3)^3`, without losing components.
2. The exact closure `Xbar_M=cl(X_M)` is a compact semialgebraic set, and
   `I_M=Xbar_M\X_M` is exactly genuine parent infinity: a coordinate or parent
   bracket has vanished.  No chart seam, residual wall, witness-rank drop, or
   artificial box boundary is placed in `I_M`.
3. Extending the polynomial block-Gordan equations over `Xbar_M` defines a
   compact pair `(Gammabar_R,Gammabar_R,I)` with interior `Gamma_R`; zero
   padding realizes every inclusion `R subseteq R'` as an inclusion of pairs.
4. One finite triangulation is compatible with all seven nonempty signature
   subsets, the relative boundary, block-mass filtration, witness-coordinate
   faces, parent signs, residual walls, and declared rank strata.  It yields
   globally stable labels, exact strict closures, integral oriented incidence,
   and a true-infinity subcomplex.
5. The relative complex computes `H_c^*(Gamma_R)` and hence `H_c^*(B_R)`;
   its block-mass filtration recovers compact-support Mayer--Vietoris through
   total degree two, with exact integer boundary matrices available for later
   middle-rank replay.

The positive endpoint is the full five-clause theorem with all quantifiers and
an independently accepted proof.  The negative endpoint is an exact
admissible parent/signature instance or theorem-level obstruction disproving
one clause.  A missing software backend, inability to materialize the enormous
triangulation, or failure of one coordinate presentation is not a negative
mathematical result.

## Mandatory solution-convergence evaluation

Proof-distance vector fields are theorem ledger and Score deficit; named
diagonal invariant; Open load-bearing obligations; Certified exhaustive residual;
Global coverage; Same-blocker streak; and Zero-ledger streak.

Opening Proof-distance vector:

```text
(2/9, 1, {diag3_pair_hc1, diag3_triple_hc0}, 7,
 UNKNOWN, UNKNOWN, 9, 12)
```

Selected-route residual:

```text
{global_gluing, extension_labels, strict_closure, relative_infinity} = 4/4 open
```

Convergence hypothesis: the compact relative source theorem turns the full
block-Gordan geometry into one finite labeled relative chain object.  If all
five clauses are proved, the four selected load-bearing obligations close and
the canonical open-obligation count decreases from `7` to `3`, while the
ledger remains `2/9`.  The next authorized research question would then be
only the actual global acyclicity/middle-rank/escape problem on this source.

Minimum acceptable decrease: independently prove the complete theorem and
close all four selected obligations, or prove an exact full-scope obstruction
that retires this compact-relative construction.  Literature plausibility,
an existence slogan, a row-2599 atlas, an unverified proof memo, or a
homological replacement that loses labels or relative infinity gives no
decrease.

Mid-cycle convergence check: after the source architect and adversarial
topologist freeze their first handoffs, or after three elapsed hours,
whichever comes first.  If the frame quotient, closure pair, compatible finite
family, or compact-support comparison remains unstated at full quantifier,
freeze the first missing edge and stop.

Automatic strategy-reset rule: `NULL`, timeout, a merely abstract
triangulation with no label/boundary fidelity, or unchanged selected residual
forbids same-route `CONTINUE` and requires `PIVOT` or `STOP`.  An exact
obstruction requires `RETIRE`.  A positive result authorizes no diagonal claim
and no automatic successor; the global potential/middle-rank gate requires a
separate opening.

## Obligation graph

The selected package attacks, in dependency order, `global_gluing`,
`extension_labels`, `strict_closure`, and `relative_infinity`.  It does not
close `middle_rank_replay`, `diag3_pair_hc1`, or `diag3_triple_hc0`.  The latter
two remain independent, and only both can prove diagonal three.

## Canonical input accounting

`OPENING_STATE.json` pins V11, the full block-Gordan audit, all-codimension
coherence and mass-transfer results, the mutation-map no-go, the theorem
prospectus, the stopped mixed-carrier report, protocol, and the two locally
stored Basu--Karisani papers.  Pair residual and coverage remain
`UNKNOWN / UNKNOWN`; triple source accounting remains
`77,940,147 + 1,162,302 = 79,102,449` and is not a component denominator.

## Concurrency and non-overlap

The source architect and adversarial topologist are logically independent and
must freeze separate handoffs before verification.  The independent verifier
may use neither producer's acceptance code.  The closing referee reviews only
a frozen integrated candidate.  Historical worktrees are read-only evidence.

No parallel worker was started by this opening itself; the roles are a
preregistered separation-of-concerns contract for execution of the cycle.

## Roles

- Source architect: prove the compact relative block-Gordan construction and
  give the finite-family triangulation and compact-support comparison.
- Adversarial topologist: try to break global frame normalization, exact
  closure, relative-boundary semantics, subdiagram naturality, and filtration
  compatibility; a software limitation is not a mathematical obstruction.
- Independent verifier: reconstruct the proof from pinned sources, check every
  quantifier, and run hostile mutations against the opening and closing data.
- Closing referee: enforce proof-distance accounting, exact nonconsequences,
  and the single `CONTINUE/PIVOT/RETIRE/STOP` closing classification.

## Resource ceiling

- total governed wall clock after opening: `6` hours;
- source architect and falsifier checkpoint: `3` hours;
- independent verification: `90` minutes;
- closing review: `60` minutes, with unused time as buffer;
- peak resident memory: `16 GiB` per role;
- new scratch: `2 GiB` total;
- external/cloud workers and paid spend: `0`;
- no bulk triangulation, CAD, Groebner, RUR, or saturation execution.

Stop immediately on source drift, loss of a parent component in normalization,
weak-inequality substitution for exact closure without proof, an internal seam
or rank stratum labeled infinity, a triangulation that does not make all
signature subsets and filtrations simultaneous subcomplexes, producer
self-acceptance, or a ledger/diagonal promotion.

## Scope prohibitions

- no author contact, paid/external compute, or external solver;
- no GitHub write, push, pull request, CI trigger, or merge;
- no canonical-state or theorem-ledger edit;
- no raw saturation, CAD, SREP execution, atlas growth, census growth,
  residual sampling, or factor-19069/D9 continuation;
- no claim that existence of a finite source proves its middle-rank exactness;
- no claim that a Basu--Karisani homological replacement automatically
  preserves integral incidence, strict closure, labels, or genuine infinity;
- no claim of independent acceptance from this opening draft.

Read-only primary-source research is allowed only to check theorem hypotheses
and must be pinned; it cannot replace a proof of the five clauses.

## Closing requirements

The cycle report must record exact base and frozen revisions, all source pins,
producer handoffs, the five-clause verdict, independent hostile replay,
opening/midpoint/closing vectors, exact obligation delta, resources,
nonconsequences, and exactly one of `CONTINUE`, `PIVOT`, `RETIRE`, or `STOP`.

## Storage and publication authority

The saved local Codex project `E:\Projects\9DVL Research` is the canonical
working home. Bundles and manifests go under `E:\Projects\9DVL Research\outputs`.
A native-filesystem recovery mirror to `G:\My Drive\Projects\research-backups`
is optional, byte-length and SHA-256 verified, and nonblocking. Never invoke
the Google Drive connector and never block on connector approval. GitHub is
read-only: do not push, publish branches, open or update pull requests, trigger
or rerun CI, or merge. This opening authorization does not permit secrets,
unrelated private files, paid/external compute, force-push, history deletion,
or irreversible external action.

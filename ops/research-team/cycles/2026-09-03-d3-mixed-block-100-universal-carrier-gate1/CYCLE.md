# D3 mixed-block `(1,0,0)` universal carrier gate 1

Canonical theorem state: `CANONICAL_RESEARCH_STATE_V10.json`.

Canonical base revision: `fb667bfe33ef9e945a82e9a23b615e67f5f39c0f`.

Opening theorem ledger: `2/9`.

## Authority and reconciliation

The user explicitly authorized execution of the recommended next research
cycle on 2026-09-03.  That authorization selects this bounded theorem-or-
obstruction gate.  It does not authorize author contact, network research,
external or paid compute, GitHub writes, CI, merges, canonical-ledger edits,
or construction of the global pair atlas.

The base is the independently closed D3 critical-saturation cycle.  Its
accepted result is `Q0 NULL / Q1 DENIED / STALLED / STOP / NONE`; it did not
supersede the theorem facts in canonical V10.  This opening therefore pins
both V10 and the saturation close.  Historical lane worktrees are frozen
evidence, not live concurrent cycles.  No unfinished successor exists at
opening.

## Mandatory opening strategy evaluation

Higher scores are favorable except stagnation risk, where lower is favorable.

| route | Ledger leverage | Quantifier readiness | Coverage burden | Terminality | Structural compression | Independent verification | Resource / information | Stagnation risk | decision |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| conditional universal mixed `(1,0,0)` carrier plus full specialization coherence, or an exact universal obstruction | 5 | 4 | 4 | 5 | 5 | 5 | 5 | 2 | **PIVOT / SELECT** |
| repair and rerun component-decorated saturation | 4 | 2 | 2 | 4 | 3 | 4 | 2 | 5 | reject: predecessor Q0 null; component witnesses and five endpoint compilers absent |
| resume global SREP / pair replacement | 5 | 1 | 1 | 4 | 5 | 4 | 2 | 5 | reject: backend, denominator, and closed compactification formulas absent |
| exact counterexample to the selected universal carrier | 4 | 4 | 4 | 5 | 5 | 5 | 5 | 1 | required adversarial endpoint inside selected gate |
| D9 simultaneous insertion or sign geodesy | 2 | 2 | 2 | 4 | 4 | 4 | 3 | 4 | defer: lower ledger leverage and prior null/timeout |

Opening verdict: `PIVOT_SELECT_D3_MIXED_100_UNIVERSAL_CARRIER_OR_OBSTRUCTION`.
This is not a continuation of the stopped atlas, SREP, saturation, sampling,
or singleton-root filler routes.

## Bounded target

Target:

```text
D3_MIXED_BLOCK_1_0_0_UNIVERSAL_CARRIER_OR_OBSTRUCTION_GATE1
```

The target is conditional on any finite `L_source` satisfying the declared
interface in the pinned theorem memo.  It quantifies over every realizable
uniform rank-four parent on eight labels, normalized realization component,
required chart, source-derived proper antichain of pairwise-incomparable
proper extension signatures, admissible witness-support stratum, primitive
integral kernel class, labeled active-block placement for `1 <= r <= 3`, and
active-block permutation.

The positive statement requires:

1. a finite genuinely mixed chain family of type `(1,0,0)` for every relevant
   primitive integral kernel class, with integral orientations and the exact
   required boundary;
2. no filler supported on one fixed bad block at a time;
3. naturality under support loss, zero weights, chart transitions and
   monodromy, parent-boundary specialization, parent-rank and witness-rank
   strata, residual walls, and active-block permutations; and
4. equality of induced chain maps for every composable face flag, not merely
   codimension-one squares, while artificial boundaries remain outside true
   parent infinity.

The negative statement must exhibit one admissible finite `L_source`, one
primitive integral class, and an exact obstruction proving that no chain
family satisfying all four requirements exists.  Failure of a chosen formula
or ansatz is not negative evidence at this quantifier.

## Mandatory solution-convergence evaluation

Proof-distance vector fields are recorded as follows: theorem ledger and
Score deficit; the selected theorem invariant; Open load-bearing obligations;
Certified exhaustive residual; Global coverage; Same-blocker streak; and
Zero-ledger streak.

Canonical opening vector:

```text
(2/9, 1, {diag3_pair_hc1, diag3_triple_hc0}, 7,
 UNKNOWN, UNKNOWN, 8, 11)
```

Selected-route theorem residual:

```text
{O3_universal_mixed_chain, O4_arbitrary_flag_coherence} = 2/2 open
```

The last three comparable closing vectors are:

| cycle | closing vector |
| --- | --- |
| D3 mixed-carrier theorem feasibility | `(2/9,1,{pair,triple},7,UNKNOWN,UNKNOWN,6,9)` |
| D3 global semialgebraic replacement | `(2/9,1,{pair,triple},7,UNKNOWN,UNKNOWN,7,10)` |
| D3 critical saturation | `(2/9,1,{pair,triple},7,UNKNOWN,UNKNOWN,8,11)` |

Convergence hypothesis: a universal proof of both O3 and O4 supplies the new
theorem-capable input absent from the prior feasibility audit and reduces the
selected-route theorem residual from `2/2` to `0/2` without weakening the
global quantifiers.  An exact universal obstruction retires this structural
route and also returns terminal information.  Either endpoint is independently
falsifiable and strictly smaller than the opening problem.

Minimum acceptable decrease: independently prove both O3 and O4, or prove an
exact full-quantifier obstruction to their conjunction.  A local filler,
finite example, new memo, formal-shape count, sampled success, incomplete seam
matrix, or failure of one construction does not decrease proof distance.

Mid-cycle convergence check: immediately after the first frozen constructor and
falsifier handoffs, or at two elapsed hours, whichever comes first.  If a
universal proof or exact universal obstruction is no longer reachable inside
the remaining ceiling, freeze `NULL`, classify `STALLED`, and stop.

The canonical ledger remains `2/9` even after a positive route theorem.  The
formal ten-shape taxonomy and its provisional `3/10` result are not global or
end-to-end D3 denominators and may not be promoted.

Automatic strategy-reset rule: a null, timeout, incomplete-coherence result,
or unchanged selected-route residual forbids same-route `CONTINUE` and
requires `PIVOT` or `STOP`; an exact obstruction requires `RETIRE` for the
quantified mixed-carrier route.

## Obligation graph

The seven canonical D3 obligations remain global gluing, complete extension
labels, strict closure, genuine relative infinity, global middle-rank replay,
`diag3_pair_hc1`, and `diag3_triple_hc0`.  This gate attacks the two selected
route-interface obligations `O3_universal_mixed_chain` and
`O4_arbitrary_flag_coherence`.  A positive result supplies an input to later
O1/O2 construction; it bypasses no canonical obligation.

## Canonical input accounting

`OPENING_STATE.json` pins V10, the independently closed saturation report and
manifest, the prior mixed-carrier theorem and naturality audits, and the
theorem-reset falsifier.  It preserves pair accounting `UNKNOWN / UNKNOWN`
and triple accounting `77,940,147 + 1,162,302 = 79,102,449`.

## Concurrency and non-overlap

Constructor and falsifier may run concurrently only after the accepted
opening commit.  The independent verifier begins only after both handoffs are
frozen.  The closing referee begins only after the coordinator freezes the
integrated candidate.  Historical worktrees are read-only evidence.

## Roles

- Constructor: state and attempt the universal integral mixed-chain theorem,
  emit a finite proof certificate or fail-closed null frontier, and never
  accept its own work.
- Falsifier: independently search the full admissible interface for an exact
  obstruction and attack every claimed quantifier; it may not repair or
  accept the constructor.
- Independent verifier: read frozen producer inputs as untrusted data,
  reconstruct all algebraic and categorical checks without importing
  producer acceptance code, and issue the O3/O4 gate verdict.
- Closing referee: review only a frozen integrated candidate, replay opening
  and accepted gates, bind the verdict to exact Git objects, and enforce the
  proof-distance and strategy rules.

Each worker owns only its named `ops/team/d3-mixed-100-*` surface and works in
an isolated branch/worktree.  Only the coordinator edits this cycle directory
or integrates evidence.

## Resource ceiling

- total governed wall clock after opening: `4` hours;
- constructor/falsifier discovery checkpoint: `100` minutes;
- independent verification: `60` minutes;
- frozen closing review: `60` minutes, with the unused balance as buffer;
- peak resident memory: `16 GiB` per lane;
- new scratch: `2 GiB` total;
- constructor handoffs: `1`;
- verifier-directed repairs: `1`;
- cloud workers and external spend: `0`.

Stop immediately on source drift, an unstated restriction on `L_source`, a
chartwise or codimension-one claim promoted to global coherence, any local or
artificial boundary tagged as parent infinity, producer self-acceptance,
reuse of the singleton-root filler, or a claim that `3/10` is global coverage.

Positive handoff: `PROVED_UNIVERSAL_MIXED_100_CARRIER_AND_COHERENCE`.

Negative handoff: `DISPROVED_UNIVERSAL_MIXED_100_CARRIER_BY_EXACT_ADMISSIBLE_OBSTRUCTION`.

Null handoff: first missing universal proof edge with `NULL / STALLED / STOP`.

Timeout handoff: exact frozen proof frontier with all unfinished quantifiers
open and `TIMEOUT / STALLED / STOP`.

## Scope prohibitions

- no author contact or network research;
- no GitHub write, push, pull request, CI trigger, or merge;
- no cloud, paid API, external solver, or external compute;
- no raw Groebner, RUR, saturation, SREP, CAD, atlas, census, sampling, or
  residual-growth campaign;
- no construction of O1, O2, O5, or O6 in this cycle;
- no theorem credit from local canaries, formal taxonomy, modular ranks, or
  conditional statements missing any declared coherence quantifier;
- no canonical-state or theorem-ledger edit.

## Closing requirements

The cycle report must give exact base and frozen revisions, source pins, role
handoffs, proof/obstruction scope, independent hostile replay, opening,
midpoint and closing vectors, canonical and selected-route deltas, resource
accounting, nonconsequences, and exactly one of `CONTINUE`, `PIVOT`, `RETIRE`,
or `STOP`.  A positive theorem may justify a separately authorized pivot to
O1/O2; it does not automatically authorize global construction.

## Storage and publication authority

The saved local Codex project `E:\Projects\9DVL Research` is the canonical
working home. Bundles and manifests go under
`E:\Projects\9DVL Research\outputs`. A native-filesystem recovery mirror to
`G:\My Drive\Projects\research-backups` is optional, byte-length and SHA-256
verified, and nonblocking. Never invoke the Google Drive connector and never
block on connector approval. GitHub is read-only: do not push, publish
branches, open or update pull requests, trigger or rerun CI, or merge. This
opening authorization does not permit secrets, unrelated private files,
paid/external compute, force-push, history deletion, or irreversible external
action.

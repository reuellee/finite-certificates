# Frozen actual-parent review

Principal constructor handoff: `7d0efe1c55efff2d17c906569e90f5fb0ae0e371`,
integrated as `8b8adb7`. The worker committed successfully but encountered a
model-capacity error when returning its final message. The coordinator
recovered the saved commit and reran the standard-library verifier; no
producer file was repaired or changed during recovery.

The independent referee must reconstruct the following claims without
importing either producer's acceptance code:

1. The 1,680 choices exhaust exactly the proposed common-label support
   condition, including zero weights and supports using fewer than two
   incident triples.
2. The recorded parent is uniform and represents the pinned parent class;
   the three signatures are valid, nonempty, proper, and pairwise incomparable.
   Bind all auxiliary witnesses to the same labeled parent class.
3. Every retained-row system has an exact strict separating vector for at
   least one block. Reconstruct all 56 derived normals and all restrictions
   from primary data and check the integer inequalities. Check full badness
   separately. Search completeness and solver success are not acceptance rules.
4. The conditional escape theorem uses a genuine parent boundary and a
   proper path in the projective quotient, including zero weights and rank
   changes. A global continuous selection is not needed for Hc0; no pair-Hc1
   conclusion may be inferred.
5. Decide the exact route retirement and cycle-wide research credit. This
   is not a proposed D3 counterexample or evidence of a compact component.

The falsifier's separately frozen certificate may be added as a second
explicitly pinned input. No moving producer artifact may be accepted.

The referee owns only `ops/team/ai-d3-shared-pencil-referee` in a fresh
isolated worktree. Limit: 60 wall-minutes, 16 GiB RAM, 300 MiB new scratch,
within the existing program ceiling. No new research search, new cohort,
producer repairs, human contact, paid/external compute, push, or merge.
Record exact source hashes, an independent checker and hostile mutations,
scope, resources, and one acceptance or rejection decision. Stop at a
decisive defect or a completed review.

The source-existence review is already frozen at `ed10cbd5580e7266128cb477f5553bd122ee6805`
and the generic-countermodel review at `0de26f6a6561705b37d59dfb5d3886857ab96bd3`.
Their exact accepted scope is unchanged by this subsequent work order.

Stage 2c CALL 2-RETRY — finish the Pluecker-ideal cell-wide verifier and find certificates by undetermined coefficients. Your previous session timed out mid-task; do not restart from scratch — build on what is already on disk.

TIME BUDGET: ~20 minutes hard cap. CHECKPOINT DISCIPLINE IS MANDATORY: (1) within the first 5 minutes, run the verifier self-tests and write stage2c_gemini/call2_notes.md recording their outcomes; (2) update stage2c_gemini/call2_outcomes.json after EVERY single target, not at the end; (3) append to stage2c_gemini/symbolic_certs.json the moment any certificate verifies.

WORKING DIRECTORY: C:/Users/reuel/AppData/Local/Temp/claude/E--Projects/8f05ea6b-b743-4889-b448-362647b88861/scratchpad/finite-certificates/ai/maxout

DO NOT SEARCH THE FILESYSTEM. Read ONLY: stage2c_gemini/check_cellwide.py (EXISTS from your previous session — reuse it, fix it if buggy), stage2c_gemini/ansatz_catalogue.json, stage2c_gemini/extracted_certs.json, stage2c_gemini/mine_certs.py, stage2c_gemini/diagnosis.md, stage2c_gemini/call2_outcomes.json, stage2b_gpt/STAGE2B.md, stage2b_gpt/symbolic_gp_search.py, stage2b_gpt/symbolic_gp_results.json, stage2_gemini/STAGE2A.md. Python-load only (gzip+json): stage2b_gpt/reference_structure.json, stage2b_gpt/gordan_bundle.json.gz, stage2b_gpt/symmetric_coverage.json.gz.

Interpreter: E:/Projects/sae-identifiability/.venv/Scripts/python.exe
Rules: no network; never modify/delete files outside stage2c_gemini/ and never delete CALL 1 outputs; check_*.py naming; exact arithmetic for anything called a certificate; log seeds; labels via bundle index arithmetic only (idx -> rep ascending -> sigma in (rep, rep xor (2^20-1)) -> k in (1,2)).

STATE FROM YOUR TIMED-OUT SESSION: check_cellwide.py was written; call2_outcomes.json shows 5 targets equality_failed (53599, 50615, 65855, 53767, 55859 and residue 32316) and the rest not_attempted; symbolic_certs.json is []. The equality failures are EXPECTED if the CALL 1 rational fits reproduce the numeric certificate at U_ints but are not polynomial identities modulo the Grassmann-Pluecker ideal — that is precisely the cell-wide gap. The fix is NOT better fitting; it is SOLVING in the quotient ring.

GP SETUP (recap): p_abc = det(u_a,u_b,u_c), ten variables; ideal generated by the 5 three-term relations (for each a with remaining i1<i2<i3<i4: p_{a,i1,i2}p_{a,i3,i4} - p_{a,i1,i3}p_{a,i2,i4} + p_{a,i1,i4}p_{a,i2,i3}); D_abc = chi_abc p_abc with chi the exact chirotope of U_ints from reference_structure.json.

RECOMMENDED REFORMULATION (use it — it shrinks the problem): split B^T y = 0 into
  (T) the three T-columns: sum over support SIDE rows of lambda_s * sigma_s * (+/- C_ij(s)) = 0. Equivalent to five scalar identities (dot with each u_t, using <u_t, u_i x u_j> = p_tij), each required to lie in the GP ideal.
  (W) the five weight columns: with the side multipliers fixed, the weight-row multiplier for w_t is FORCED: mu_t = - sum over side rows of lambda_s * (signed s_t D_tij entry). No solving needed — mu_t is determined.
So the search space is ONLY the side-row multipliers lambda_s(p), and a target is proven cell-wide iff:
  1. (T) holds as GP-ideal membership;
  2. every lambda_s is manifestly nonnegative on the cell (coefficientwise nonnegative in the D's, possibly after adding a GP-ideal element), not all zero;
  3. every forced mu_t is manifestly nonnegative on the cell in the same sense (reduce modulo GP and try to reach a coefficientwise-nonnegative form in the D's; a Groebner normal form plus a bounded search over adding multiples of the 5 GP generators is acceptable);
  and positivity failures must be witnessed: an exact rational configuration in the SAME chirotope cell (verify full chirotope equality exactly; single-vector positive rescalings of U_ints are cheap probes) where the multiplier is negative -> SUBCELL_ONLY, not cell-wide.

METHOD — undetermined coefficients (the core of this retry):
For each target (sigma, k) and support S (take supports from the target's own numeric bundle certificate — side rows of its support; optionally allow the support to grow by 1-2 side rows found in related certificates):
  - Ansatz lambda_s = homogeneous polynomial in the ten p's of total degree d (start d=1, then d=2; stop at 3).
  - Impose the five dotted (T) identities modulo the GP ideal: reduce each identity's polynomial by a Groebner basis of the GP ideal (grevlex, QQ) and set all normal-form coefficients to zero -> a linear system over QQ in the unknown ansatz coefficients. Solve exactly (sympy linsolve / nullspace over QQ).
  - Intersect with positivity: among the nullspace, look for a vector making every lambda_s coefficientwise-nonnegative in the D-language (chi-twisted variables q_abc = chi_abc p_abc; a linear feasibility/small LP over the nullspace basis with rational arithmetic, or rational vertex sampling of the nullspace). Sanity anchor: the numeric bundle certificate at U_ints must be in the evaluated solution set at U_ints if the support is right.
  - Then compute the forced mu_t, reduce mod GP, classify positivity as above.
SELF-TEST FIRST (mandatory, before any target): (POS) push the known-good Stage 2a 5-cycle identity (stage2_gemini/STAGE2A.md) through the (T)-identity checker — it must PASS; (NEG) corrupt one coefficient — it must FAIL. Record both in call2_notes.md before proceeding.

SCOPE (tightened): work these 6 targets ONLY, in this order: 53599, 50615, 53767, 55859 (failed-100 group), 32316, 60552 (both-splits residue group). If a target resists at degree <= 2 after ~3 minutes, mark it honestly and move on. Only if all 6 are settled and time remains, continue with 65855 and further not_attempted targets.

DELIVERABLES (same paths as before; update, do not discard existing content — merge):
- stage2c_gemini/check_cellwide.py (final, runnable standalone: also accepts symbolic_certs.json and re-verifies every entry)
- stage2c_gemini/symbolic_certs.json (full polynomial data per verified or subcell certificate: sigma bits, k, support, lambda and mu polynomials as strings in D_abc, positivity class per multiplier, method)
- stage2c_gemini/call2_outcomes.json (statuses: proven_cellwide / subcell_only / equality_ok_positivity_undecided / no_solution_at_degree_<=d / equality_failed / not_attempted — with per-target detail)
- stage2c_gemini/call2_notes.md (self-test outcomes FIRST, then per-target notes, seeds, timings)

End by printing one line: counts per status, and the two most important file paths.

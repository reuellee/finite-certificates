Stage 2c CALL 3 of 3 — audit, scale, and write the STAGE2C.md ledger.

TIME BUDGET: ~20 minutes hard cap. CHECKPOINT DISCIPLINE: write a first draft of stage2c_gemini/STAGE2C.md by minute 12 and refine after; update JSON artifacts after every phase, not at the end.

WORKING DIRECTORY: C:/Users/reuel/AppData/Local/Temp/claude/E--Projects/8f05ea6b-b743-4889-b448-362647b88861/scratchpad/finite-certificates/ai/maxout

DO NOT SEARCH THE FILESYSTEM. Read ONLY: stage2c_gemini/check_cellwide.py, stage2c_gemini/symbolic_certs.json, stage2c_gemini/call2_outcomes.json, stage2c_gemini/call2_notes.md, stage2c_gemini/diagnosis.md, stage2c_gemini/ansatz_catalogue.json, stage2c_gemini/mine_certs.py, stage2b_gpt/STAGE2B.md, stage2b_gpt/symbolic_gp_results.json, stage2_gemini/STAGE2A.md. Python-load only (gzip+json): stage2b_gpt/reference_structure.json, stage2b_gpt/gordan_bundle.json.gz, stage2b_gpt/symmetric_coverage.json.gz.

Interpreter: E:/Projects/sae-identifiability/.venv/Scripts/python.exe
Rules: no network; never modify/delete anything outside stage2c_gemini/; check_*.py naming; exact arithmetic for anything called proven; log seeds; honesty over coverage.

STATE: CALL 2 found, by undetermined coefficients modulo the Grassmann-Pluecker ideal, degree-1 certificates claimed proven_cellwide for bundle idx 53599, 50615, 53767, 55859 (k=3 group) and 32316 (k=1), all of the SINGLE-CLASS EQUAL-PAIR shape: support {(i,j,+),(i,j,-), three weight rows}, equal monomial multipliers on the two side rows, monomial weight multipliers, all coefficientwise positive in the D's. 60552 found no positive solution at degree <= 2 (notes say so, but call2_outcomes.json still lists it not_attempted — fix that inconsistency). symbolic_certs.json contains DUPLICATE entries per idx (two monomial variants) — deduplicate, keeping variants as a list per target.

PHASE A — MANDATORY AUDIT (do this first; the ledger is worthless without it):
A1. The equal-pair no-go collision: STAGE2B.md Stage 2b-1 proves (via exact strict primal witnesses in symmetric_coverage.json.gz) that NO equal-pair certificate exists for the symmetric-residue classes. A single-class equal-pair certificate IS an equal-pair certificate. Therefore the claimed cert for "residue" target 32316 contradicts Stage 2b UNLESS the label was wrong. CALL 1 read the residue lists and reported entries like 32316 as "bundle indices" — determine the ACTUAL encoding of the residue lists inside symmetric_coverage.json.gz (representative sigma bits? representative index? bundle index?) by inspecting the artifact's own structure/keys/documentation fields in python. Decode correctly and answer: is the system (sigma=516678 as decoded via bundle order, k=1) actually in the k=1 residue set, the k=2 residue set, both, or neither? If it is NOT a residue class, say plainly: the CALL 1/2 "residue" targets were mislabeled, the certs are for non-residue classes, and the residue population remains uncovered. If it IS a residue class, then there is a genuine contradiction with Stage 2b — in that case re-verify both sides exactly (the cert via check_cellwide.py AND the Stage 2b primal witness) and report which one breaks. Do the same decode check for all 12 CALL 1 "residue" targets.
A2. Independent exact re-verification of all claimed certs: rebuild the integer row matrix B for each (sigma, k) at U_ints from reference_structure.json (bundle conventions), evaluate each symbolic certificate at U_ints with exact integers, and confirm y >= 0, y != 0, B^T y = 0. ALSO re-run check_cellwide.py on symbolic_certs.json for the ideal-membership + positivity verification. Both must pass for a cert to stay "proven".
A3. Reconcile k-conventions: the k=3 targets come from symbolic_gp_results.json mapped splits while the bundle stores k in (1,2); state exactly which split vector s was used in each proven cert and that it matches the target system.

PHASE B — SCALE THE MECHANISM (the systematic family):
The single-class mechanism is purely combinatorial: system (sigma, s) has a cell-wide degree-1 certificate on class (i,j) iff sigma_{ij,+} = sigma_{ij,-} and sigma_{ij} * s_t = -1 for all three t not in {i,j} (then y = [D_kij on both side rows; weight rows w_t: D_tij * D_kij appropriately] with every multiplier a positive monomial — write the general formula once and prove it once, symbolically, via check_cellwide.py on the GENERIC pattern, not per-instance). Scan ALL 16,570 representatives x both labeled members x k in {1,2} (and note k=3,4 follow by global flip) with pure bit arithmetic: for each system, does ANY of the 10 classes satisfy the criterion? Produce exact counts: systems covered by the single-class family; overlap with (i) the 100 failed symbolic targets (all four k-mapped systems each), (ii) the correctly-decoded k=1 residue, k=2 residue, and both-splits residue sets (expected: zero residue overlap — confirm or report the contradiction), (iii) the classes covered by Stage 2b-1 equal-pair (this family is a cell-wide subfamily of it). Save the scan to stage2c_gemini/family_scan.json (counts + the criterion + per-population overlaps; do not dump all 33,140 rows, store counts and compressed lists of covered failed-100 targets).
PHASE C — only if time remains: attempt degree-1/degree-2 undetermined-coefficient certs (general supports, GP-ideal T-cancellation) for 2-3 correctly-decoded residue classes — these are the classes where equal-pair provably fails, so any success would be a genuinely new mechanism. Record outcomes honestly (a clean "no solution at degree <= 2 on tried supports" is valuable).

PHASE D — THE LEDGER: write stage2c_gemini/STAGE2C.md:
- Headline: exactly what was proven, with counts. No inflation: if the certs cover only non-residue, equal-pair-coverable classes, the honest headline is "first CELL-WIDE (symbolic, Pluecker-ideal-verified) certificates, forming a systematic single-class family covering N of the 33,140x2 systems and M of the failed-100 targets; the equal-pair residue remains uncovered cell-wide".
- Method section: the Pluecker-ideal verification principle (weight columns + T columns dotted with u_t; GP ideal generated by the 5 three-term relations; positivity from chirotope signs).
- Proven / conjectured / failed ledger with explicit counts, including: the audit outcome for the mislabel/collision question; 60552 and 65855 negatives; the CALL 1 rational-fit hypotheses failing ideal membership (equality_failed) — meaning the numeric-fit route is dead and the quotient-ring solve is the viable route.
- Honest frontier: what remains open (residue classes cell-wide; degree bounds; the global max f0(3,5)=42 over all cells; transfer beyond the reference chirotope).
- Reproduction: exact commands with the interpreter path, run from the working directory (the checker rerun on symbolic_certs.json must be one command); all seeds from calls 1-3.
- File inventory of stage2c_gemini/ with one-line descriptions.
Also update symbolic_certs.json (deduplicated, audited statuses, generic-family entry with its one-time symbolic proof) and call2_outcomes.json (fix 60552; downgrade any cert that failed the audit).

End by printing: the headline sentence, counts per ledger category, and the paths of STAGE2C.md, symbolic_certs.json, family_scan.json.

# Diagonal three: exact parent-positive sign certificates

## Result

Among the 17,824 candidate full-support residual factors for the strict
row-2599 parent cell:

```text
exact interior crossings                10,844
exact empty zero sets by sign identity   1,177
still unresolved                         5,803
total                                   17,824
```

The 1,177 empty factors have canonical sorted-ID digest

```text
f9022c922d56c69ea541d6098cea89004bdfcff73b01a411a159a78b5c369484
```

The 5,803-factor open set has digest

```text
0c85f0e7370963df311a5a7d04e175e4a801b79c54fe0600a69bb777bf30a76a
```

No feasibility claim is made for those 5,803 factors.

## Certificate theorem

Write

\[
Q_I=\chi_I[ I]
\]

for the target-signed row-2599 parent brackets in the normalized nine-variable
chart. Every `Q_I` and every chart coordinate is strictly positive in the
strict parent cell.

The certificate identities have one of three forms:

\[
\epsilon p=\sum Q_AQ_B,
\qquad
\epsilon p=\sum x_kQ_AQ_B,
\qquad
\epsilon p=\sum x^uQ_A,
\]

where `epsilon` is `+1` or `-1`. Each right-hand side is strictly positive,
so the residual factor `p` has a fixed nonzero sign and its zero set is empty
in the strict parent cell.

The broadest screen finds 965 factors directly as sums of signed-parent
products. Its factor-ID digest is

```text
4121a7abb7610d969ed18cecc5c3df918a6a3263633efb2a1e99129dd3c4469c
```

A fixed bank of 150 short seed identities includes 63 coordinate-shifted
product identities and two parent-monomial identities. Moving-column
permutations are used only to transform these polynomial formulas. Each
transport is accepted only when all transported parent factors match actual
row-2599 signed parent factors with one common summand sign. Unioning these
conditional identities with the 965 direct products gives 1,177 distinct
base-open factors.

## Exactness boundary

Floating-point linear programming is discovery-only. It proposes unit
coefficients in the positive cone generated by parent-bracket products. A
factor is accepted only when the proposed identity expands to literal
integer polynomial equality. Numerical error can therefore cause only a
missed identity or a failed replay, not a false theorem.

The verifier also intersects the result with the exact 6,980-factor base-open
set and fails if any fixed-sign factor conflicts with one of the 10,844 exact
segment crossings.

Replay:

```bash
PYTHONDONTWRITEBYTECODE=1 python ai/omreal/verify_diag3_pair_fullsupport_parent_product_signs.py
```

## Strategic consequence

The positive-identity pass removes 1,177 factors without CAD and without an
invalid symmetry quotient. The next wall-feasibility target is the 5,803
factor residue, but wall classification is still only an input to the actual
diagonal-three obligation: a coverage-certified nonrelative master closure
complex and exact middle-rank replay.

The honest 9DVL score remains **2/9**.

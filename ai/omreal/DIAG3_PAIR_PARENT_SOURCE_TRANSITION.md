# Exact row-2599 parent-source transition

This certificate advances the invariant diagonal-three pair obligation from one local parent germ to an exact connection between two genuinely distinct stored germs.  It does not close that obligation and does not change the honest 9DVL theorem score from `2/9`.

## Objectively selected segment

The row-2599 bank contains 178 exact normalized parent-chart points.  The earlier parent-safe segment census certifies 105 straight segments, of which 57 leave chart 0.  Among those 57, the segment from chart 0 to chart 89 has the minimum exact endpoint factor-state Hamming distance, 1,197.  This makes it the smallest certified source change on which to develop label continuation.

All 70 row-2599 parent brackets restrict to rational univariate polynomials on the segment.  Recursive exact Bernstein positivity proves their prescribed signs on the closed segment.  Thus the entire segment remains inside the parent stratum, with no parent-infinity cell.

## Complete residual roadmap

The verifier restricts every one of the 17,824 candidate residual factors to the segment and uses exact Sturm counts and rational root isolation on the open parameter interval.  The result is:

- 16,607 factors with no root;
- 1,197 factors with one root;
- 20 factors with two roots;
- 1,217 rooted factors and 1,237 distinct, strictly ordered root events;
- every event a sign crossing, with isolating width at most `2^-22`;
- 1,179 occurrence-multiplicity-one events, 13 multiplicity-two events, 6 multiplicity-15 events, and 39 multiplicity-65 events.

Flipping each factor state at its ordered events exactly reconstructs the stored chart-89 factor state from chart 0.  In particular, the 20 two-root factors flip twice, while the endpoint Hamming distance remains 1,197.

The induced one-dimensional regular CW path has 1,239 zero-cells, 1,238 one-cells, 2,477 cells total, and 2,476 strict closure pairs.  Its endpoints are ordinary scope cells and its parent-infinity subcomplex is empty.

## Label-continuation follow-on

The residual roadmap initially left signature-label continuation open at the 58 compound events.  The first compound event is event 38: factor 8,652, a cubic restriction with occurrence multiplicity 65, isolated in

```text
[75295/4194304, 2353/131072].
```

That gap is now closed by `DIAG3_PAIR_PARENT_SOURCE_LABELS.md`.  Its hybrid exact verifier updates all 1,179 simple events by antipodal simplicial mutations and performs full tope enumeration only after the 58 compound events.  The complete continued endpoint equals the independently stored raw chart-89 label state.  Global parent-cell coverage remains open.

## Replay

```bash
python ai/omreal/verify_diag3_pair_parent_source_transition.py
```

The verifier rebuilds the complete certificate from the pinned exact sources, checks the root ordering, endpoint factor-state reconstruction, CW counts, theorem-scope language, and rejects nine hostile corruptions.

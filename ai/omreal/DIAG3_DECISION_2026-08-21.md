# Diagonal-three research decision — 2026-08-21

## Decision

The active target is now the **full-support master-closure compiler**, not
another standalone wall-feasibility sweep.

The honest 9DVL score remains **2/9**. This decision changes the research
process and target ordering; it does not claim diagonal three.

## Audit finding

The project has unusually strong exact certificate practice, but its research
state was distributed across prose, historical open objects, and pull-request
descriptions. That produced two concrete contradictions:

1. historical generator preflight records still named the paired
   four-dimensional proper supports as the next target, even though the later
   exact relative-boundary theorem proves that all 3,374 proper supports
   generate zero relative chains; and
2. one current note promoted a bounded degree-one multiplier screen as the
   next action while the broader strategy checkpoint correctly identified the
   coverage-certified master closure complex as the proof-bearing bottleneck.

The new decision ledger makes historical artifacts auditable without allowing
them to select current work.

## Invariant obligation graph

Diagonal three requires both branches:

- **Triple branch:** prove
  `H_c^0(B_0 intersection B_1 intersection B_2;Q)=0`. The current sufficient
  factor route leaves 1,162,302 of 79,102,449 source orbits unresolved.
- **Pair branch:** prove injectivity of the alternating pair-to-triple
  restriction map. The smallest sufficient certificate is a
  coverage-certified finite integral relative complex whose mod-two middle
  rank is exact.

Neither branch can cancel the other in the closed-cover spectral sequence.

## Correct row-2599 pair frontier

The current exact full-support ledger is

| class | count |
|---|---:|
| exact interior wall | 10,844 |
| exact empty zero set | 1,177 |
| feasibility unresolved | 5,803 |
| total | 17,824 |

The master generator therefore has at most 16,647 active walls after deleting
the 1,177 exact empty factors. An unresolved feasibility factor stays in the
generator input until the generator itself certifies that it has no cell.

All 3,374 proper supports are relative. The former dimension-four support
subdivision is retired as a theorem target. Parent-wall and coordinate escape
still have to be retained as relative **frontier tags** on closures of
nonrelative cells.

## Target ranking

The canonical scoring rule gives:

| target | priority |
|---|---:|
| full-support master-closure compiler | **35** |
| triple boundary-stratified roadmap canary | 24 |
| standalone degree-one multiplier screen | 18 |

The degree-one multiplier language remains useful only as one bounded
preprocessing pass inside the master generator. Its output cannot by itself
change the diagonal-three theorem score.

## Selected experiment contract

### Question

Can a deterministic exact generator and independent verifier construct the
order-two closure data for the strict row-2599 parent interior while treating
every genuine parent-wall escape as relative?

### Version-one certificate interface

A generated certificate must contain:

1. a pinned active-factor input and exact chart/domain identifiers;
2. nonrelative cell IDs, dimensions, exact witnesses and factor signs;
3. every strict closure-comparable pair and strict three-cell chain needed by
   the barycentric two-skeleton;
4. simultaneous-factor and rank-drop tags;
5. genuine parent-infinity/frontier tags, with no local-scope boundary
   substitution;
6. complete bad-signature labels whose lower-cell sets are closed;
7. coverage, disjointness, no-duplicate and regular-ball certificates;
8. integral signed incidences satisfying `d^2=0`;
9. the unsigned mod-two middle-rank replay; and
10. deterministic build metadata and an independent verifier.

### Canaries

The verifier must reject:

- an unsigned moving-column transport that flips a target-signed parent
  factor;
- a sampled chart bank presented as parameter-space coverage;
- a local outer boundary mislabeled as parent infinity;
- a closure relation that does not decrease dimension;
- a corrupted empty-factor or residue digest.

### Stop rule

Every projection, CAD, or roadmap pilot must declare one finite layer and
preserve its exact frontier if it exceeds the resource ceiling. A failed pilot
must identify the generator obstruction it exposes. It may not be followed
automatically by a wider version of the same search.

## Tooling consequence

A complete sign-invariant CAD is not automatically the right object. The pair
proof only needs connected strata and order-two closure incidence, so exact
semialgebraic roadmap or critical-point methods should be tested as compiler
backends. SAT/SMT, modular arithmetic, numerical optimization, and CAS may
discover decompositions or identities, but exact coverage and independent
replay remain the trust boundary.

A proof assistant is a sensible later target for the small stable reductions:
the obligation split and the integral-lift/mod-two rank implication. It will
not replace the generated coverage certificate.

## Work completed in this checkpoint

- established the canonical decision ledger;
- encoded the obligation graph, current counts, retired routes, target scores,
  experiment contract and publication gates;
- added an independent standard-library verifier;
- linked historical open objects back to the canonical ledger;
- selected the proof-bearing master-closure compiler as the next target.

This checkpoint also implements the version-one certificate schema and a
small exact regular-cell fixture whose independent verifier rejects six
hostile corruptions. The next code-bearing checkpoint should connect that
interface to one bounded proof-producing roadmap or critical-point backend on
a full-support canary before attempting a large row-2599 decomposition.
